Centralised Blotter Mapping Studio - stitched raw OCR audit
Generated from ordered HEIC sequence in /Users/nigelli/Desktop/Centralised Mapping

// OCR PHOTO 001: IMG_7125.HEIC
<meta name="viewport" content=*width=device-width, initial-scale=1.0* />
<script src=*https://unpkg.com/xlsx@latest/dist/xlsx.full.min.js*>c/script>
<script src=*https://unpkg.com/highcharts@latest/highcharts.js*></script>
<link rel="icon" href="/studio/static/ui/hsbc_icon.ico* />
ot 241
<title>Centralised Blotter Mapping Studio</title>
- +
smeta charset=*UTF_8* />
--hsbc-red:#db0011;
soft-green:#ecfdf3;
--soft-amber:#fffaeb;
--muted:#667085;
<!DOCTYPE html>
<html lang-"en'»
--ink:#101828;
--Line:#e4e7ec;
--blue:#175cd3;
-green:#027a48;
-amber:#b54708;
--purple:#6d28d9;
--soft-blue:#efffff;
--soft-red:#fef312;
--red:#b42318;
—-bg:#f4f6f8;
--card:#ffffff;
Search
cheaoz
<style>
troot!

// OCR PHOTO 002: IMG_7126.HEIC
font-family:-apple-system,"Segoe UI", Roboto,Helvetica,Arial,sans-serif;
of 241
box-shadow:0 4px 12px rgba(16,24,40,03):
--shadow:0 2px 10px rgba(16,24,40.06);
background:rgba(255,255,255,.97);
border-bottom: 1px solid var(-line):
- +
#ready-marker{display:none}
backdrop-filter:blur(10px);
-soft-orange:#fff7ed;
*(box-sizing:border-box}
background:var(--bg):
body[min-height:100vh}
--topbar-h:82px;
color:var(-ink);
position:sticky:
width:100%;
,topbar-inner!
Search
html,bodyl
margin:0;
padding:0;
z-index:30;
topbar(
top:0;

// OCR PHOTO 003: IMG_7127.HEIC
Jall ullit

// OCR PHOTO 004: IMG_7128.HEIC
o241@ID
border:1 px solid var(-line):
justify-content:flex-end;
text-decoration:none;
align-items:flex-start;
border-radius:999px;
color-var(-blue):
display:inline-flex;
align-items:center;
font-weight:600;
padding:4px 10px;
flex-wrap:wrap:
flex:O 1 1100px;
background:#fff;
color:#344054;
font-size:11 px;
font-weight:600;
display:flex;
Search
brand al
gap:6px;
gap:6px;
pills!
-pill

// OCR PHOTO 005: IMG_7129.HEIC
-pill.amber{background:var(--soft-amber);border-color:#fed7aa;color:var(--amber)}
pill.green{background:var(--soft-green):border-color:#b7e4c7;color:var(--green)}
zm
-pill.blue(background:var(--soft-blue);border-color:#bfdbfe;color:var(--blue)}
-pill.red{background:var(-soft-red);border-color:#fecaca;color:var(--red)}
-pitl.purple(background:#f4f0fe;border-color:#ddd6fe;color:#6d28d9}
of241 OTD
-pill.green:before[background:#12b76a}
_pill.amber:before(background:#f79009)
-pill.purple:before(background:#7c3aed}
_pill.red:before(background:#f04438)
-pill.blue:before{background:#2e90fa}
- +
white-space:nowrap;
border-radius:999px;
background:#98a2b3;
display:inline-block;
max-width:1720pxj
line-height:1.5;
flex:0 0 auto:
margin:0 auto;
Search
pill:beforel
content:**:
width: 7px;
height:7px;
shellf

// OCR PHOTO 006: IMG_7130.HEIC
o241 @| [D
ДокбетЛвe ниne ao
bau groune мan чaне,
padding: tapx
Search
loane

// OCR PHOTO 007: IMG_7131.HEIC
8+ 241
- +
field.narrow{flex:0 0140px)
field.medium(flex:0 0 180px}
field.tight/flex:0 0 220px}
field.wide(flex:2 1 480px}
text-transform.uppercase:
flex-direction:column:
letter-spacing: 05em;
align-items:end;
min-width:170px;
coloriver(-muted);
flex-wrap:wrap:
font-weight:700;
flex:1 1 170px;
font-size:10px;
display:flex;
display:flex;
Search
controls!
gaр:8px;
gap:4px;
field labell
_fieldi

// OCR PHOTO 008: IMG_7132.HEIC
input[type='file"].input|type="text"],input[type=*number"],select,textarea(
ot 241
box-shadow:0 0 0 3px rgba(219,0,17,.08);
input:focus,select:focus,textarea:focus{
border:1 px solid var(-hsbo-red);
border-colorvar(-hsbc-red):
border:1 px solid #d0d5dd;
padding:7px 10px;
min-height:36px;
border-radius:8px;
background:#fff;
color var(-ink);
min-height:90px;
font-size:13px;
line-height: 1.45;
resize vertical;
width: 100%;
outline:none;
Search
fontinherit
textarea
btnf

// OCR PHOTO 009: IMG_7133.HEIC
btn.ghost(background:transparent;color:#344054;border:1px solid transparent;box-
_btn.secondary{background:#fff;color:#101828;border:1px solid #d0d5dd;box-
of241 | [D
_btn.secondary:hover[background:#f8fafc;border-color:#98a2b3}
.btn.ghost:hover(background:#f8fafc;border-color:#e4e7ec)
btn:hover(background:#b8000e;border-color:#b8000e}
note,-good-note, warn-note, bad-note, blue-note(
btn:disabled(opacity:.5;cursornot-allowed}
box-shadow:0 1px 2px rgba(219,0,17,,18);
- +
background:var(-hsbc-red):
border:1 px solid var(-line):
padding:8px 14px;
border-radius:8px;
padding:10px 12px;
border-radius:10px:
min-height:36px;
font-weight:600;
font-size:13px;
cursor pointer:
font-size:12px,
line-height:1.45;
Search
shadowinone}
shadowinone}
color:#ffT:

// OCR PHOTO 010: IMG_7134.HEIC
•good-note{background:#ecfdf3;border-color:#b7e4c7;color:#14532d}
warn-note(background:#fffaeb;border-color:#fed7aajcolor:#7c4400)
O241D ED
bad-note(background:#fef3f2;border-color:#fecaca;color:#7a271a}
•
font-family:Consolas,Monaco,monospace,
.note(background:#f8fefc;color:#344054)
border-bottom:2px solid var(-tine):
- +
border: 1 px solid var(--Line);
display:inline-block;
background:#f8fafc;
background:var(--bg):
padding:1px 6px;
border-radius:6px;
top:var(-topbar-h);
font-size:11 px:
position:sticky;
flex-wrap wrap:
margin:O 0 16px;
padding:0 4px;
Search
inline-codel
2-index:20;
display:flex;
tabbarl
gap:2px;

// OCR PHOTO 011: IMG_7135.HEIC
@media (max-width:1500px).grid-7[grid-template-columns:repeat(4,minmax(0,1fr))]}
@media (max-width:1400px)(-grid-6(grid-template-columns:repeat(3,minmax(0,1fr))}}
@media (max-width:1350px)(-grid-7{grid-template-columns:repeat(3,minmax(0,1fr))#
@media (max-width:1100px)( grid-2, grid-6,grid-7(grid-template-columns:1fr))
zm
tab.active(color:var(=-hsbc-red);border-bottom-color:var(--hsbc-red)]
OID
tab:hover{color:var(--ink):background:rgba(15,24,40,.03)}
-grid-2(grid-template-columns:repeat(2,minmax(0,1fr))}
grid-6(grid-template-columns:repeat(6,minmax(0,1fr))}
grid-7{grid-template-columns:repeat(7,minmax(0,1fr))]
C$ 241
border-bottom:2px solid trensparent;
- +
padding:10px 12px 8px;
background:transparent;
_view.active[display:block)
-grid(display:grid;gap:12px)
mergin-bottom:-2px;
white-space.nowrap;
view[display:none]
color:#475467;
font-size:12.5px:
font-weight:600;
cursor:pointer;
border:none:
Search
tab!

// OCR PHOTO 012: IMG_7136.HEIC
of 241
12
font-variant-numeric.tabular-nums:
- +
border: 1 px solid var(--line);
text-transformiuppercase,
letter-spacing:.06em;
letter-spacing*-01 em
padding:12px 14px;
border-radius:12px:
color:var(-muted);
margin-bottom:6px;
margin-bottom:5px;
color:var(--muted);
background:#fff;
font-size:10px;
font-weight:700:
font-size:26px;
font-weight:700;
line-height:1.02;
font-size:11.5px;
Search
kpi tabel
kpi value!
.kpi .subl
kpil

// OCR PHOTO 013: IMG_7137.HEIC
0t 241
justify-content:space-between;
- +
border:1 px solid var(--line):
border-collapse:collapse)
margin-bottom: 10px;
align-items:center;
border-radius:10px;
line-height:1.35;
max-width:320px;
flex-wrap:wrap;
background:#fff;
overflowauto:
font-size:12px;
display:flex;
width: 100%;
table-wrapl
width: 100%;
Search
toolbar
gap:8px;
search"
table{

// OCR PHOTO 014: IMG_7138.HEIC
zm
.mono(font-family:Consolas, Monaco, monospace;font-size:11.5px)
.wrap-table th,wrap-table td(white-space:normaliline-height:1.4}
6f 241
tbody trinth-child(even)[background:#fafbfc}
td{font-variant-numericitabular-nums}
border-bottom: 1px solid var(-line):
trilast-child td{border-bottom:none}
tbody tr.hover(background:#effftf}
- +
text-transform:uppercase,
white-space:nowrap;
background:#f8fafc:
letter-spacing:.05em;
.num(text-align:right)
padding:8px 10px;
vertical-align:top;
text-align:left;
position:sticky:
color:#475467;
font-size:10.5px;
font-weight:700;
Search
z-index:1;
th,td
top:0;
th

// OCR PHOTO 015: IMG_7139.HEIC
zm
.mono(font-family:Consolas, Monaco,monospace;font-size:11.5px)
•wrap-table th,wrap-table td/white-space/normal;line-height:1.4}
ot 241
tbody trinth-child(even)[background:#fafbfc}
td{font-variant-numenic.tabular-nums)
border-bottom: 1px solid var(-line):
tr.last-child td{border-bottominone}
tbody tr.hover[background:#eff6ff}
text-transform:uppercase,
white-space:nowrap:
background:#f8fafc;
letter-spacing:.05em;
.num(text-align.right)
padding:8px 10px;
vertical-align:top:
text-align:left;
positionisticky:
color:#475467;
font-size:10.5px;
font-weight:700;
Q Search
z-index:1;
th,td{
top:0;
th

// OCR PHOTO 016: IMG_7140.HEIC
of 241
16
-processed-table editable-cellinput[
background:#fff7ed limportant;
outline:2px solid var(-hsbc-red);
border: 1 px solid #d0d5dd:
outline-offset:-2px;
editable-cell.editingl
outline-offset:-2px;
display-intine-block;
border-radius:999px;
padding:4px 6px:
min-width:90px;
min-height:28px;
padding:4px 6px;
border-radius:Spx;
background:#fff;
color:var(-ink):
font-size:12px;
line-height: 1.35;
padding:2px 7px:
width: 100%;
Search
fonttinherit;
badgel

// OCR PHOTO 017: IMG_7141.HEIC
_badge.green(background:#ecfdf3;border-color:#b7e4c7;color:#027a48}
.badge.amber(background:#fffaeb;border-color:#fed7aa;color:#b54708}
_badge.purple(background:#f4f0fe;border-color:#ddd6fe;color:#6d28d9}
badge.red(background:#fef3f2;border-color:#fecaca;color:#b42318}
badge.blue{background:#eff6ff;border-color:#bfdbfe;color:#175cd3}
ot 241
border: 1 px solid var(--line);
border:1px deshed #dod5dd;
letter-spacing:.01em;
background:#0f172a;
padding:26px 16px;
color:var(-muted);
border-radius:10px;
font-weight:700;
background:#fff;
text-align:center;
font-size:10px:
color:#344054;
background:#fff;
font-size:12.5px;
line-height:1.5;
Search
emptyl
.log-boxl

// OCR PHOTO 018: IMG_7142.HEIC
.log-line(padding:2px 0;border-bottom:1px dotted rgba(255,255,255,.08)}
101 241
font-family:Consolas,Monaco,monospace;
.log-line:last-child{border-bottom:none}
border:1 px solid #1e293b;
Log-success{colon:#86efac)
white-space:pre-wrap:
_log-info{color:#cbd5e1}
log-warn{color:#fde68a}
log-error|color:#fca5a5}
•plain-list li(margin:4px 0}
border-radius:10px;
margin:8px 0 0 18px;
max-height:520px;
color:#e2e8f0:
padding: 12px:
overflow:auto;
font-size:11px;
line-height:1.45;
font-size:12px;
line-height: 1.5;
color:#344054;
Search
padding:0;
plain-list(

// OCR PHOTO 019: IMG_7143.HEIC
0$ 241
border:1 px solid var(--line);
border:1 px solid var(-line):
align-items:center;
border-radius:10px;
min-height:320px;
align-items:stretch;
border-radius:12px;
background:#f8fafc;
margin-bottom:14px:
flex-wrap:wrap;
background:#fff;
position:relative;
user-select:none;
.action-rowl
display:flex;
width: 100%;
padding:12px;
Search
gap:8px;
chart-box!
ms-canvas"
display:flex;
gap:20px:

// OCR PHOTO 020: IMG_7144.HEIC
_ms-col-head|display:flex;flex-direction:column;gap:6px)
of 241
ms-canvas-mid(flex:1 1 auto;min-width:80px}
.ms-canvas-col.rhs(flex:1 1 620px}
text-transform:uppercase;
flex-direction:column;
letter-spacing:.06em;
flex-direction:column;
overflow:hidden:
min-height:480px;
color:var(--muted):
_ms-canvas-col
font-size:10.5px;
max-height:420px;
font-weight:700;
flex:0 0 320px;
min-width:0;
display:flex;
ms-col-titlel
ms-chip-list{
display:flex;
Search
gap:8px;
z-index:2;
gap:6px;

// OCR PHOTO 021: IMG_7145.HEIC
transition:border-color 120ms ease,box-shadow 120ms ease,background 120ms ease;
.ms-chip:hover{border-color:#98a2b3;box-shadow:0 1px 4px rgba(16,24,40,.08)}
width:8px;height:8px;border-radius:999px;flex:0 0 auto:background:#d0d5dd;
zm
_ms-chip.hover-linked{border-color:#175cd3;background:#eff6ff}
0/ 241
21
.ms-dot.source/background:#12b76a}
.ms-dot.rule(background:#6d28d9)
border: 1 px solid #0d5dd;
border-radius:8px;
padding:6px 10px;
align-items.center:
position relative;
background:#fff;
line-height:1.35;
overflow: auto:
font-size:12px;
padding:2px;
ms-chip-row
display:flex;
min-width:0;
Search
.ms-chip(
gaр:6px;|
.ms-dot!

// OCR PHOTO 022: IMG_7146.HEIC
Font-size:10.5px;color:var(--muted);overflow:hidden;text-overflow:ellipsis;white-
font-weight:600;color.var(--ink);overflow:hidden;text-overflow:ellipsis;white-
zm
.ms-tag.source[background:fecfdf3;border-color:#b7e4c7;color#027a48)
ms-tag.blank{background:#f3f4f6;border-color:#e5e7eb;color-#6b7280}
.ms-tag.rule(background:#f4f0fe;border-cotor./#ddd6fe;color:#6d28d9}
TOIE
an tet
.ms-dot.const/background:#2e90fa}
.ms-dot.blank[background:#d0d5dd}
space:nowrap;margin-top:1px;
border: 1 px solid var(--line);
text-transform:uppercase;
display inline-block;
border-radius:999px;
background:#f8fafc;
letter-spacing:04em;
.ms-chip-namel
spaceinowrap:
Ims-chip-sub!
padding:0 6px;
font-size:9.5px;
font-weight:700;
color:#475467;
flex:0 0 auto:
Search
_ms-tagt

// OCR PHOTO 023: IMG_7147.HEIC
#msSvg path.default-line{strokeirgba(152,162,179,55):stroke-width:1.5:stroke-
position:absolute,inset:0;width:100%;height:100%;pointer-events:none;z-
#msSvg path.user-line(stroke:rgba(219,0,17,9);stroke-width:2;filt:none}
.ms-star(color:#db0011/font-weight:700;margin-left:2px}
юt 241
#msSvg path.hot(stroke-width:3;opacity:1}
- +
#msSvg path.dim[opacity:.16}
-processed-table th.select-col,
-processed-table td.select-col
processed-table th.source-col
-processed-table td.source-col
index:1;overflow.hidden;
dasharray: 5 4 fill:none)
position:sticky:
background:#fff;
text-alignicenter;
min-width:46px;
positionisticky;
background:#fff;
Search
width: 46px;
z-index:3;
left: 46px;
z-index:3;
#msSvgl
left:0;

// OCR PHOTO 024: IMG_7148.HEIC
01 241
ms-canvas(flex-direction.cotumn;min-height:auto)
ms-canvas-col,.ms-canvas-col.rhs(flex:1 1 autol
processed-row-excluded td.trade-cokopacity:1)
-processed-row-excluded to(opacity:.52)
-processed-row-excluded td.select-col,
processed-row-excluded td.source-col,
processed-table thead th.select-col,
-processed-table thead th.source-col,
processed-table thead th.trade-col
.ms-canvas-mid(display-none)
_processed-table th.trade-col,
«processed-table td.trade-col
Omedia (max-width:1150px)[
background:#f8fafc;
min-width:92px;
position:sticky:
background:#fff;
min-width: 140px;
Search
width:92px;
left: 138px;
2-index:3;
z-index:4;

// OCR PHOTO 025: IMG_7149.HEIC
<strong>three Trade ID modes</strong>, preserves <strong>mapping studio</strong>,
Cross-asset extension of the current Linear Zero board, using one centralised output
zm
<strong>processed-sheet editing</strong>, <strong>exceptions</strong>,
<strong-Structured Fl / Linear Zero</strong>, <strong>Collar</strong>,
<strong>Illiquid Credit + Repack</strong>, and <strong>Equity TRS</strong>.
Output keeps <strong>Primary Amount</strong> instead of <strong>*$
<strong>golden record</strong>, and <strong>selected-row export</strong>.
<a href=*/studio/shared-html/YE32UpkrboADOQLtMmaLohqjypizQ9ES*
<div id="ready-marker" data-status="loading">loading</div>
of 241
<h1>Centralised Blotter Mapping Studio</h1>
target="_blank" rel="noopener"›current board</a>.
<body data-htmlid=*1783996576871* >
<strong>Al formula copitot</strong>,
<div class="topbar-inner"y
#msSvg(display-none}
<header class="topbar"*
Volume</strong>, supports
<div class="brand">
Source reference:
<div class=*pills">
template across
Search
</style>
cyhead
<р>
</р>
</div>

// OCR PHOTO 026: IMG_7150.HEIC
<span id="pillAsset" class="pill blue">Asset: Structured FI / Linear Zero</span>
<span id="pillDateFmt" class="pill blue">Date format: dd/mm/yyyy</span>
<span id="pillWorkbook* class="pilt amber">Workbook: none</span>
<span id="pillTradeld" class=*pill blue">Trade ID: business key</span>
<span id="pillMode" class="pitt blue">Mode: trade by trade</span>
<span id="pillRute" class="pill blue">Rulebook: by asset</span>
<span id="pillRutes" class="pill purple">Custom Rules: 0</span>
<span id="pillManual" class="pill amber">Manual Edits: 0</span>
<span id="pillGolden" class="pill blue">Golden Trades: 0</span>
<span id="pillSelected" class="pill blue">Export Rows: 0/0</span>
<span id="pillSheet" class="pill amber">Sheet: none</span>
<span id="pillAi" class="pill purple">Al Formuta: idle</span>
<span id="pillExceptions" class="pill*>Exceptions: 0</span>
<span id="pillFiles" class="pitl blue">Inserted Files: 0</span>
<span id="pillProcess" class="pill">Process: idle</span>
<inputid="filelnput" type="file" accept=*xlsx, xls, csv* />
07 2411
<div class=*section-title*>Inputs & Controls</div>
26
<label>Workbook upload</abel>
<section class="card section">
«div class="field wide"»
<div class=controls">
<main class="shell'>
Search
</header>
</div>
</divp

// OCR PHOTO 027: IMG_7151.HEIC
sinput id="addonWindowDays" type="number" min="0" max="30" step="1" value="7"
<option value="native_ref_preferred'>Deterministic native-ref preferred ID</option>
zm
<div class="field narrow" id="addonWindowField" style="display:none">
<option value="business_key">Deterministic business-key ID</option>
<option value="Legacy_row_order">Legacy row-order ID</option>
soption value=**>Upload a workbook first</option>
ют 241
<select id=*runModeSelect">c/select>
<label>Add-on window (days)</label>
<select id=tassetSelect*»c/select>
cselect id="sheetSelect' disabled>
«select id=*tradeldModeSelect>
<label>Trade ID mode‹/label>
- +
<div class=*field medium>
clabel>Worksheet</label>
-div class="field medium">
clabel>Run mode</label>
cdiv class=*field medium*>
clabel>Assets/label>
<div class="field tight">
Search
</select»
</div>
<ydive
</div>
</div>
</div>

// OCR PHOTO 028: IMG_7152.HEIC
<button id="btnExportXlsx" class="btn secondary' disabled>Export XLSX≤/button>
«button id="btnProcess" class="btn' disabled>Process selected sheet</button>
<button id="btnExportCsv" class-"btn secondary" disabled>Export CSV</button>
<button id="btnResetRules" class="btn ghost">Reset current asset rules</button>
3. Output dates are always <span class="inline-code">dd/mm/yyyy</span><br />
6. Add-on window is shown only for assets that support add-on amalgamation
zm
4. Output « span class=*inline-code'>Book</span> stays blank<br /3
o241 @1CD
1. Final output template is centralised across all assets<br />
listed in <span class=*intine-code">Exceptions</span><br />
<span class="inline-code">*Tier 1 Product Type</span>,
<span class='inline-code">*Tier 2 Product Type</span>,
<span class='inline-code'>*Tier 3 Product Type</span>,
<div class="grid grid-2" style="margin-top:12px"›
<span class="inline-code">*Trade Date</span>,
<b>Shared implementation rules</b><br />
29
<b>Mandatoryfields</b>cbr />
code*>*$ Volume</span><br />
cdiv class-"blue-note>
<div class="note">
Search
</select>
4/div>
</div>
4/divz

// OCR PHOTO 029: IMG_7153.HEIC
Waiting for a workbook. The board will try to match the selected asset to its preferred
id="kpiOutputs">0</div><div class="sub">Rows generated after mode logic.</div></div>
id="kpiValid">0</div><div class="sub">Rows with no current exceptions.</div></div>
zm
<div class=*kpi*><div class="label">Rows loaded</div><div class=*value*
<div class="kpi"><div class-"label*>Output rows</div»cdiv class="value"
<div class="kpi"><div class=*label'>Valid rows</div><div class=*value*
id="kpiRows">0</div><div class="sub">Rows parsed from the selected
<div id="statusBox" class="blue-note" style=*margin-top:12px">
<span class-'inline-code›*Treats Acronym</spanp,
‹span class='inline-codePrimary Amount</spanp,
ot 241
<span class=*inline-code">*Primary CCY</span>,
<span class="intine-code">*Legal Entity</spanp,
<span class-*inline-code">*$ VA/GNBV</span>,
<span class="intine-code">*Revenue CCY</span>
<span class="inline-code">*Trade ID</span>,
<div class="section-title">Run Summary</div>
cspen class="intine-code">*$ PC</span>,
29
<section class-card section
- +
source sheet automatically.
<div class="grid grid-7">
worksheet.</divpc/div>
Search
</section>
4/divz
c/divb
4/divs

// OCR PHOTO 030: IMG_7154.HEIC
id="kpiErrored">0</div>cdiv class="sub">Rows with one or more exceptions. </div></div>
<button class="tab active" data-tab="mappingStudio">Mapping Studio</button>
zm
<div class="kpi"><div class=*label'>Errored rows</div»<div class="value"
id="kpilssues">0</div><div class="sub">Fietd-level exc-ptions in the current
<div class="kpi*><div class="label*>Custom rules</div><div class=*value*
<button class="tab" data-tab="processedSheet">Processed Sheet</button>
¿button class='tab* data-tab="goldenRecord">Golden Record</button>
«div class="kpi*»cdiv class="label">Issues</div»<diy class=*vatue"
‹button class-"tab" data-tab="defaultRules>Default Rules</button>
«button class-"tab" data-tab-"exceptions*>Exceptions</button>
<button class="tab" data-tab="activityLog">Activity Log</button>
<button class-"tab" data-tab-*overview*>Overview</button>
0$ 241
<div id="mappingStudio* class="view active"></div»
<div id="processedSheet' class="view'>c/div>
<div id="exceptions" class="view"></div>
<divid='overview" class="view*›/divp
supported.</div>c/div>
<div class= tabbar">
force.</div>c/div>
run, </div> c/div>
Search
e/section)
</divp
</divp*

// OCR PHOTO 031: IMG_7155.HEIC
commentary_search,research_news,web_search.refinitiv_news.personal_email,bloombe
const SNAPSHOT_MODE = 1lwindow._SNAPSHOT_MODE || !Inavigator.webdriver |l
const STORAGE_KEY = "centralised blotter_mapping_cross_asset_v12";
0$ 241
/HeadlessChrome/i.test(navigator.userAgent || **);:
html/YE32UpkrboADOQLtMmaLohqjypizQ9ES*;
<div id="goldenRecord" class="view"></div>
<div id='defaultRules* class="view"›c/div>
const BASE_LOGIC_URL = /studio/shared-
<div id="activityLog" class="view"></div>
const Al_SELECTED_TOOLS -
const TARGET_HEADERS = [
**Salesperson (Coverage)*,
*-Tier 1 Product Type",
"*Tier 2 Product Type".
**Tier 3 Product Type*,
**Treats Acronym",
*Primary Amount',
**Trade Date",
**Primary CCY",
**Legal Entity",
Search
"Site Code",
"*$PC",
</main>
<script>

// OCR PHOTO 032: IMG_7156.HEIC
of241 O[D
32
- +
"Sales Team (Coverage)*,
"FX Platform Description",
"Sales Person Country".
"Sales Client Sector".
"Sales Client Country",
"Commission Code",
**Revenue CCY",
"Secondary CCY",
*Sales Client Type",
"*$ VA/GNBV",
"Maturity Date",
"BTB Trade Site",
"Sales Client",
**Trade ID",
"ISIN Code",
Search
"Platform",
"PC CODE",
"СCY РC",
"Security",
"Buy/Sell",
"Book",
"Trader",
"Issuer",
"Ticker",
*S TV",
"Price",

// OCR PHOTO 033: IMG_7157.HEIC
ot 241
33
const MANDATORY_FIELDS = new Set([
const NUMERIC_FIELDS = new Set([
- +
**Salesperson (Coverage)*,
"*Tier 1 Product Type",
**Tier 2 Product Type",
"*Tier 3 Product Type",
"Liquidity Reserve",
**Treats Acronym",
"Primary Amount",
**Primary CCY",
"*Revenue CCY"
"Flow Value",
**Trade Date",
"*Legal Entity",
"*$ VA/GNBV",
Search
"Risk Book",
"TFX Flag".
"Comment"
"*Trade ID",
"CVA",
"*$ PC",
"FVA",

// OCR PHOTO 034: IMG_7158.HEIC
const CCY_TARGETS = new Set(["*Primary CCY"**Revenue CCY","Secondary CCY","CCY
"Primary Amount",*$ PC']"*$ VA/GNBVy'Price""$ TV""'Flow Value",'Liquidity
of241 T 0/ID
const DATE_TARGETS = new Set(['"*Trade Date"."Maturity Date");
const UPPER_TARGETS = new Set(['ISIN Code","Issuer","Ticker*):
const SHARED_INTENTIONAL_BLANK_FIELDS = new Set([
salespersonCoverage: "mark lok leung*.
- + &
const SHARED_CONSTANTS = {
"Sales Team (Coverage)*:
"FX Platform Description",
"Sales Person Country",
"Sales Client Sector",
"Commission Code",
Reserve"."CVA',"PVA"
"Liquidity Reserve",
legalEntity: "HBAP",
siteCode: "HKH",
"Platform",
"PC CODE",
"Flow Value"
Search
*TFX Flag*
"$TV",
"CVA",
"FVA",
PC*):
1):

// OCR PHOTO 035: IMG_7159.HEIC
( key:"notionalUsdMio", patterns:["notionat usd mio","notionat usd mm","notional usd"]
preferredSheets: ['"Linear Zero Traded", "Linear Zero", "Structured FI", "Structured FI/
{ key:"issuerRaw", patterns:['issuer moody s s p fitch 1 ,"issuer moodys s p fitch
{ key:"comment", patterns:['notes","note"' comment*] ).
{ key:"currency", patterns:["currency"."primary ccy"] }.
{ key:"tradeDate", patterns:["date",'trade date'] },
{ key:"client", patterns:["client",'sales client"] },
{ key:"product", patterns: ["product","security"] },
( key:"settlement', patterns:["settlement"] },
{ key:"reoffer", patterns:[*reoffer',"price"] },
{ key:"isin", patterns:["isin","isin code"] },
label: "Structured FI / Linear Zero",
{ key:"tenor", patterns:["tenor*] },
{ key:"book", patterns:[*book']},
supportsAggregation: true,
const ASSET_CONFIGS = [
btbTradeSite: *LOH'
riskBook: *IRSMTN*
key: "structured_fi",
structured_fi: [
sourceSpecs: |
Search
Linear Zero'].
1'"issuer*] }.
pc: 0,

// OCR PHOTO 036: IMG_7160.HEIC
{ key:"gnbvUsd", patterns:["total gnbv usd","total gnbv (usd) "."gnbv usd","gnbv (usd)"] },
{ key:"notionalUsd", patterns:["notional amount usd","notional amount (usd)"," notional
( key: "tenorNum", patterns:["tenor number value","tenor number","tenor num"] },
zm
{ key:"action", patterns:["new addon","new add on","new/add on",new
( key:"action", patterns: ["new/unwind" new unwind""action""side"] ).
o241 •|CD
{ key:"tradeDate", patterns:["first trade date","trade date"] },
( key:"bookedFlag", patterns:["booked","booked flae"] ).
{ key:"maturity", patterns: ["maturity date","maturity"] ),
{ key:"maturity", patterns: ["maturity","maturity date"] ].
{ key:"underlying", patterns:['underlying""'ticker*1 ).
{ key:"nativeRef", patterns:["pims code","pims"] }.
( key:"product", patterns:['product", 'security*1 }.
{ key:"currency", patterns:["currency","ccy"] ],
preferredSheets: ['Collar Blotter", "Collar").
36
{ key:"isin", patterns:["otc isin","isin"] },
{ key:"trader", patterns:["trader"] }
{ key:"nnbv", patterns:["nnbv"] },
{ key:"book", patterns:["book"] }.
- +
unwind","new/unwind","action"] },
supportsAggregation: false,
usd""primary amount"] ),
key: "collar",
label: "Collar"
sourceSpecs: [
Search
collar: (

// OCR PHOTO 037: IMG_7161.HEIC
usd""volume usd","usd volume",'agreed usd amount",'usd amount field","primary amount"]
preferredSheets: ["Illiquid Credit+Repack", "Iltiquid Credit + Repack", "Illiquid Credit",
{ key:"volumeUsd", patterns:["volume mm usd",*volume ('mm) usd",' volume (mm)
zm
{ key:"issuerRaw", patterns ['issuer raw""'issuer (raw)""'issuer name*] ),
( key:"gnbvUsd", patterns:['gnbv usd*,'gnbv (usd)*,total gnbv usd"] }.
{ key:"booking", patterns: ["booking"."book","booking book"] },
{ key:"comment", patterns:["'note",'notes"!'comment"] }.
{ key:"maturity", patterns: ["maturity","maturity date"] }.
oF 241
( key:"nativeRef", patterns:["pims code","pims"] }.
{ key:"tradeDate", patterns:['"trade date", 'date*] },
( key:"issuer", patterns:['issuer code","issuer'1 }.
{ key:"dealName", patternsi|"deal name"f deal*1)
{ key:"currency", patterns:["ccy",'currency"] 1.
{ key:"reoffer", patterns:["reoffer', "price'] )
( key:"trader", patterns:["trader*] )
{ key:"status", patterns:["status"] }.
label: "Illiquid Credit + Repack",
{ key:"nnbv", patterns:["nnbv']},
key: *illiquid_credit_repack",
{ key:"isin", patterns: ["isin"] },
supportsAggregation: false,
illiquid_credit_repack: {
sourceSpecs: [
Search
*Repack"],

// OCR PHOTO 038: IMG_7162.HEIC
{ key:"referenceNumber", patterns:[*reference number"," reference no""reference"] },
{ key:"notionalUsd", patterns:["notional in usd',"notional usd","primary amount"] }.
{ key:"customerText", patterns:['final customer","'customer","customer text"] )
zm
( key:"action", patterns:["action"," new/unwind","new unwind","status"] },
{ key:"comment", patterns:["remarks"," remark","corfiment"," notes"]),
{ key:"gnbv", patterns:['gnbv field","gnbv", "gnbv usd"'gnbv (usd)"] }.
{ key:"action", patterns:['new/unwind","new unwind",'action'] },
{ key:"productType", patterns: ['product type","product"] ),
{ key:"ticker", patterns:["bbg tix 1"'bbg tix1", ticker']}.
[ key: "nativeRef", patterns:["svcs no.'!"svcs no","svcs"] }.
( key:"maturity", patterns: ["maturity","maturity date"] }.
f key:"summitld", patterns:["summit id",'summit"] ),
( key:"tradeDate", patterns: ['"trade date","date"] },
{ key:"grossPrice", patterns:['gross price",' price"] }.
{ key:"underlying", patterns:['underlying""ticker*] )
{ key:"currency", patterns:["currency","ccy"] },
preferredSheets: ["Equity TRS", *TRS"],
{ key:"trader", patterns: ["trader"] ).
{ key: "book", patterns:["book'] }.
supportsAggregation: false,
label: "Equity TRS",
key: "equity_trs",
sourceSpecs: [
Search
equity_trs: (

// OCR PHOTO 039: IMG_7163.HEIC
TOD
( key:"finalCustomer"', patterns:["final customer","'apistomer"])
0f241
{ key:"product", patterns:["product','security'] }.
39
{ key:"structure", patterns:["structure"]}.
activeTab:"mappingStudio",
manualOutputOverrides:0,
currentFileFingerprint: **
currentFileHistoryld:*)
fieldRulesByAsset:0.
const state = (
workbookinulll,
sheetHeaders:I],
logicalRecords:[].
sheetName:**
sourceRows:0,
mappedRows:[.
previewFilter:*,
rawFile:null,
exceptions:[.
headerMap:0.
fileName:**
fileHistory:U.
Search
logs:[.

// OCR PHOTO 040: IMG_7164.HEIC
zm
function gsa(sel){ return Array.from(document.querySelectorAll(sel)): }
function qs(id)( return document.getElementByld(id): }
ot 241
processedintineEdit:(rowld:"*, field:**, draft:** },
40
processedSelectedField:"*Trade Date",
aiFormulaField:"Maturity Date",
processedQuickSelectCount:5,
- +
tradeldMode:"business_key",
aiFormulaLastAutoPrompt:**
runMode:"trade_by_trade",
aiFormulaConversationld:**
aiFormulaRawResponse:"
aiFormulaSuggestion:nutl,
processedSelectedRowld:**
goldeninstancelndex:0).
assetKey:"structured_fi",
aiFormulaLoading:false,
selectedGoldenKey:**,
addonWindowDays:7,
exportExcludedRows:(/
aiFormulaPrompt:
aiFormulaEditable:"*,
processedSearch:**
goldenRecords:I].
aiFormulaError:**
Search

// OCR PHOTO 041: IMG_7165.HEIC
if(Istate.fieldRulesByAsset[state.assetKey]) state.fieldRutesByAsset[state.assetKey]=0:
function assetConfig)( return ASSET_CONFIGS[state.assetKey] II
( value:"aggregate _addons", label: "Aggregate add-ons" }
el.dataset.ready = status === "ready" ? "true" : "false";
ot 241
{ value:"trade_by_trade", label:"Trade by trade" },
: [[ value:"trade_by_trade", label:"Trade by trade" )):
return state.fieldRulesByAsset[state.assetKey]10:
return state.fieldRulesByAsset[state.assetKeyl;
window._BOARD_READY = status === "ready":
return assetConfig().supportsAggregation
function ensureCurrentAssetRuleStore(X
ASSET_CONFIGS.structured_fi; }
function evailableRunModes(K
const el = qs(*ready-marker*):
function currentFieldRules(X
function setReady(status)
el dataset status = status;
el.textContent - status;
if(lel) return:
Search

// OCR PHOTO 042: IMG_7166.HEIC
box.class Name = tone =—- "bad" ? "bad-note" : tone === "warn" ? "warn-note" : tone ---
state.logs.push({time:nowStr(), level, msg:String(msg) }):
if(state.activeTab -=- "activityLog") renderActivityLog0;
01 241
return d.toLocaleTimeString("en-GB", { hour12:false
function nowlso()f return new Date().tolSOString): }
if(state.logs.length > 2000) state.logs.shift();
String(d getMilliseconds()-padStart(3,0"):
function setStatus(html, tone=*blue"*
if(el) el.textContent = String(vatue):
"good" ? "good-note" : "blue-note";
function log(msg, level="info"X
function setTextSafe(id, value)[
- +
const box = qs("statusBox"):
const d - new Date):
box.innerHTML = html;
function nowStriX
const el = qs(id):
if(lbox) return;
Search

// OCR PHOTO 043: IMG_7167.HEIC
function trimFormulaText(v)( return String(v ?? **).trim(); }
of 241
replace(/Nu2018\u2019/g.***)
replace(/[\u201C\u201D]/g.**)
- +
function canonicalHeader(v)
.replace(/[^a-20-9]+/g,**)
function escapeHtm[(vX
replace(/8/g,"&amp;")
replace(/'/g."&quot;*)
replace(//g."&#39:");
-replace(\u00A0/g,* *)
return String(v ?2 **)
replace((</g,"&it;")
-replace(/>/g,"&gt;")
function cleanText(vH
.replace(/&/g," and *)
return String(v ?? **)
replace(As+/g.* *)
return cleanText(v)
.replace(\s+/g,"")
.toLowerCasel
Search
trim";

// OCR PHOTO 044: IMG_7168.HEIC
const t = cleanText(v).replace(/,/g.**).reptace(/\$/g.**). replace(/%/g.**).replace(//g.*');
zm
if(typeof v === "number" && Number.isFinite(v)) return v;
if(n > 0 &&n <= 1.5) return Number((n * 100).toFixed(4)):
0t 241
if(v -=- null || v --- undefined || v --- **) return null;
function isBlank(v) return cleanText(v) --- **= }
return (arr || U).find(v => lisBlank(v)) || ***;
return Number.isFinite(n) ? n : null;
function pickFirstNonBlank(arr)k
function normalizePricePoints(v)
if(n =-= nutl) return *PENDING*:
-+
return Number(n.toFixed(4)):
function prettyNumber(v, d=O)
const n = parseNumber(v):
function parseNumber(vy
const n = Number(t);
ff(lt) retur null;
Search
trim):

// OCR PHOTO 045: IMG_7169.HEIC
zm
if(v === null || v === undefined || v === ** || v === "NULL") return "NULL";
funotion clonePlain(v){ return JSON-parse(JSON.stringify(v ?2 null); }
return <span class="badge ${cts]">$(escapeHtml(text)}</span>*
if(typeof v === "string" && v === 'PENDING") return "PENDING";
return v === "Valid" ? badge("Valid","green") : badge("Error","red");
return n.toLocaleString(undefined, { minimumFractionDigits:d,
ot 241
if(c =-= "medium") return badge("Medium","amber"):
45
if(c -== *high") return badge("High",'green*):
return t.length > n ? t.slice(0, n - 1) + *.*: t;
maximumFractionDigits:Math.max(d,4) }):
if(c === "low") return badge("Low","red");
#f(Number.isNaN(n)) return String(v):
const c = canonicalHeader(v ||**):
- +
function confidenceBadge(v)
return badge(v || "n/a","blue");
function badge(text, cls=**)
function statusBadge(v)
function shorten(v, n=90)
const n = Number(v):
constt = cleanText(v):
Search

// OCR PHOTO 046: IMG_7170.HEIC
const num = String(Math.abs((h1 >>> 0) * 100000 + (h2 >>> 0) %
0t241
return num.stice(O, 12).padStart(12, *0*);
h = ((h cc 5) - h) + str.charCodeAt([):
h2 = Math.imul(h2, 2246822519);
return Math.abs(h).toString(36);
h1 = Math.imul(h1, 16777619);
for(let ( = O; i < str.length; i++X
function stableNumericid(str){
for(let i = 0; i < txt.length; i++)(
const ch = txt.charCodeAt(i);
letht = 2166136261>>> 0;
function simpleHash(strk
let h2 - 16777619 >>> 0;
const txt - String(str ||**);
100000)).replace(AD/g.*)*
h2 *= (ch + i);
Search
h1 x= ch;
let h = 0;
h|= 0;

// OCR PHOTO 047: IMG_7171.HEIC
return "$(prefix)_${Date.now)}_$[Math.random().toString(36).slice(2,8))*;
if(num >= 1024 * 1024) return *${(num / (1024 * 102j)).toFixed(2); MB* ;
return file ? [fite.name || **, fite.size || O, file.lastModified || O].join("Tº) : *'*;
hour:"2-digit",minute:"2-digit",second:"2-digit", hour12:false
if(num >= 1024) return ^$((num / 1024).toFixed(1)} KB*:
6f 241.
year:"numeric", month:"2-digit",day:"2-digit",
function compactObject(obj, maxKeys=60)
lif(Number.isNaN(d.getTime())) return iso;
function formatDateTimeDisplay(isoX
return d.toLocaleString("en-GB", {
const num = Number(n || 0);
function makeld(prefix="id"X
function fileFingerprint(fite)[
const d = new Date(iso):
function bytesLabel(n)
return "$(num}B*;
if(liso) return ***;
Search
7):

// OCR PHOTO 048: IMG_7172.HEIC
6241OD
manualOutputOverrides: state.manualOutputOverrides,
localStorage.setltem(STORAGE_KEY, JSON.stringify({
goldenRecords: state.goldenRecords.stice(0, 1500),
goldeninstancelndex: state.goldenInstancelndex,
selectedGoldenKey: state.selectedGoldenKey,
fieldRulesByAsset: state.fieldRulesByAsset,
if(lobj I| typeof obj!== "object") return out;
fileHistory: state.fiteHistory.slice(0, 200),
tradeldMode: state tradeldMode.
Object.keys(obj).forEach(k => (
- +
if(count >= maxKeys) return;
runMode: state.runMode,
assetKey: state.assetkey,
function saveLocal(i
const v = obj[k];
const out = 0:
if(lisBlank(v)X
let count = 0;
outlk] = v;
count += 1:
Search
retumn out;
tryl

// OCR PHOTO 049: IMG_7173.HEIC
if(payload.addonWindowDays !== undefined && payload.addonWindowDays !=- null)(
if(payload.assetKey && ASSET_CONFIGS[payload.assetKey]) state.assetKey=
state.fileHistory = Array.isArray(payload.fileHistory) ? payload.fileHistory: 0;
const payload = JSON.parse(localStorage.getltem(STORAGE_KEY) || ")*):
state.manualOutputOverrides - payload.manualOutputOverrides ||0:
if(payload.tradeldMode) state.tradeldMode = payload.tradeldMode:
processedQuickSelectCount: state.processedQuickSelectCount
state.goldenRecords = Array.isArray(payload.goldenRecords) ?
state.goldeninstancelndex = payload.goldeninstancelndex || 0):
state.selectedGoldenKey = payload.selectedGoldenKey || **=
"warn");
state.fieldRulesByAsset = payload.fieldRulesByAsset | 0:
if(payload.runMode) state.runMode = payload.runMode;
of 241
exportExcludedRows: state.exportExcludedRows,
log("Local persistence save failed: * + e.message,
addonWindowDays: state.addorWindowDays,
aiFormulaPrompt: state.aiFormulaPrompt,
if(payload && typeof payload === "object"X
49
aiFormulaField: state.aiFormulaField,
- +
payload.goldenRecords : [I:
function loadLocal0l
payload assetKey:
Search
Jcatch(e)(
7):
tryl

// OCR PHOTO 050: IMG_7174.HEIC
if(payload.aiFormulaPrompt) state.aiFormulaPrompt= payload.aiFormulaPrompt;
zm
lif(payload.aiFormulaField) state.aiFormutaField = payload.aiFormulaField;
state.addonWindowDays = Number(payload.addonWindowDays) ||7;
o241 • CD
state.exportExcludedRows = payload.exportExcludedRows |I0:
if(payload.processedQuickSelectCount !== undefined &&
return {y.v.getFullYear), miv.getMonth[) + 1, d:v.getDate()}:
log("Failed to restore locat state: " + e.message, "warn");
state.processedQuickSelectCount = Math.max(1,
return'$(pad2(parts.d)/${pad2(parts.m))/$(parts.y)*:
if(y === null || v -== undefined || v =-= **) return null;
if(v instanceof Date && |Number.isNaN(v-getTime()X
Number(payload.processedQuickSelectCount) || 5);
function pad2(n)( return String(n).padStart(2,'0"); }
payload.processedQuickSelectCount !== nutl)
function formatOutputDateFromParts(parts)
50
function parseDatePartsNoTz(vX
- +
if(-parts) return **:
Search
Jcatch(e)(

// OCR PHOTO 051: IMG_7175.HEIC
lif(m) return { y:Number(m[1]), m:Number(m[2]), d:Number(m[3]) }:
if(typeof v =—- "number" && window.XLSX && XLSX.SSFX
0t 241
if(parsed && parsed y && parsed.m && parsed.dX
const parsed = XLSX.SSF.parse_date_code(v);
return [y:parsed y, m:parsed.m, d:parsed.d }:
m = t.match(/~(\d{1,2])V(\d{1,2])V(\d{2,4)) $/);
return { d:Number(m[1]), m:Number(m[2]), y}:
m = t.match(/^(d(1,2)-(d[1,2})-(d(2,4))$/):
31
let m = t.match(/*(\d(4))-(\d{2})-(\d{2})$/):
- +
const t = cleanText(v);
let y = Number(m[3]):
lif(y < 100) y += 2000;
let y = Number(m[3]):
if(y < 100) y += 2000;
if(lt) return null;
Search
if(m)
if(m)

// OCR PHOTO 052: IMG_7176.HEIC
function partsToUtcDate(parts)( return new Date(Date.UTC(parts.y, parts.m - 1, parts.d)):
function utcDateToParts(dt)f return{ y:dt.getUTCFullYear(), m:dt.getUTCMonth) + 1.
W
m = t.match(/~(\d{1,2)|-/ljan|feb|mar|apr|may|jun|jul|aug|sep|oct|nov|dec)[-
zm
Gjan:1,feb:2,mar:3,apr:4,may:5,jun:6,jul:7,aug:8,sep:9,0ct:10,nov:11,dec:12};
return {y:native.getFullYear(),m:native.getMonth() + 1, d:native.getDate) }:
return (d:Number(m[1]). m:monthMap[m[2].toLowerCase(]. y }:
0+ 241
return { d:Number(m[1]). m:Number(m[2]), y }:
if(!Number.isNaN(native.getTime))X
dt.setUTCDate(dt.getUTCDate) + n):
function addDaysToParts(parts, n)(
const dt = partsToUtcDate(parts):
- +
const native - new Date(t):
let y = Number(m[3]):
const monthMap -
if(y < 100) y += 2000;
d:dt.getUTCDate0 x}
Search
1(d(2,47)$/71):
return nully
if(m)

// OCR PHOTO 053: IMG_7177.HEIC
53
Search

// OCR PHOTO 054: IMG_7178.HEIC
if(m) return ( parts:addMonthsToParts(tradeDateParts, Number(m[1])). derived:true,
function deriveMaturitySafe(explicitMaturityRaw, settlementRaw, tradeDateRaw,
zm
const settlement = resolveSettlementParts(settlementRaw, tradeDateParts):
return {value:formatOutputDateFromParts(direct), derived:false,
note:* Settlement parsed from "$/cleanText(settlementRaw)]"* };
3:241 0
const tradeDateParts = parseDatePartsNoTz(tradeDateRaw):
const direct - parseDatePartsNoTz(explicitMaturityRaw);
settlementDerived:false, note:*Direct source maturity" );
function parseTenorYears(tenorRaw, tenorNumRaw){
m = t.match(/trade date\s*\+\s*(\d+)\s*month/);
return ( parts:null, derived:false, note:** 7:
constin = parseNumber(tenorNumRaw);
constt - canonicalHeader(tenorRaw):
const m = t.match(/*(\d+)\s+year/):
-+
if(m) return Number(m[1):
tenorRaw, tenorNumRawX
if(n l-- null) return n;
Search
return null;
if(direct

// OCR PHOTO 055: IMG_7179.HEIC
zm
? formatOutputDateFromParts(parseDatePartsNoTz(explicitMaturityRaw))
const maturityParts = addYearsToParts(settlement.parts, tenorYears);
const maturityParts - addYears ToParts(tradeDateParts, tenorYears):
note: Maturity derived from settlement + tenor ($(tenorYears)y)*
note: *Maturity derived from trade date + tenor ($(tenorYears)y)*
const tenorYears = parseTenorYears(tenorRaw, tenorNumRaw);
at 241
value: formatOutputDateFromParts(maturityParts),
Ivalue: formatOutputDateFromParts(maturityParts),
value: parseDatePartsNoTz(explicitMaturityRaw)
58
if(settlement parts && tenorYears !== nullk
settlementDerived: settlement.derived,
if(tradeDateParts && tenorYears !-- nullK
: cleanText(explicitMaturityRaw),
settlementDerived: false,
derived: true,
derived: true,
Search
return (
return
return (

// OCR PHOTO 056: IMG_7180.HEIC
if(c.includes(* singapore *) I| c.includes(* sgh ") || c.includes(" sg")) return "SG";
if(c.includes(* hong kong*) || c.includes(* hkh *) I| c.includes(*hk *)) return "НК";
return parts ? formatOutputDateFromParts(parts) : cleanText(raw);
ot 241
56
settlementDerived:settlement.derived,
const parts = parseDate PartsNoTz(raw):
const c =** + canonicalHeader(text) + **;
const b - extractBookCode(bookRaw):
function routePbByBook(bookRaw)l
note:*Unable to derive maturity"
function formatDateOutput(raw)l
function extractBookCode(text)[
- +
treatsAcronym:*PENDING".
salesClient: "PENDING",
if(lc.trim(i) return "'™';
bookResolved: **
derived:false,
Search
return **
return!
if(!bX

// OCR PHOTO 057: IMG_7181.HEIC
6t241
salesClient:"HSBC PRIV BK (S) SA SGH*,
salesClient:*HSBC PRIV BK (S) SA HK*,
const c = canonicalHeader(actionRaw):
salesClientCountry: *PENDING",
function sharedBuySell(actionRaw)l
if(c.includes("unwind")) retum 1B*;
salesClientType: *PENDING*
treatsAcronym:*HRCHSGH",
treatsAcronym:"HROHHKH",
salesClientCountry:"SG",
salesClientCountry:"HK",
salesClientType:"BOF*
bookResolved: *SG,
bookResolved: "HK",
salesClientType:*FBL*
if(!c) return "PENDING";
if(b === "SG*X
Search
return i
return (

// OCR PHOTO 058: IMG_7182.HEIC
if(c.includes(*mm") || c.startsWith(*mm") || c.endsWith(* mm") l| c.includes(* mio") |l
if(c.includes("addon") || c.includes("add on") || c.includes ("tap")) return *S*;
OII
01 241
function scaleAmountByHeader(value, headerLabelX
lif(c.includes("notional usd mio*)) return n * 1000000;
const c - canonicalHeader(headerLabel):
c.includes("million")) return n * 1000000;
const t = cleanText(v).toUpperCase():
function mapknownissuerText(raw)(
if(c. includes("new")) return "S*;
const n = parseNumber(value);
function looksLikelssuerCode(vX
const c = canonicalHeader(raw);
return /^[A-ZO-9._-]+$/ test(t);
if(t.length > 18) return false;
if(n --- null) return null;
lit(/As/.test(t)) return false;
return *PENDING":
if(lt) return false;
if(Ic) return *:
Search
return na

// OCR PHOTO 059: IMG_7183.HEIC
if(c.includes("the hongkong and shanghai banking corporation limited")) return "HВAP";
if(c.includes("hongkong and shanghai banking corporation limited")) return "HBAP":
if(c.includes("hong kong and shanghai banking corporation")) return "HВАР";
if(looksLikelssuerCode(codeRaw)) return cleanText(codeRaw).toUpperCase():
if(looksLikelssuerCode(rawText)) return cleanText(rawText).toUpperCase():
const mapped - mapKnownlssuerText(rawText || codeRaw):
if(c.includes(*hsbc continental europe")) return *HBCE*;
const t = cleanText(v).replace(/s+/g.**).toUpperCase():
ot 241
function resolvelssuerValue(codeRaw, rawText)
if(!isBlank(v)) mexCol = Math.max(maxCol, i):
if(c.includes ("hsbc bank plc")) return *HCIB";
function normalize NativeRef(v)
- +
if(mapped) return mapped;
return t ? t.slice(0, 40) = **;
function trimMatrix(matrix)[
(row || []).forEach(v, i) => 1
matrix.forEach(row => [
return "PENDING";
let maxCol = -1;
Search
return *:

// OCR PHOTO 060: IMG_7184.HEIC
const matched = (spec.patterns || O).some(p => row.some(r => г ---
const row = (matrix[] Il I]).map(canonicalHeader) filter(Boolean):
01241
canonicalHeader(p) || r.includes(canonicalHeader(p))));
return matrix.map(row => (row || []) slice(0, cut)):
const specs - assetConfig().sourceSpecs || 0:
const maxScan = Math.min(matrix.length, 20);
if(score > best score) best = [ idx:i, score }:
function uniqueHleaders(rawHeadersX
function detectHeaderRow(matrix)
return rawHeaders.map((h, idx) =2(
- +
for(let i - 0; i < maxScan; i++X(
let best = { idx:0, score:-1 }:
specs.forEach(spec => [
const cut = maxCol + 1:
if(matched) score += 1:
return best.idx;
const seen = 0:
let score = 0;
Search
7):

// OCR PHOTO 061: IMG_7185.HEIC
const partial = map.find(x => x.canonical.includes(target)):
const exact = map.find(x => x canonical === target):
function findHeaderMatch(headers, patterns)(
if(iname) name = *Unnamed_Sfidx + 1}':
name = *$(name)_ $[seen[name]]:
const target - canonicalHeader(p):
const target = canonicalHleader(p):
if(seen[name]|== undefined)
- +
for(const p of patterns |OX
lif(exact) return exact.raw;
for(const p of patterns || DX
if(partial) return partial.ravv:
let name - cleanText(h):
seen[name] += 1:
seen[name] = 0;
return name;
Search
return **:
Jelsel
2):

// OCR PHOTO 062: IMG_7186.HEIC
2m
OID
map[spec.key] = findHeaderMatch(headers, spec.patterns || D):
const headers = uniqueHeaders(trimmed[headerldx] ||J):
return { headers, rows, headerindex: headerldx + 1 }:
headers.forEach((h, idx) => obj[h] = arr[idx] 2? **);
(assetConfig().sourceSpecs || ().forEach(spec => (
const headeridx = detectHeaderRow(trimmed):
for(let i = headeridx + 1; i < trimmed.length; i++X
const hasData - arr.some(v => lisBlank(v);
62
obi._logical_index = rows.length + 1:
function extractSheetRows(matrix)
const trimmed - trimMatrix(matrix):
function buildHeaderMap(headers)
obj- source_excel_row =i + 1;
const arr = trimmed[] I| 0;
if(!hasData) continue:
rows.push(obj):
const rows - D;
const obj - 0:
const map - 0:
Search
return map:
});

// OCR PHOTO 063: IMG_7187.HEIC
function sourceHeaderName(key)( return state.headerMap[key] || **; )
010
0241
tradeDate:getSourceField(rawRow, "tradeDate").
issuerRaw:getSourceField(rawRow, "issuerRaw*),
settlement:getSourceField(rawRow, "settlement*),
product:getSourceField(rawRow, "product"),
currency:getSourceField(rawRow, "currency"),
63
const header - sourceHeaderName(key):
sourceRow: rawRow._source_excel_row,
function buildCanonicalSource(rawRowX
client:getSourceField(rawRow, *client"),
tenor:getSourceField(rawRow, "tenor'),
function getSourceField (rawRow, key)
logicalindex: rawRow.__logical_index
return header ? rawRow[header] : ***;
if(state.assetKey === "structured_fi"X
isin").
isin:getSourceField(rawRow,
- +
assetKey: state.assetKey,
const base -
raw: rawRow,
Search
return (
base.

// OCR PHOTO 064: IMG_7188.HEIC
notionalUsdMio:getSourceField(rawRow, "notionalUsdMio"),
bookedFlag:getSourceField(rawRow, "bookedFlag"),
notionalUsd.getSourceField(rawRow, "notionatUsd"),
of 241
commentigetSourceField(rawRow, 'comment"),
tenorNum:getSourceField(rawRow, "tenorNum*),
nativeRef:getSourceField(rawRow, "nativeRef"),
tradeDate:getSourceField(rawRow, "tradeDate*),
underlying:getSourceField(rawRow, 'underlying").
maturity:getSourceField(rawRow, "maturity"),
currency:getSourceField(rawRow, "currency"),
maturity:getSourceField(rawRow, "maturity"),
gnbvUsd:getSourceField(rawRow, *gnbvUsd*),
reoffer")
action:getSourceField(rawRow, "action"),
book:getSourceField(rawRow, "book"),
nnbv:getSourceField(rawRow, "nnbv*).
trader:getSourceField(rawRow, "trader")
action:getSourceField(rawRow, "action'),
book: getSourceField(rawRow, "book*),
isin:getSourceField(rawRow, 'isin"),
reoffer:getSourceField (rawRow,
if(state.assetKey =-- *collar"y
- +
Search
return (
base.

// OCR PHOTO 065: IMG_7189.HEIC
productType:getSourceField(rawRow, "productType"),
volumeUsd:getSourceField(rawRow, "volumeUsd"),
0t 241
comment:getSourceField(rawRow, "comment"),
nativeRef:getSourceField(rawRow, "nativeRef").
tradeDate:getSourceField(rawRow, "tradeDate"*).
issuerRaw:getSourceField(rawRow, "issuerRaw"),
dealName:getSourceField(rawRow, "dealName"),
currency:getSourceField(rawRow, 'currency"),
comment:getSourceField(rawRow, "comment"),
product:getSourceField(rawRow, "product"),
if(state.assetKey --- "illiquid_credit_repack"X
booking:getSourceField(rawRow, "booking").
maturity:getSourceField(rawRow, "maturity"),
gnbvUsd:getSourceField(rawRow, "gnbvUsd*),
trader:getSourceField (rawRow, "trader")
status:getSourceField(rawRow, "status*),
reoffer:getSourceField (rawRow, "reoffer"*),
issuer.getSourceField(rawRow, *issuer").
nnbv:getSourceField(rawRow, "nnbv*),
ticker:getSourceField(rawRow, 'ticker"),
isin:getSourceField (rawRow, "isin"),
- +
Search
return [
...base,

// OCR PHOTO 066: IMG_7190.HEIC
zm
referenceNumber getSourceField (rawRow, "referenceNumber"),
customerText:getSourceField(rawRow, "customerText")
finalCustomer:getSourceField(rawRow, *finalCustomer*)
notionalUsd:getSourceField(rawRow, "notionalUsd"),
Ю+ 241
nativeRef:getSourceField (rawRow, "nativeRef"),
summitid:getSourceField(rawRow, "summitld"),
tradeDate:getSourceField(rawRow, "tradeDate*).
grossPrice:getSourceField(rawRow, "grossPrice").
underlying:getSourceField(rawRow, "undertying").
currency:getSourceField (rawRow, "currency*).
maturity:getSourceField(rawRow, "maturity").
structure:getSourceField(rawRow, "structure*),
product:getSourceField(rawRow, "product"),
66
trader getSourceField(rawRow, "trader").
action: getSourceField(rawRow, "action"),
action:getSourceField (rawRow, "action"),
book:getSourceField (rawRow, "book"),
gnbv:getSourceField(rawRow, *gnbv"),
Search
return ?
hbase.

// OCR PHOTO 067: IMG_7191.HEIC
zm
OID
return Math.round((count /state.sourceRows.length) * 1000) / 10;
return Math.round((count /state.mappedRows.length) * 1000) /10;
return vI== "* && v!== null && y != undefined &&v !== "NULL":
0t241
return key-startsWith("raw:*) ? key-slice(4) : key;
const count - state.mappedRows.filter(r=> [
67
function sourceFieldFillRate(header
if(Istate.sourceRows.length) return 0;
retumn state.sheetHeaders.map(h => (
sub: "Fill $(sourceFieldFilRate(h)}%*
if(|state.mappedRows.length) return 0;
const v = routput && routput[field];
function rawHeaderLabelForKey(key)
function getBlotterFieldNodes(X
- +
function fillRate(field)[
key:raw:$(hy
Search
label:h,
7) length:
7)):

// OCR PHOTO 068: IMG_7192.HEIC
if(state.assetKey!== "structured_fi" || state.runMode !== "aggregate_addons")(
zm
return c.includes("addon") I| c.includes("add on") I| c.includes("tap");
return diffDays >= 0 && diffDays <= Number(days 1| 0);
61241
function withinWindow(baseRow, otherRow, days)[
const p = parseDatePartsNoTz(src.tradeDate):
retur p ? partsToUtcDate(p)-getTime() : 0;
const diffDays - (b - a) / (24 * 3600 * 1000):
const a - getTradeTimeMs(baseRow):
const b - getTradeTimeMs(otherRow):
const c = canonicalHeader(src.action):
const c = canonicalHeader(src.action):
return state.sourceRows.map(sro »> ((l
function isStructuredAddonRow(src)
function isStructuredNewRow(src)
function getTradeTimeMs(src)
function buildLogicalRecords(X
return c.includes(*new*):
if(la l| lb) return false:
Search

// OCR PHOTO 069: IMG_7193.HEIC
31 241
cleanText(src.currency).toUpperCase),
cleanText(src.settlement).toUpperCase0
state.sourceRows.forEach(src => (
cleanText(src.isin).toUpperCase(),
cleanText(src.book).toUpperCase(),
if(lgroups.has(key)) groups.set(key. I):
const ta = getTradeTimeMs(a):
const tb = getTradeTimeMs(b);
- +
const groups = new Map():
groups.get(key).push(src):
groups for Each(rows => (
if(ta !== tb) returnta - tb;
rows.sort((a,b) =>(
aggregated:false
const records = 0;
type:"single",
baseRowisrc,
addonRows:I1,
rows:[src].
const key = [
Search
T.join(*.*):
7)):
7):

// OCR PHOTO 070: IMG_7194.HEIC
if(isStructuredAddonRow(src) && cluster && withinWindow(cluster.baseRow, src,
zm
type:cluster.addonRows.tength ? "cluster" : "single",
cluster = (baseRow:src, rows:[src], addonRows:[] };
6F241
aggregated:cluster.addonRows.length > 0
return a logicalindex - b.logicalindex:
addonRows:[...cluster.addonRows],
cluster.addonRows.push(src);
base Row:cluster.baseRow,
if(isStructuredNewRow(src))
- +
function flushCluster)l
rows:[..cluster.rows].
state.addonWindowDays)X
cluster.rows.push(src):
rows.forEach(src => [
if(lcluster) return;
let cluster = null;
records.push(
flushCluster);
cluster - null;
Search
return:
7):

// OCR PHOTO 071: IMG_7195.HEIC
return a.baseRow.logicalindex-b.baseRow.logicallndex;
61 241
const ap = getTradeTimeMs(a.baseRow):
const bp - getTradeTimeMs(b.baseRow):
function getResolvedBook(src)
- +
if(bp !== ap) return bp - ap;
records.sort((a,b) => (
aggregated:false
flushCluster():
records.push(f
type:"single",
baseRow:src,
addonRows:[.
flushCluster);
return records;
rows:[src],
Search
return;
7):
)):

// OCR PHOTO 072: IMG_7196.HEIC
const n = scaleAmountByHeader(src.notionalUsdMio, header || "notional usd mio"):
constn = scaleAmountByHeader(src.notionalUsd, header |I *notionat amount usd"):
zm
const fromCustomer = extractBookCode([src.customerText, src.dealName,
const header - sourceHeaderName("notionalUsdMio*):
const fromBooking - extractBookCode(src.booking):
01241
const header = sourceHeaderName("notionalUsd*):
const header = sourceHeaderName("volumeUsd*):
const fromStatus = extractBookCode(src.status);
if(state.assetKey === "itliquid_credit_repack"X
if(state.assetKey === "Iliquid credit repack'X
if(fromCustomer) return fromCustomer;
function getPrimaryAmountForRow(src)
if(fromBooking) return fromBooking:
if(state.assetKey === "structured_fi"X
if(fromStatus) return fromStatus;
return extractBookCode(src.book):
return n === null? *PENDING* : n;
return n ==- null ? "PENDING : n:
if(state.assetKey === "collar"X
— +
src.comment]_join(**));
Search
return **;

// OCR PHOTO 073: IMG_7197.HEIC
const n = scaleAmountByHeader(src.notionalUsd, header ]| "notional in usd"):*
2m
constn = scaleAmountByHeader(src.volumeUsd, header || "volume usd"):
const pa - typeof primaryAmount === *number" ? primaryAmount :
lif(nnbv!== null && pa !== null) return Number((nnbv * pa) toFixed(4)):
01 241
const header - sourceHeaderName ("notionalUsd"):
if(state.assetKey === "illiquid_credit repack"X
function getVaForRow(src, primaryAmount)(
const n = parseNumber(src.gnbvUsd):
if(state.assetKey ==- "structured_fi"X
const g - parseNumber(src.gnbvUsd);
const nnbv = parse Number(src.nnbv):
return n ==- null? "PENDING* i n;
constn - parseNumber(src.nnbv):
return n =—- null ? "PENDING" : n;
return n =-= null ? "PENDING" : n;
return n === nutt ? "PENDING" : n:
if(state.assetKey === "collar"y
- +
parseNumber(primaryAmount):
if(g l== null) return g:
return "PENDING";
Search

// OCR PHOTO 074: IMG_7198.HEIC
const primaryCcy = cleanText(src.currency).toUpperCase() || "PENDING";
maturityDate = formatDateOutput(src.maturity) || "PENDING":
0t 241
const tradeDate - formatDateOutput(src.tradeDate):
let primaryAmount = getPrimaryAmountForRow(src);
let vaGnbv = getVaForRow(src, primaryAmount):
const resolvedBook = getResolvedBook(src):
const pb - routePbByBook(resolvedBook):
const buySell - sharedBuySell(src.action):
src.tenor, src.tenorNum).value || 'PENDING":
if(state.assetKey ==- *structured_fi"X
if(state.assetKey === *structured_fi°X
const n = parseNumber(src.gnbv);
return n === null? "PENDING" -n:
function buildHelpers(record)
const src - record.baseRow;
let price = "PENDING";
let issuer = *PENDING":
let maturityDate - **;
Search
Jelsel

// OCR PHOTO 075: IMG_7199.HEIC
const addonVa = record.addonRows.reduce ((sum, r) => sum + (parseNumber(r.nnbv)
price - sortedAddons.length ? normalizePricePoints(sortedAddons[0].reoffer) :
const addonAmount = record.addonRows reduce((sum, r) =3 [
const maybelssuer = mapKnownlssuerText(src.issuerRaw);
sourceHeaderName("notionatUsdMio") I| "notional usd mio");
const ta - getTradeTimeMs(a), tb = getTradeTimeMs(b);
const n = scaleAmountByHeader(r.notionalUsdMio,
ot 241
if(record aggregated && record.addonRows.lengthXl
primaryAmount = addonAmount || "PENDING*:
price = normalizePrice Points(src.reoffer):
return a.logicalindex - b.logicalindex;
vaGnbv - addonVa || "PENDING";
if(state.assetKey === *collar'X
- +
if(ta !-- tb) return ta - tb;
issuer = maybelssuer 1|*
return sum + (n | 0);
issuer = *:
Search
PENDING":
3, 0);
Jelsel
н о), о);

// OCR PHOTO 076: IMG_7200.HEIC
)I [D
price = gp === null ? "PENDING" : Number(gp.toFixed(4));
issuer = resolvelssuerValue(src.issuer, src.issuerRaw);
resolvedBook: pb.bookResolved || resolvedBook || **
6t 241
if(state.assetKey === "illiquid_credit_repack"X
16
price = normalizePricePoints(src.reoffer):
const gp = parseNumber(src.grossPrice):
if(state.assetKey ==- "equity_trs"X
assetLabel: assetConfig()label,
- +
secondaryCcy: primaryCcy,
revenueCcy: primaryCoy,
price = "PENDING";
issuer = "PENDING":
issuer = "PENDING";
ccyPc: primaryCoy,
primaryAmount,
maturityDate,
tradeDate.
primaryCcy,
Search
return (
pb.

// OCR PHOTO 077: IMG_7201.HEIC
anchorType: record.aggregated ? "Anchor + add-ons" = "Single row"
if(state.tradeldMode ==- "legacy_row_order") return src.logicalindex;
aggregatedAddonCount: record.addonRows.length ||0,
0t 241
cleanText(src.currency).toUpperCase),
function buildTradeld(record, helpers)(
lit(state.assetKey =-- "structured f*X
cleanText(src.book).toUpperCase(),
String(helpers.primaryAmount 72 *)
cleanText(src.isin).toUpperCase(),
cleanText(helpers.maturityDate),
aggregated: record.aggregated,
- +
const src = record.baseRow;
cleanText(helpers.buySell),
cleanText(sro tradeDate),
let businessParts = 0:
"STRUCTURED_FI",
businessParts = [
Search
vaGnbv,
price,
issuer,
buySell,

// OCR PHOTO 078: IMG_7202.HEIC
ot 241
Jetse if(state.assetkey == *illiquid_credit_repack*X
cleanText(helpers.resolvedBook) toUpperCase().
cleanText(src.underlying).toUpperCase(),
cleanText(src.currency).toUpperCase).
cleanText(src.currency).toUpperCase(,
cleanText(src.dealName).toUpperCase0
cleanText(src.book).toUpperCase(),
String(helpers.primaryAmount ?? **)
String(helpers.primaryAmount ?? ***),
Jelse if(state.assetKey === 'collar*X
cleanText(src.isin).toUpperCase(),
cleanText(src.isin).toUpperCase().
"ILLIQUID_CREDIT_REPACK*,
- +
cleanText(helpers.buySell),
cleanText(helpers.buySell),
cleanText(src.tradeDate),
cleanText(src.tradeDate),
cleanText(src.maturity),
cleanText(src.maturity),
businessParts = [
businessParts - [
businessParts = 1
"COLLAR",
Search
Jelsel

// OCR PHOTO 079: IMG_7203.HEIC
if(state.tradeidMode === "business_key*) return businessKeyld;
6t 221
cleanText(src.referenceNumber).toUpperCase(),
Jelse if(state.assetKey === *itiquid credit repack**
70
cleanText(src.underlying).toUpperCase(),
nativeRefs = [src.nativeRet, src.summitld]:
cleanText(src.currency).toUpperCase().
cleanText(src.book).toUpperCase(),
String(helpers.primaryAmount ??**)
if(state.assetKey === "structured_fi"X
nativeRefs = [src.referenceNumber]:
Jelse if(state.assetKey === 'collar"X
- +
cleanText(helpers.buySell),
nativeRefs = [src.nativeRef];
nativeRefs = [sro.nativeRef]:
cleanText(src.tradeDate),
cleanText(src.maturity),
"EQUITY_TRS",
let nativeRefs - U]:
Search
Jelsel

// OCR PHOTO 080: IMG_7204.HEIC
if(field === "Book") return { type:"blank", sources:[], summary:"Output Book stays blank
if(field === "*$PC*) return ( type:"constant", sources:[), summary:"Same current-board
if(field === "Site Code") return ( type:"constant", sources:[], summary:"Same current-
if(field === "Risk Book") return { type:"constant", sources:[], summary:"Same current-
if(field === "*Treats Acronym") return ( type:"derived", sources:filter([label("book"),
resolved input Book: SG → HRCHSGH, otherwise HK if book resolves, else PENDING." );
if(field === 'BTB Trade Site") return { type:"constant", sources:[], summary:"Same
label("booking*), label("status"), label(*customerText')]), summary:"PB routing from
zm
const blankMeta - { type: "blank", sources:[], summary:"Blank by design." ];
if(field —=- "*Salesperson (Coverage)") return { type:"constant", sources:0.
summary: "Same current-board logic: fixed salesperson coverage constant." ):
if(SHARED_INTENTIONAL_BLANK_FIELDS.has(fietd)) return blankMeta;
const native = nativeRefs.map(normalizeNativeRef).find(Boolean):
const filter = arr => Array.from(new Set((arr || (]).filter(Boolean)));:
6t 241
const label - (k, fallback=**) => h[k] || fallback;
30
current-board logic: fixed BTB Trade Site.* }:
function defaultMappingMeta(field)(
return native | businessKeyld:
- +
consth - state.headerMap;
board logic: fixed Legal Entity.' };
board logic: fixed Site Code." }:
board logic: fixed Risk Book."}:
across all assets.' );
logic: fixed *$ PC." }:
Search

// OCR PHOTO 081: IMG_7205.HEIC
lif(field == = "Sales Client Country") return { type:"derived", sources:filter([label("book*),
if(field —-- "*Trade ID*) return { type:"derived", sources:[], summary:"Driven by selected
if(field ==- "Sáles Client Type") return { type:"derived", sources:filter ([label("book"),
Trade ID mode: deterministic business-key, native-ref preferred, or legacy row-order." };
label("booking"), label("status"), label("customerText")]), summary:*PB routing from
label("booking"), label("status"), label("customerText")]), summary:"PB routing from
label("booking"), label("status"), tabel("customerText")]), summary:"PB routing from
label("status")]), summary:*Shared rule: New / Addon / Add On / Tap - S, Unwind - B,
case "*Tier 1 Product Type": return { type:"constant", sources:[], summary:"Keep
case **Tier 2 Product Type*: return { type:"constant", sources:[], summary:"Keep
case "*Tier 3 Product Type": return { type:"constant", sources:[], summary:"Keep
if(field -== "Sales Client") return { type:"derived", sources :filter([label("book").
case "*Trade Date": return ( type:"source", sources:filter([label("tradeDate")]),
if(field --= "Buy/Sell") return { type:"derived", sources:filter([label("action"),
case "CCY PC": return (type:"source", sources:filter([label("currency")]).
0+ 241
summary:* Copy $(label("currency"."Currency")}. };
if(state.assetKey === "structured_fi*X
- +
current Structured Fl logic.* }:
current Structured Fl logic." 7:
current Structured FI logic." ];
case"*Primary COY":
case "*Revenue CCYM
case "Secondary CCY":
resolved input Book." );
resolved input Book"}:
resolved input Book.' }:
otherwise PENDING.'}:
switch(field)
Search

// OCR PHOTO 082: IMG_7206.HEIC
: ( type:"derived", sources:filter([label("notionalUsdMio")]), summary: * Structured FI
tabel("tenor"), label("tradeDate")]), summary:* Use explicit maturity, else derive settlement
: { type:"derived", sources:filter([label("nnbv'")J), summary: Structured FI trade-by-
label("tenor"), label("tradeDate")]), summary: Use explicit maturity or derive settlement +
label("action")]), summary:*Keep current Structured Fl branch logic: issuer only resolves on
? { type:"derived", sources:filter([labet("nnbv"), label("action")]), summary: " Keep
type: "derived", sources:filter([label("maturity"), label("settlement"), labet("tenorNum"),
type:"derived", sources:filter([label("maturity"), label ("settlement"), label("tenorNum*),
case "Price": return { type:"derived", sources:filter([label("reoffer*), label("action*)]).
case "Ticker": return ( type:"blank", sources:[], summary:"Keep current Structured Fl
+ tenor, else trade date + tenor, output dd/mm/yyyy." j;case "Maturity Date": return (
case "Security": return ( type:"blank", sources:[label("product")]. filter(Boolean),
? ( type:"derived", sources:filter([label("notionalUsdMio*), label('action*)]),
summary: Keep current Structured Fl add-on logic and write amount into Primary
summary:"Structured Fi price uses current-board price-point normalization." }:
case "Trader": return { type:"blank", sources:[label("trader")].filter(Boolean),
zm
case *Issuer: return { type:"derived", sources :filter([label("issuerRaw*),
current Structured Fl add-on aggregation logic for VA / GNBV.* }
summary:"Keep current Structured Fl logic: Trader left blank." }:
31 241
return state.runMode --- "aggregate_addons"
return state.runMode =-- "aggregate_addons*
32
summary:"Keep current Structured Fl logio.* }:
aggregated path where applicable."}:
case "Primary Amount":
tenor, output dd/mm/yyyy.*}:
case "*$ VA/GNBV":
Search
Amount.* }
USD.* }:
logic." };

// OCR PHOTO 083: IMG_7207.HEIC
case *Comment": return ( type:"blank", sources:[label(*comment")].filter(Boolean),
case "ISIN Code": return { type:"source", sources:filter([label("isin")]), summary:* Map
default: return ( type:"blank", sources:[], summary:*Blank by current Structured Fl
summary: " Map from $(label("tradeDate", "First Trade Date")} and output dd/mm/yyyy.*}:
case "*Tier 3 Product Type": return { type:"constant", sources:[), summary:"Still
case "*Trade Date": return { type:"source", sources:filter([label("tradeDate").
case "Maturity Date": return ( type:"source", sources:filter([label("maturity")]),
summary:" Map from $(label("maturity", "Maturity*)} and output dd/mm/yyyy where
summary:" Primary Amount = $(label("notionalUsd","Notional Amount (USD)")}.* };
case "*$ VA/GNBV": return( type:"source", sources filter([label("gnbvUsd*)]),
zm
case 'CCY PC*: return { type:"source", sources:filter([label("currency")]),
summary:" VA / GNBV = $(label("gnbvUsd", Total GNBV (USD)")}. }:
summary:" Map from $(label("currency","Currency")}.* ):
оt 241
32
summary:"Keep current Structured Fl logic." }:
Mif(state.assetKey ==- 'collar*K
from $(label("isin",*OTC ISIN")}.* }:
case **Tier 1 Product Type":
case "*Tier 2 Product Type*:
case "*Primary CCY":
case "*Revenue CCY*:
case "Secondary CCY":
PENDING for Collar'" }:
switch(field)
Search
parseable.* }:
logic. }

// OCR PHOTO 084: IMG_7208.HEIC
case "ISIN Code": return { type:"source", sources:filter([label("isin")]), summary:* Map
case "Primary Amount": return ( type:"source", sources:filter([label("volumeUsd")]).
label("productType")]), summary:"Security = Deal Name preferred, fallback Product Type."
case "Ticker": return { type:"source", sources:filter([label("ticker")]), summary:*Ticker
case "Price": return { type:"derived", sources:filter ([label("reoffer"))), summary:"Price =
case *Trader": return { type:"source", sources:filter([label("trader")]), summary:"Trader
default: return { type:"blank", sources:[]. summary:"Blank by design or shared current-
label("nnbv"), label("volumeUsd")]), summary:*Preferred GNBV (USD); fallback NNBV *
case "Maturity Date": return ( type:"source", sources:filter([label("maturity")]).
summary:" Map from $(label("maturity","Maturity")} and output dd/mm/yyyy where
case **$ VA/GNBV": return { type:"derived", sources:filter ([label("gnbvUsd"),
case "Comment": return { type:"source", sources:filter([label("comment")]),
zm
case "CCY PC": return (type:"source", sources:filter([label("currency")]),
case "Security": return { type:"derived", sources:filter([label("dealName"),
case "Issuer*: return {type:"derived", sources:filter([label("issuer"),
summary:" Map from $[label("currency","Ccy / Currency")}.* };
= ${label("ticker","BBG Tix 1*)} if available, else PENDING.* };
summary:"Comment = $[label("comment","Remarks")}-* ]:
6X 241
Primary Amount if GNBV is blank." };
Reoffer normalized to price points." };
from S(label("isin", "ISIN").* 3:
mapping, else PENDING." }:
if present, else blank.* }:
Search
parseable." );
board logic." };

// OCR PHOTO 085: IMG_7209.HEIC
case "ISIN Code": return { type:"source", sources:filter([label("isin")J), summary:* Maр
case "Primary Amount": return { type:"source", sources:filter([label("volumeUsd")]).
label("productType")]), summary: "Security - Deal Name preferred, fallback Product Type."
case "Ticker": return { type:"source", sources:filter([label("ticker")]), summary:* Ticker
case "Price": return { type:"derived", sources:filter([label("reoffer*)]), summary:"Price =
case "Trader": return { type:"source", sources:filter([labe!("trader")]), summary:*Trader
default: return { type:"blank", sources:[], summary:"Blank by design or shared current-
label("nnbv"), label("volumeUsd")]), summary:*Preferred GNBV (USD); fallback NNBV x
Label("'issuerRaw")]), summary:*Use issuer code if available, else conservative raw-text
case "Maturity Date": return { type:"source", sources:filter ([label("maturity")]).
summary: Map from $flabel("maturity","Maturity")} and output dd/mm/yyyy where
case "*$ VA/GNBV": return { type:"derived", sources:filter([label("gnbvUsd"),
case "Comment": return [type:"source", sources:filter([label("comment")]).
zm
case "CCY PC*: return { type: "source", sources:filter([label("currency")]),
case "Security": return ( type:"derived", sources:filter([tabel("dealName"),
case "Issuer": return { type:"derived", sources:filter([label("issuer*),
summary: * Map from $(label("currency","Ccy / Currency")}.* *;
= $(label("ticker","BBG Tix 1")) if available, else PENDING.* ]:
0 241 %
summary: "Comment = $[label("comment", "Remarks*))-
DAL
85
Primary Amount if GNBV is blank."};
Reoffer normalized to price points.* :
from $/label("isin""ISIN"y].* *
mapping, else PENDING.* }:
if present, else blank." );
Search
parseable.* }:
board logic."};

// OCR PHOTO 086: IMG_7210.HEIC
*, sources:filter([label("notionalUsd")]).
case 'Issuer": return { type:"constant", sources:[], summary:"Still PENDING for Equity
case "*Tier 3 Product Type": return ( type:"constant', sources:[). summary:"Still
summary:" Map from $(label("tradeDate",'Trade Date")} and output dd/mm/yyyy.* ];
", sources:filter([label ("tradeDate")]).
case "Maturity Date": return { type: "source", sources:filter([label("maturity")]).
summary:" Map from ${label("maturity"."Maturity")} and output dd/mm/yyyy where
labe(("product")]), summary:"Security = Structure preferred, fallback Product." };
zm
case 'CCY PC": return { type: "source", sources:filter([label("currency")]),
summary:" Primary Amount = $(label("notionalUsd","Notionat in USD")).* ];
case "*$ VA/GNBV": return ( type:"source", sources:filter([label("gnbv")]),
case "Security": return ( type:"derived", sources:filter([Label(*structure*),
case 'Ticker": return ( type:"source', sources:filter([label("underlying")).
case "Price": return ( type:"source", sources:filter([label("grossPrice")]).
summary:"Ticker = $(label("underlying"."Underlying").* H:
summary:" Map from $flabel("currency","Currency")}. }:
6t 241
summary: " Use $(label("gnbv","GNBV fietd")} directly.* }:
summary:* Price = $(label('"grossPrice""Gross Price*).* }:
case "Primary Amount": return { type:"source",
36
case "*Trade Date": return { type:"source",
case "*Tier 1 Product Type*:
case **Tier 2 Product Type":
PENDING for Equity TRS." }:
case "*Primary CCY":
case "*Revenue CCY*:
case "Secondary CCY":
switch(field)(
Search
Equity TRS.' }:
parseable.* :
TRS." }:

// OCR PHOTO 087: IMG_7211.HEIC
default: return ({ type: "blank", sources:[], summary:'Blank by design or shared current-
case "Trader": return ({ type:"blank", sources:[], summary:"Blank untess source later
", constant:"*, expr:"* );
if(mode === "source") return " <span class='ms-tag source">custom source</span>*;
case "Comment": retumn ( type:"blank", sources:[], summary:"Blank by design." };
if(mode === "constant") return* <span class=*ms-tag const'>constant</span>*;
if(mode === "formuta") return * <span class="ms-tag rute">formula</span>* ;
zm
if(rule.mode === "formula*) return * Formula: ${shorten(rule.expr || **, 80)} :
return currentFieldRutes([field] || { mode:"default", sourceKey:**,
if(rute.mode === "constant") return * Constant: $(rute.constant) ;
of 241
if(rute.mode === "source") return* Custom source:
const mode = currentRule(field).mode || "default";
if(rule.mode ==- "blank*) return "Forced blank'
return defaultMappingMeta(field).summary;
27
S(rawHeaderLabelForKey(rule.sourceKey)7:
function currentRuleSummary(field)
const rule = currentRule(field);
function currentRuleTag(field)(
function currentRule(field)
Search
board logic."};
added." };

// OCR PHOTO 088: IMG_7212.HEIC
if(meta.type === "constant") return" <span class=*ms-tag const">constant</span>* ;
if(fietd === "Sales Client Country") return helpers.pb.salesClientCountry || "PENDING";
if(meta.type ==- "source") return * «span class="ms-tag source">source</span>*;
if(meta.type =-- "blank") return <span class="ms-tag blank">blank</span>*;
if(field =-- "*Treats Acronym") return helpers.pb.treatsAcronym || "PENDING";
if(field === "Sales Client Type") return helpers.pb.salesClientType || *PENDING":
if(mode === "blank") retum* <span class="ms-tag blank">blank</span>*;
if(field === "Sales Client") return helpers.pb.salesClient || 'PENDING";
if(field === "*Legal Entity") return SHARED_CONSTANTS.legalEntity;
if(field —-- "Site Code") return SHARED CONSTANTS.siteCode;
if(field === "Primary Amount") return helpers.primaryAmount;
if(field --- **Trade ID") return buildTradeld(record, helpers);
if(field ==- "Secondary CCY") return helpers.secondaryCcy;
if(field ——- **Revenue CCY") return helpers.revenueCcy;
D$ 241
return <span class="ms-tag rute">derived</span>;
if(field =-- "*$PC") return SHARED_CONSTANTS.pc;
function sharedField Value(field, record, helpers)(
if(field --- **$ VA/GNBV") return helpers.vaGnbv;
if(field --- "*Salesperson (Coverage)*) return
if(field === *Buy/Sell") return helpers.buySelt;
SHARED_CONSTANTS.salespersonCoverage:
if(field === "CCY PC*) return helpers.ccyPc;
const meta = defaultMappingMeta(field);
if(field === "Book*) return ***
Search

// OCR PHOTO 089: IMG_7213.HEIC
case "ISIN Code": return cleanText(src.isin).toUpperCase() I| "PENDING";
if(field === "BTB Trade Site" return SHARED_CONSTANTS.btbTradeSite;
case "*Tier 2 Product Type": return *Interest Rate Linked Note -PPN";
case "*Tier 3 Product Type*: return *Interest Rate Linked Note -PPN*;
if(field -=- "Risk Book*) return SHARED_CONSTANTS.riskBook;
case **Primary CCY": return helpers.primaryCcy || "PENDING";
case "Maturity Date": return helpers maturityDate || 'PENDING":
if(SHARED_INTENTIONAL_BLANK_FIELDS.has(fietd)) return **;
case "*Trade Date": return helpers tradeDate || "PENDING";
function assetSpecificField Value(field, record, helpers){
case **Tier 1 Product Type": return "Structured Rates";
•
ot 241
if(state.assetKey === "structured_fi*X
case "Issuer": return helpers.issuer;
case "Price*: return helpers,price:
const src - record .baseRow:
case "Comment": return **;
case "Security": retumn***
case "Trader": return *™;
case Ticker": return **;
default: return ***
return undefined:
switch(field)
Search

// OCR PHOTO 090: IMG_7214.HEIC
case "Maturity Date": return formatDateOutput(src.maturity) || 'PENDING":
case *Ticker": return cleanText(src.undertying).toUpperCase() I| "PENDING";
case "ISIN Code": return cleanText(src.isin).toUpperCase() I| "PENDING";
case "*Primary CCY": return helpers.primaryCcy || "PENDING";
case "*Trade Date": return helpers.tradeDate || "PENDING";
case "Security": return cleanText(src product) || *PENDING";
DAL
0024T
case "Comment": return cleanText(sra.comment);
case "*Tier 3 Product Type": return "PENDING";
case "*Tier 3 Product Type": return *PENDING*:
if(state.assetKey === filliquid_credit_ repack")
case "Trader": return cleanText(src.trader):
case "Issuer": return "PENDING";
case "Price": return *PENDING";
if(state.assetKey === "collar"X
case "*Tier 1 Product Type":
case "*Tier 2 Product Type*:
case "*Tier 1 Product Type':
case "*Tier 2 Product Type":
default: return **:
switch(fieldX
switch(field)[
Search

// OCR PHOTO 091: IMG_7215.HEIC
case "Maturity Date": return formatDateOutput(src.maturity) || "PENDING";
zm
case "ISIN Code": return CleanText(src.isin).toUpperCase() I| "PENDING";
case "Maturity Date": return formatDateOutput(src.maturity) || 'PENDING";
case "Ticker": return cleanText(src.ticker).toUpperCase() I| 'PENDING";
case "Security": return pickFirstNonBlank([cleanText(src.dealName),
case "Security": return pickFirstNonBlank([cleanText(src.structure),
case "*Primary CCY': return helpers.primaryCcy || "PENDING":
case "*Primary CCY": return helpers.primaryCcy || "PENDING":
case "*Trede Date": return helpers.tradeDate || "PENDING";
case "*Trade Date': return helpers tradeDate || "PENDING";
S
6t 241
case "Comment": return cleanText(src.comment):
case "*Tier 3 Product Type*: return "PENDING":
case 'Trader": return cleanText(src.trader):
cleanText(src.productType)]) || "PENDING";
case "Issuer': return helpers.issuer;
case "ISIN Code": return "PENDING";
case "Price": return helpers.price;
cleanText(src.product)]) |[*PENDING*;
case "Issuer": return *PENDING":
case '*Tier 1 Product Type*:
case **Tier 2 Product Type":
case "Trader": return ***
default: return ***
switch(fieldX
Search

// OCR PHOTO 092: IMG_7216.HEIC
case "Ticker": return cleanText(src.underlying).toUpperCase) I| 'PENDING":
if(typeof value ==- "string" && value —-- "PENDING") return "PENDING";
return parts ? formatOutputDateFromParts(parts) : cleanText(value);
-
0t241
if(value -=- null ]| value —- undefined) return **;
return String(value) length > 9 ? String(value) : n/
function normalizeFieldValue(field, value)(
const parts - parseDatePartsNoTz(value):
return n === null ? cleanText(value) : n:
if(n === null) return cleanText(value):
case "Price": return helpers.price:
if(NUMERIC_FIELDS.has(field)X
const n = parseNumber(value);
const n = parseNumber(value):
case "Comment": retum*:
if(DATE_TARGETS.has(field)X
if(field === **Trade ID*X
default: retum
Search

// OCR PHOTO 093: IMG_7217.HEIC
if(CCY_TARGETS.has(field)) return cleanText(value).toUpperCase) II (cleanText(value)
const value = shared !== undefined ? shared : assetSpecificField Value(field, record,
zm
if(UPPER_TARGETS.has(field)) return cleanText(value).toUpperCase():
if(sourceKey startsWith("raw:")) return src.raw[sourceKey.slice(4)]:
if(key === "addonCount") return helpers.aggregatedAddonCount;
=== "pending" ? 'PENDING" : cleanText(value).toUpperCase());
const shared = sharedFieldValue(field, record, helpers):
function evaluateNodeValue(sourceKey, src, helpers)[
sourceKeys: (meta,sources || 0) mep(h => *raw:* + h).
function computeDefaultField(field, record, helpers)(
const meta = defaultMappingMeta(field);
value: normalizeFieldValue(field, vatue),
if(sourceKey.startsWith("agg:")X
const key = sourceKey.slice(4):
return helpers[sourceKey] || **
provenance: "DEFAULT_RULE".
- +
return cleanText(value):
if(IsourceKey) return**;
Search
return ***
helpers):
return (

// OCR PHOTO 094: IMG_7218.HEIC
*src". "record", "helpers","parseNumber","cleanText","mapKnownissuerText","normalizePriceP
"deriveMaturitySafe","routePbByBook","parseDatePartsNoTz",'resolveSettlementParts",
deriveMaturitySafe, routePbByBook, parseDatePartsNoTz, resolveSettlementParts,
src, record, helpers, parseNumber, cleanText, mapKnown|ssuerText,
DRLL
31 2411
"parseTenorYears","formatOutputDateFromParts',
function evaluateFormula(expr, record, helpersX
parseTenorYears, formatOutputDateFromParts
function evaluateField (fietd, record, helpers)
const rute - currentRute(field):
const src = record.baseRow;
const sro = record, baseRow:
*source"y
constfn = new Function(
note: meta.summary
* return ($(expr});*
normalizePricePoints,
if(rule.mode =
Search
return fn(
oints",

// OCR PHOTO 095: IMG_7219.HEIC
W
Em
note: "Custom source: $|rawHeaderLabetForKey(rule.sourceKey)}*
LOD
const raw = evaluateNodeValue(rule.sourceKey, src, helpers);
const raw = evaluateFormula(rule.expr ]| ***, record, helpers);
01241
sourceKeys: rule.sourceKey ? [rule.sourceKey] = (].
value: normalizeFieldValue(field, rule.constant),
95
value: normalizeFieldValue (field, raw).
provenance: "CONSTANT_OVERRIDE",
value: normalizeFieldValue(field, raw).
provenance: "FORMULA_OVERRIDE'
provenance: "SOURCE_OVERRIDE",
note: "Constant: $(rule.constant)
- +
it(rule mode --- "constant"y
if(rule.mode === "formula*X
note: "Formula override"
sourceKeys: I.
sourceKeys: E.
Search
return (
return
return (
tryl

// OCR PHOTO 096: IMG_7220.HEIC
*, "error"):
0t 241
return computeDefaultField(field, record, helpers):
log( Formula error for $(field}: $[e.message)"
const meta = defaultMappingMeta(fietd):
note: * Formula error: $(e.message}*
provenance: "FORMULA_ERROR",
provenance: "BLANK_OVERRIDE",
function buildAiDefaultPrompt(field)l
- +
if(rule.mode === "blank"X
value: "PENDING",
note: "Forced blank*
sourceKeys: I].
sourceKeys: [,
Search
Jcatch(eX
return (
value: **
return (

// OCR PHOTO 097: IMG_7221.HEIC
requirements: $(meta.summary]Dates must be output as dd/mm/yyyy whenever this field
$fassetConfig).label} branch. Start from the board default rule, then adapt it with these
is a date. Return "PENDING rather than guessing when the input is insufficient.*:
const sampleSourceRows = state.sourceRows.slice(0, 5).map(src →> ({
const currentOutputPreview - state.mappedRows.slice(0, 5).map(r => ({
return ' Give me a tailored JavaScript expression for "$[field}" in the
31 241
sourceRow: r.record.baseRow.sourceRow,
const meta - defaultMappingMeta(field):
currentTergetFieldValue:r.output[field]
canonical: compactObject(src, 80),
source_logic urt: BASE_LOGIC_URL,
function buildAiFieldContext(field)[
raw: compactObject(src.raw, 80)
anchorType: r.helpers.anchorType,
logicalindex: src.logicalindex,
- +
sourceRow: src. source Row,
tradeld: r.output["*Trade ID'],
workbook: state.fileName || **
sheet: state.sheetName 11M
rowStatus: r.rowStatus,
Search
return "
3)):

// OCR PHOTO 098: IMG_7222.HEIC
"src","record", "helpers","parseNumber", "cleanText",'mapKnownlssuerText"," normalizePriceP
"deriveMaturitySafe","routePbByBook","'parseDatePartsNoTz'","resolveSettlementParts",
"Use src.raw[ "Exact Header] it you need the original worksheet header directly..
"Use helpers for already-derived board values when that is cleaner")
current rule_summary: currentRuleSummary(field),
01 241
"parseTenorYears""formatOutputDateFromParts*
DALR
addon window_days: state.addonWindowDays,
mandatory: MANDATORY_FIELDS.has(field),
source_row_count: state.sourceRows.length,
output_ row_count: state.mappedRows.length,
98
default_rule_summary: meta.summary,
default_source_headers: meta.sources,
detected_header_map: state.headerMap,
date_output_standard: "dd/mm/yyyy",
source_headers: state.sheetHeaders,
current run_mode: state.runMode,
trade_id_mode: state.tradeldMode,
preferred_formula_runtime: {
- +
asset: assetConfig).label,
expression_only: true,
available_identifiers: [
target field: field,
Search
notes:
oints",

// OCR PHOTO 099: IMG_7223.HEIC
*Y"formuta\":\"stringl",\"explanation)":\"stringl", \"assumptions\":[\"stringl"],\"warnings!":[\*
W
"Do NOT wrap the expression in a function, const, let, return, or backticks.",
normalizePricePoints, deriveMaturitySafe, routePbByBook, parseDatePartsNoTz,
zm
"src, record, helpers, parseNumber, cleanText, mapKnownlssuerText,
resolveSettlementParts, parseTenorYears, formatOutputDateFromParts.",
*Use ONLY the structured context provided in the user message..
*The formula must be a SINGLE valid JavaScript expression only.",
*Do NOT use web search, research, or any external knowledge.",
"The expression will run with these identifiers already available:"*,
*Return "PENDING' instead of guessing unresolved data."
0: 241
current_output_preview: currentOutputPreview
"If the target is a date, output dd/mm/yyyy.".
sample_source_rows: sampleSourceRows,
string\"], \"confidence\":\"high|medium|low)"y",
99
function buildAiProjectInstructions(X
- +
*Required JSON schema:*,
Search
return I
board.",
notes."

// OCR PHOTO 100: IMG_7224.HEIC
"If the rule is uncertain or the input is insufficient, return 'PENDING' rather than
zm
if(start === -1) throw new Error("No JSON object found in Al response"):
61 241
if(Icleaned) throw new Error(*Empty Al response*):
let depth = 0, inString = false, escapeNext = false;
if(escapeNext)( escapeNext = false; continue: }
if(ch === W*Y escapeNext = true; continue; }
1001
tryf return JSON.parse(cleaned): }catch(e)0
*For all date outputs, use dd/mm/yyyy.*
for(let i = start, i < cleaned.tength; i++)
function extractFirstJsonObject(text)(
const start = cleaned.indexOf(*(*):
const cleaned - String(text ||**)
if(ch === ***) inString = false;
- +
-replace(/** - json/i,**)
const oh = cleaned(];
_replace(/^- - -7**)
replace(/* $/.**)
Search
J-join("\n");
if(inStringX
continue;
guessing.
0.9958."
trim()
trimi;

// OCR PHOTO 101: IMG_7225.HEIC
*src""record","helpers","'parseNumber","cleanText","mapKnownlssuerText"," normalizePriceP
"derive MaturitySafe"," routePbByBook","'parseDatePartsNoTz","resolveSettlementParts",
DID
throw new Error(*Unable to parse JSON object from Al response");
lif(depth === 0) return JSON.parse(cleaned.slice(start, i + 1));
DAAL
31241
if(Itext) return { ok:false, error:"Formuta is empty" }:
"parseTenorYears",'formatOutputDateFromParts",
101
it(ch === *Y**) inString - true; continue; }
function validateFormulaExpression(expr){
const text = trimFormulaText(expr):
return ( ok:false, errone.message }
if(ch === *(*) depth += 1;
return { ok:true, error:** };
if(ch =-= *}*) depth -= 1;
return ($(text}):*
new Function(
Search
Jcatch(eX
tryl
oints",

// OCR PHOTO 102: IMG_7226.HEIC
body: JSON.stringify({ title:"Al Formuta Copilot", project_id:**, is_tool_call:true })
if(!res.ok) throw new Error(^ Conversation create failed: HTTP $(res.status)* ):
if(state.aiFormulaConversationid) return state.aiFormulaConversationid;
if(f] I| tj.id) throw new Error("Conversation create returned no id");
юt 241
constres - await fetch("/studio/api/conversations', 1
headers:( "Content-Type": "application/json* }.
102
async function ensureAiConversationK
state.aiFormulaConversationid = j.idg
state.eiFormulaRawResponse = **:
state.aiFormulaSuggestion = null;
state.siFormulaLoading = false;
- +
function clearAiSuggestionCl
state.aiFormulaEditable = **:
function updateAiStreamBox0X
const el = qs("aiStreamBox"):
credentials:"same-origin*,
state.aiFormulaError = **:
constj = await res.json(:
updateTopPills(:
method:"POST".
Search
return j.id;
7):

// OCR PHOTO 103: IMG_7227.HEIC
setStatus('Process a worksheet first so the Al Formula Copilot can use actual headers
setStatus("Enter a prompt describing the tailored field logic you want.", "warn"):
if(el) el.textContent = state.aiFormulaRawResponse || *Waiting for streamed
"warn");
setStatus("Choose a target field for the Al formula request.",
const prompt = trimFormulaText(state.aiFormulaPrompt);
0+ 241
103
async function runAiFormulaCopilot0
const field - state.aiFormulaField;
setActiveTab("mappingStudio");
state.aiFormulaLoading = true:
- +
if(Istate.sourceRows.length)l
and sample rows.", "warn");
renderMappingStudio0:
clearAiSuggestion®:
updateTopPills(:
Search
if(Lprompt)
response..
return;
if(|field)
return:
return;

// OCR PHOTO 104: IMG_7228.HEIC
const res = await fetch(* /studio/api/session/run?$[params.toString0)*
setStatus(' Al Formula Copilot is generating a tailored formula for
const conversationid = await ensureAiConversation():
message: JSON.stringify(messagePayload, null, 2),
3 241
"info*);
headers:( "Content-Type":"application/json* },
104
log( Al formula request started for $(field)" ,
const params - new URLSearchParams([
request_type: "tailored_field_formula"
<b>$(escapeHtm|(field)j</b>.*, "btue"):
selected _tool: AI_SELECTED_TOOLS
conversation_id: conversationid,
context: buitdAiFieldContext(field)
const messagePayload = [
asset: assetConfig().tabel,
credentials: same-origin'.
user prompt: prompt,
body: JSON.stringity(E
model: "gpt-5-low",
target field: field,
method:"POST,
Search
tryl

// OCR PHOTO 105: IMG_7229.HEIC
lif(!res.ok) throw new Error(* Al formula run failed: HTTP $(res.status)* );
if(tres.body) throw new Error("Al formula stream returned no body");
if(evt.type === "message" && typeof evt.content === "string"K
throw new Error("Al formula request was aborted");
of 241
project_instructions: buildAiProjectinstructions()
if(evt.type ==- "message" && evt.start === trueX
state.aiFormulaRawResponse += evt.content;
105
const decoder = new TextDecoder("utf-8");
if(levt || typeof evt !== "object") return;
const reader = res.body.getReader();
state.aiFormulaRawResponse = "*;
function handleEvent(evt){
if(evt.type -== "aborted"
updateAiStreamBox();
updateAiStreamBox(;
let buffer = "*;
Search
return;
return;

// OCR PHOTO 106: IMG_7230.HEIC
const parsed = extractFirstJsonObject(state.aiFormulaRawResponse);
tryf handleEvent(JSON.parse(payload)); jcatch(e)0
const formula = trimFormulaText(parsed.formula (''*);
const validation = validateFormulaExpression(formula);
of 241
buffer += decoder.decode(value, { stream:true });
106
const { value, done } = await reader.read();
if(Itrimmed.startsWith("data:")) continue;
const payload = trimmed.slice(5).trim();
const chunks = buffer.split("In\n");
const lines = chunk.split("In");
- +
buffer = chunks.pop() || ";
for(const chunk of chunksX
const trimmed = line.trim();
state.aiFormulaSuggestion = /
for(const line of lines){
if(!payload) continue;
if(done) break;
while(trueX
Search
field,

// OCR PHOTO 107: IMG_7231.HEIC
setStatus(* Al suggested a tailored formula for <b>$[escapeHtml(field)]</b>. Review it
setStatus(* Al returned a draft for <b>${escapeHtm!(field)}</b>, but it needs a quick
assumptions: Array.isArray(parsed.assumptions) ? parsed.assumptions.map(x=>
state.aiFormulaError = * Al returned a formula but it is not valid JavaScript yet:
zm
warnings: Array.isArray(parsed.warnings) ? parsed.warnings.map(x=>
*, "warn");
log(* Al formula invatid for $(field}: $(validation.error)*
6t241
log(* Al formula generated for $(field}' , "success");
explanation: cleanText(parsed.explanation ||**).
confidence: cleanText(parsed.confidence ||**)
state.aiFormulaEditable - formula;
state.aiFormulaLoading = false;
cleanText(x)) filter(Boolean) : D,
cleanText(x)).filter(Boolean) : 0.
edit before applying.*, "warn");
and apply when ready.*, "good");
state.aiFormulaError = ***
renderMappingStudio(:
updateTopPills(:
if(Ivalidation.ok)
S(validation.error)"
Search
formula,
fcatch(e)(
Jelsel

// OCR PHOTO 108: IMG_7232.HEIC
setStatus('Al Formula Copilot failed. $[escapeHtml(e.message)}*, "bad"):
if(value === null | value ==- undefined || value =-=**) return**
"error"):
of 241
if(field --= "Price") return prettyNumber(value, 2);
for(const record of state.logicalRecords.stice(0, 5)X
log( Al formula request failed: $(e.message)*"
108
function renderValueForDisplay(field, value){
function previewFormulaForField(field, expr)(
if(|state.logica|Records.length) return rows;
const helpers = build Helpers(record);
state,aiFormulaError - e.message;
state.aiFormulaLoading =false:
const text = trimFormulaText(expr):
- +
if(typeof value -== "number"y
return prettyNumber(value, 0):
renderMappingStudio0):
return String(value);
if(ltext) return rows;
updateTopPills):
const rows - 0:
Search

// OCR PHOTO 109: IMG_7233.HEIC
const current = state.mappedRows.find(m => m.record.baseRow.sourceRow ---
D ID
01 241
const raw = evaluateFormula(text, record, helpers);
currentValue: current ? current.output[field] : **:
currentValue: current ? current.output[field] : **
const velue - normalizeFieldValue(fietd, raw);
109
source Row: record.baseRow.sourceRow,
sourceRow: record.baseRow.sourceRow,
anchorType: helpers.anchorType,
anchorType: helpers.anchorType,
function applyAiFormulaSuggestion(t
- +
record.baseRow.sourceRow):
erron e.message
result: value,
rows.pushQ
rows.push(f
error: **
Jcatch(e)
result: *
return rows;
Search
tryt
)):

// OCR PHOTO 110: IMG_7234.HEIC
const expr = trimFormulaText(state.aiFormulaEditable || (state.aiFormulaSuggestion &&
setStatus(" The formuta is not valid yet: $(escapeHtmt(validation.error)}*, *warn");
2m
state.aiFormulaError - * Formula validation failed: $(validation.error}* ;
success"):
log(* Al formula applied to $(field) for $(assetConfig).label}*
rutes[field] = ( ...currentRule(field), mode:"formuta", expr}:
setStatus(*There is no Al formula to apply yet.", *warn");
3$ 241
const validation = validateFormulaExpression(expr);
const rules = ensureCurrentAssetRuleStore):
1101
state_aiFormulaSuggestion.formula) ||**):
constfield = state.aiFormulaField:
- +
renderMappingStudio();
state.aiFormulaError = **
ff(-validation.okX
saveLocal):
Search
if(lfield)(
return
if(lexpry
return:
return;

// OCR PHOTO 111: IMG_7235.HEIC
<b>$/escapeHtml(assetConfig().label)j</b>. The sheet has been reprocessed with the new
if(state.aiFormulaSuggestion && state.aiFormulaSuggestion.field !=- field)
setStatus(* Applied Al formula to <b»$(escapeHtm!(field)}</b> in
state.aiFormulaPrompt === state.aiFormutaLastAutoPrompt ||
state.aiFormulaLastAutoPrompt = state.aiFormulaPrompt;
state.aiFormulaPrompt = buildAiDefaultPrompt(field);
0t 241
ItrimFormulaText(state.aiFormutaPrompt) |l
state.aiFormulaSuggestion = null;
state.aiFormulaRawResponse = **:
- +
function jumpToAiField(field)[
_const shouldRefreshPrompt -
state.aiFormulaField !=- field;
setActiveTab("mappingStudio"):
state.aiFormulaField - field;
state.aiFormulaEditable = **:
if(shouldRefreshPrompt)k
state.aiFormulaError = *;
processCurrentSheet):
renderMappingStudiol):
updateTopPilts;
rule.', "good"):
saveLocal(:
Search

// OCR PHOTO 112: IMG_7236.HEIC
return Object.prototype.hasOwnProperty.call(state.manualOutputOverrides, key)
if(el) el.scrollintoView({ behavior:"smooth", block:"start" });
function overrideKey(rowld, field)( return $(rowld}|$(fietd}* ; }
of 241
function setManualOverrideValue(rowld, field, value)(
state.currentFileFingerprint || state.fileName || "*
function getManualOverrideValue(rowld, fietd)
112
const el = qs("aiFormulaCopilotCard");
function buildStableRecordld(record)
const key = overrideKey(rowld, field);
? state.manualOutputOverrides[key]
return "RID-" + simpleHash(key);
- +
const src = record.baseRow;
state.sheetName || *,
src.sourceRow || **,
src.logicalindex || "
setTimeout(() =>{
state.assetKey,
const key = [
: undefined;
Search
J.join("|");
), 60);

// OCR PHOTO 113: IMG_7237.HEIC
zm
state.manualOutputOverrides[overrideKey(rowld, field)] = value;
delete state.manualOutputOverrides[overrideKey(rowld, field)]:
const rowids - new Set(state.mappedRows.map(r=> r.rowld)):
const rowlds - new Set(state.mappedRows.map(r=> r.rowld));
Object.keys(state.manualOutputOverrides || ().forEach(k => (
if(rowlds.has(rowld)) delete state.manualOutputOverrides[k];
of 241
function clearManualOverrideValue(rowld, field)X
function countCurrentSheetManualOverrides(T
function clearCurrentSheetManualOverrides(K
if(rowlds.has(rowld)) count += 1;
- +
const rowld = k.split(*Il")[0];
const rowld = k.split(*ll")[0];
saveLocal);
saveLocal):
let count = 0:
return count;
saveLocal();
Search
7):
3);

// OCR PHOTO 114: IMG_7238.HEIC
W
zm
if(isRowSelected(r.rowld)) state.exportExcludedRows[r.rowld]= true;
lif(keep.has(r.rowld)) delete state.exportExcludedRows[r.rowld];
if(selected) delete state.exportExcludedRows[r.rowld]:
function countSelectedRows(rows = state.mappedRows)
const keep = new Set(rowsToKeep.map(r=> r.rowld)):
return rows.filter(r => isRowSelected(r.rowid)).length:
0+ 241
function keepOnlyRows(scopeRows, rowsToKeepX
else state.exportExcludedRows[r.rowld] = true;
else state.exportExcludedRows[r.rowld] = true;
else delete state.exportExcludedRows[r.rowld];
114
function setRowsSelected(rows, selected)(
function invertRowsSelection(rowsX
- +
scopeRows.forEach(r=> [
rows.forEach(r=> (
rows.forEach(r=> (
saveLocal():
saveLocal):
saveLocal(:
Search
7):

// OCR PHOTO 115: IMG_7239.HEIC
function getSelectedMappedRows()/ return state.mappedRows.filterr=>
) TD
const rowlds = new Set(state.mappedRows.map(r => r.rowld));
if(!rowlds.has(rowld)) delete state.exportExcludedRows[rowid]):
function addFileHistoryEntry(file, sheetNames, preferredSheet)
Object.keys(state.exportExcludedRows).forEach(rowld →> {
of 241
115
sheetNames: sheetNames.slice(0, 40),
function clearAllRowSelectionState(X
lastModified: file.lastModified || 0,
preferredSheet: preferredSheet||**,
sheetCount: sheetNames.length,
function pruneExportSelection
state.exportExcludedRows = 0;
fingerprint: fileFingerprint(file),
- +
isRowSelected(r.rowid)); }
fileName: file.name ||',
sizeBytes: file.size || 0,
insertedAt: nowlso(),
id: makeld ("file"),
const entry = (
saveLocal():
saveLocal):
Search

// OCR PHOTO 116: IMG_7240.HEIC
if(state.fileHistory.length > 200) state.fileHistory = state.fiteHistory.slice(0, 200);
const entry - state.fileHistory.find(x => x.id === state.currentFiteHistoryld);
log( File inserted into board history: $(entry.fileName)*, "success");
entry.processCount = Number(entry.processCount /| 0) + 1;
entry.lastProcessedRows = Number(rowsLoaded || 0);
of 241
function markCurrentFileProcessed(rowsLoaded)
entry.lastProcessedSheet = state.sheetName || "*;
entry.lastProcessedAsset= assetConfig().label;
116
function computeTradeKey(mappedRow)(
state.currentFileHistoryld = entry.id;
if(!state.currentFileHistoryld) return;
entry.lastProcessedAt = nowlso();
state.fileHistory.unshift(entry):
lastProcessedRows: 0,
lastProcessedSheet: "*.
lastProcessedAsset: **
lastProcessedAt: **,
processCount: 0,
if(lentry) return;
saveLocal):
saveLocal);
Search

// OCR PHOTO 117: IMG_7241.HEIC
*$(state.currentFileFingerprint)|$(state.sheetName}|$(state.assetKey}$(src.sourceRow)';
zm
return ( tradeKey: "TRD-" + simpleHash(naturalKey), naturalKey };
of 241
function buildGoldenInstanceBase(mappedRow)
state.currentFileFingerprint || state.fileName || "*,
const src = mappedRow.record.baseRow;
const src = mappedRow.record.baseRow;
if(!naturalKey.replace(N//g,"'').trim()X
const out = mappedRow.output;
cleanText(out["*Primary CCY"]),
- +
cleanText(out["*Trade Date"]),
cleanText(out["Sales Client"]),
cleanText(out["*Trade ID"]),
let naturalKey = parts.join("|");
cleanText(out["ISIN Code"])
state.sheetName ||"*,
src.sourceRow || "™
src.logicallndex|*
state.assetKey,
const parts = [
state.assetKey,
naturalKey =
Search
return [

// OCR PHOTO 118: IMG_7242.HEIC
state.goldenRecords = state.goldenRecords.filter(r => r.tradeKey !== tradeKey);
zm
rec.instanceKeys = (rec.instanceKeys || (J).fitter(k => k !== instanceBase);
if(state.selectedGoldenKey= tradeKey) state.selectedGoldenKey = *;
removeGoldeninstanceFromTrade(previousTradeKey, instanceBase);
function removeGoldenInstanceFromTrade(tradeKey, instanceBase)
const rec - state.goldenRecords.find(r => r.tradeKey === tradeKey);
const instanceBase = buildGoldeninstanceBase(mappedRow);
if(previousTradeKey && previousTradeKey !== tradeKey)
state.goldeninstancelndex|instanceBase]= tradeKey;
of 241
rec.occurrences = rec.instanceKeys.length;
tunction upsertGoldenRecord(mappedRow)
const src = mappedRow.record.baseRow;
If(ItradoKey || linstanceBase) return;
if(trec.ocourrencesX
Hf(Irec) return;
Search
J.join("(");

// OCR PHOTO 119: IMG_7243.HEIC
zm
rec.instanceKeys = (rec.instanceKeys || (J).filter(k => k !== instanceBase);
if(state.selectedGoldenKey =-- tradeKey) state.selectedGoldenKey = *™*;
function removeGoldenInstanceFromTrade(tradeKey, instanceBase)(
const previousTradeKey = state.goldenInstancelndex[instanceBase];
removeGoldeninstanceFromTrade(previousTradeKey, instanceBase);
DIDD
const rec = state.goldenRecords.find(r => rtradeKey === tradeKey);
const [ tradeKey, naturalKey ) = computeTradeKey(mappedRow);
const instanceBase = buildGoldeninstanceBase(mappedRow):
if(previousTradeKey && previousTradeKey !== tradeKey)(
state.goldeninstancelndex[instanceBase]= tradeKey;
of 241
118
rec-occurrences = rec.instanceKeys.length;
function upsertGoldenRecord(mappedRow)
const src = mappedRow.record.baseRow;
if((tradeKey || linstanceBase) return;
-+
if(lrec.occurrences)[
if(lrec) return;
Search
J.join("|");

// OCR PHOTO 120: IMG_7244.HEIC
W
zm
let record = state.goldenRecords.find(r => r.tradeKey === tradeKey);
1
primaryCcy: mappedRow.output["*Primary CCY"] || "''.
of 241
tradeDate: mappedRow.output["*Trade Date"] |l"',
salesClient: mappedRow.output["Sales Client"] || *',
tradeld: mappedRow.output["*Trade ID"] ||''*,
addonWindowDays: state.addonWindowDays
119
isin: mappedRow.output["ISIN Code"] || "',
rowStatus: mappedRow.rowStatus || **
rowStatus: mappedRow.rowStatus,
tradeldMode: state.tradeldMode,
workbook: state.fileName || **.
- +
sheet: state.sheetName || **,
asset: assetConfig().label,
asset: assetConfig().label,
sourceRow: src.sourceRow,
capturedAt: nowlso),
mode: state.runMode,
const summary = (
const ioMeta = [
Search
if(!recordX
record ={
tradeKey,

// OCR PHOTO 121: IMG_7245.HEIC
const instanceSet = new Set(Array.isArray(record.instanceKeys) ? record.instanceKeys:
W
zm
workbooksSeen: Array.from(new Set([state.fileName || "*])). filter(Boolean),
record.instanceKeys = Array.from(instanceSet).slice(-1000);
const isNewInstance = linstanceSet.has(instanceBase);
of 241
goldenOutput: clonePlain(mappedRow.output),
record.occurrences = record.instanceKeys.length;
latestRowStatus: mappedRow.rowStatus || '*')
goldenInput: clonePlain(mappedRow.record),
120
latestWorkbook: state.fileName || '",
goldeninputMeta: clonePlain(ioMeta),
goldenOutputMeta: clonePlain(ioMeta)
latestSheet: state.sheetName ||'*,
state.goldenRecords.unshift(record);
return { created:true, refreshed:false};
firstSeenAt: ioMeta.capturedAt,
lastSeenAt: ioMeta.capturedAt,
latestAsset: assetConfig().label,
instanceKeys: [instanceBase],
instanceSet.add(instanceBase):
- +
occurrences: 1,
naturalKey,
summary,
Search
0);

// OCR PHOTO 122: IMG_7246.HEIC
zm
record.workbooksSeen = Array.from(new Set([...(record.workbooksSeen || 0),
if(!state.selectedGoldenKey && state.goldenRecords.lengthX
state.selectedGoldenKey = state.goldenRecords[0].tradeKey;
record.goldenOutput = clonePlain(mappedRow.output);
String(b.lastSeenAt).localeCompare(String(a.lastSeenAt)));
record.latestRowStatus = mappedRow.rowStatus ||"*':
record.goldenInput = clonePlain(mappedRow.record);
of 241
record.goldenOutputMeta = clonePlain(ioMeta);
return { created:false, refreshed:isNewInstance};
record.latestWorkbook = state.fileName || "";
record.goldenInputMeta = clonePlain(ioMeta);
function syncGoldenRecordsFromCurrentRun(X
121
record.latestSheet = state.sheetName (|"*;
record.lastSeenAt = ioMeta.capturedAt;
record.latestAsset = assetConfig().label;
const res = upsertGoldenRecord(row);
if(!state.mappedRows.length) return;
state.mappedRows.forEach(row =>{
else if(res.refreshed) refreshed += 1;
state.fileName || ''*])). filter(Boolean);
state.goldenRecords.sort((a, b) =>
- +
record.summary = summary;
let created = 0, refreshed = 0;
if(res.created) created += 1;
Search

// OCR PHOTO 123: IMG_7247.HEIC
return qsa("#processedSheet td.editable-cell").find(td -> td.dataset.rowid -=- rowld &&
const row = state.mappedRows.find(r => r.rowld === state.processedSelectedRowld) ||
W
Jelse if(state.selectedGoldenKey && !state.goldenRecords.find(r => r.tradeKey===
log(* Golden record synced: $(created} new unique trade(s), $(refreshed} refreshed
zm
state.selectedGoldenKey = state.goldenRecords[0].tradeKey;
state.selectedGoldenKey) && state.goldenRecords.length){
state.processedintineEdit = { rowld:**, field:**, draft:**}:
of 241
const statusEl = qs("processedSelectionStatus");
const clearBtn = qs("btnClearSelectedCellEdit");
function updateProcessedSelectionPanel(X
const infoEl - qs("processedSelectioninfo");
const field = state.processedSelectedField;
function findProcessedCell(rowid, fieldX
function clearProcessedinlineEditQl
td.dataset.field ==- field) || null;
existing trade(s)*, "success");
if(linfoEl || !statusEl) return;
saveLocal():
Search
null;

// OCR PHOTO 124: IMG_7248.HEIC
*Trade <span class="inline-code">${escapeHtml(row.output["*Trade ID"] || '*))</span>
statusELinnerHTML = * ${badge("No manual override","blue")} Select a processed cell
const note - row.provenance && row.provenance[field] ? row.provenance[field].note :
•Asset <span class="inline-code">$(escapeHtml(assetConfig().label))</span>-* +
*Current value <span class="inline-code">${escapeHtml(currentValue)j</span>*;
const currentValue = renderValueForDisplay(field, row.output[field]) || "(blank)*:
zm
*$(manual? badge("Manual override active","amber") : badge("No manual
const manual = getManualOverrideValue(row.rowld, field) !=- undefined;
*Field <span class="inline-code">$(escapeHtm((field))</span> • * +
code">${escapeHtml(row.record.baseRow.sourceRow)}</span>.*
of 241
123
infoEL.innerHTML = "No cell selected yet.";
if(clearBtn) clearBtn.disabled = true;
Source row <span class="inline-
below to edit it in place.*;
* $(escapeHtml(note)} :
statusELinnerHTML-
infoELinnerHTML=
if(!row || lfield)[
override","blue")}* +
Search
return;
n/a":

// OCR PHOTO 125: IMG_7249.HEIC
const searchTerm = canonicalHeader(state.processedSearch ||"');
qsa("#processedSheet .editable-cell.selected").forEach(td =>
of 241
function markProcessedSelectedCell(rowld, field)
...TARGET_HEADERS.map(h => r.output[h])
124
J.map(v => canonicalHeader(v)).join(" |");
if(clearBtn) clearBtn.disabled = !manual;
const td = findProcessedCell(rowld, field);
state.processedSelectedRowld = rowld;
return rowText.includes(searchTerm);
-state.processedSelectedField = field;
function getFilteredProcessedRows()f
if(td) td.classList.add("selected");
updateProcessedSelectionPanel():
:state.mappedRows.filter(r=>f
r.record.baseRow.sourceRow,
td.classList.remove("selected"));
? state.mappedRows
return !searchTerm
const rowText = [
r.rowStatus,
Search

// OCR PHOTO 126: IMG_7250.HEIC
if(colldx + 1 < TARGET_HEADERS.length) return { rowld, field:TARGET_HEADERS[colldx
zm
if(rowldx + 1 < rows.length) return { rowld:rows[rowldx + 1].rowld, field.};
if(colldx - 1 >= 0) return { rowld, field:TARGET_HEADERS[colldx - 1] };
of 241
function getProcessedMoveTarget(rowld, field, move)
const colldx = TARGET_HEADERS.indexOf(field);
125
const rows = getFilteredProcessedRows();
if(!td || td.querySelector("input")) return;
if(rowldx < 0 || colldx < 0) return nult;
function beginProcessedCellEdit(td)
const rowld = td.dataset.rowid ||"';
if(move === "down"X
if(move ==- "right"X
if(move ==- "left"X
return null;
return nutl;
return null;
Search
return null;
+ 1]%:

// OCR PHOTO 127: IMG_7251.HEIC
const currentValue = manualValue !== undefined ? manualValue : (row.output|field] ??
const nextTarget = commit? getProcessedMoveTarget(rowld, field, move) : null;
zm
state.processedinlineEdit = { rowld, field, draft:String(currentValue ?? **) );
const row = state.mappedRows.find(r => r.rowld === rowld);
const manualValue = getManualOverrideValue(rowld, field);
of 241
const input - document.createElement("input");
126
markProcessedSelectedCell(rowld, field);
input.value = String(currentValue ?? "'");
constfield - td.dataset.field || "';
function finish(commit, move)(
td.classList.add("editing");
input.autocomplete = "off";
if(!rowld || !field) return;
td.appendChild(input);
td.textContent ="";
input.type = "text";
let finished = false;
if(finished) return;
if(!row) return;
finished = true;
Search
**):

// OCR PHOTO 128: IMG_7252.HEIC
setStatus(* Applied manual in-cell edit to <b>${escapeHtml(field)j</b> on source row
zm
log(* Manual processed-sheet edit applied: asset=$(assetConfig().label),
const nextTd = findProcessedCell(nextTarget.rowld, nextTarget.field);
row=${row.record.baseRow.sourceRow}, field=$(field}*, "success");
<b>$(escapeHtml(row.record.baseRow.sourceRow))</b>., "good");
'", field:"', draft:'' };
of 241
state.processedSelectedRowld = nextTarget.rowld;
state.processedSelectedField = nextTarget.field;
setManualOverrideValue(rowld, field, draft);
if(nextTd) beginProcessedCellEdit(nextTd);
127
state.processedinlineEdit = { rowld:"*,
setActiveTab("processedSheet");
const draft = input.value;
renderProcessedSheet();
processCurrentSheet();
updateTopPills();
setTimeout() => (
if(nextTarget)
if(!commit){
Search
return;
}, 60);

// OCR PHOTO 129: IMG_7253.HEIC
input.addEventListener("input", () => { state.processedinlineEdit.draft = input.value; }):
of 241
input.addEventListener("blur", () => finish(true, null));
function openProcessedCellEditor(rowld, field)
128
input.addEventListener("keydown", e => {
markProcessedSelectedCell(rowld, field);
const td = findProcessedCell(rowld, field);
finish(true, e.shiftKey? "left" : "right");
if(td) beginProcessedCellEdit(td);
function processCurrentSheet({
- +
jelse if(e.key ——- "Escape"Y
clearProcessedInlineEdit();
Jelse if(e.key === "Tab"X
if(e.key === "Enter"X
e.preventDefault();
finish(true, "down");
e.preventDefault):
e.preventDefault():
finish(false, null);
input.focus();
input.select();
Search

// OCR PHOTO 130: IMG_7254.HEIC
of241 0
const manual = getManualOverrideValue(rowld, field);
const res = evaluateField(field, record, helpers);
state.logicalRecords = buildLogicalRecords();
129
const rowld = buildStableRecordld(record);
state.logicalRecords.forEach(record =>{
const helpers = buildHelpers(record);
TARGET_HEADERS.forEach(field =>{
TARGET_HEADERS.forEach(field => {
- +
if(!state.sourceRows.length)(
state.logicalRecords = [;
output(field] = res.value;
state.mappedRows = [];
provenance[field] = res;
if(manual !== undefined)
state.mappedRows - 0;
state.exceptions = [];
const provenance = 1;
state.exceptions = [];
const output = f);
renderAll);
Search
return;

// OCR PHOTO 131: IMG_7255.HEIC
Jelse if(MANDATORY_FIELDS.has(field) && (v===''* || v === nult || v === undefined || v
? provenance[field].sourceKeys.map(rawHeaderLabelForKey).join(", ")
sourceValue: provenance[field] && provenance[field].sourceKeys &&
const note = provenance[field] ? provenance[field].note : *';
tradeld: output["*Trade ID"] || record.baseRow.logicallndex,
reason: note || "Field unresolved and set to PENDING.",
output[field] - normalizeFieldValue(field, manual);
of 241
note: "Manual processed-sheet cell override"
sourceRow: record.baseRow.sourceRow,
130
provenance: "MANUAL_OVERRIDE",
TARGET_HEADERS.forEach(field => {
provenance[field].sourceKeys.length
provenance[field] -{
value: output|field],
const v = output|field];
const rowErrors = [);
if(v =-- *PENDING"X
sourceKeys: 0.
rowErrors.push({
Search
field,
- "NULL")X

// OCR PHOTO 132: IMG_7256.HEIC
? provenance[field].sourceKeys.map(rawHeaderLabelForKey).join(*. ")
sourceValue: provenance[field] && provenance[field).sourceKeys &&
tradeld: output["*Trade ID"] || record.baseRow.logicallndex,
const rowStatus = rowErrors.length ? "Error" : "Valid";
of 241
reason: note || "Mandatory field unresolved.",
sourceRow: record.baseRow.sourceRow,
131
state.exceptions.push(...rowErrors);
provenance[field].sourceKeys.length
- +
output(field] = "PENDING";
state.mappedRows.push({
rowErrors.push([
provenance,
Search
rowStatus
field,
:**
rowld,
record,
helpers,
output,
7):
7):

// OCR PHOTO 133: IMG_7257.HEIC
log(* Processed asset=${assetConfig().label}, sourceRows=$(state.sourceRows.length),
Manual edits: <b>${manualCount)</b>. Selected export rows: <b>$(selectedCountk</b>:
:* Processing completed successfully for <b>$(escapeHtml(assetConfig().label)</b>
<b>${state.exceptions.lengthj</b> exception(s). Output dates are <b>dd/mm/yyyy</b>.
if(btnX) btnX.disabled = state.mappedRows.length === 0 || selectedCount === 0;
if(btnC) btnC.disabled = state.mappedRows.length === 0 || selectedCount =-- 0;
outputRows=$(state.mappedRows.length}, exceptions=$(state.exceptions.lengthy,
<b>dd/mm/yyyy</b>. Manual edits: <b>$(manualCount)</b>. Selected export rows:
? Processing completed for <b>${escapeHtml(assetConfig().label)}</b> with
zm
with <b>${state.mappedRows.length}</b> output row(s). Output dates are
const manualCount = countCurrentSheetManualOverrides();
markCurrentFileProcessed(state.mappedRows.length);
of 241
const selectedCount = countSelectedRows();
132
state.exceptions.length ? "warn" : "success");
state.exceptions.length ? "warn" : "good"
syncGoldenRecordsFromCurrentRun();
const btnX - qs("btnExportX\sx");
const btnC = qs ("btnExportCsv");
pruneExportSelection();
state.exceptions.length
<b>$(selectedCount}</b>. ,
Search
setStatus(

// OCR PHOTO 134: IMG_7258.HEIC
return { rowsLoaded, outputs, validRows, errorRows, issueCount, aggregatedAddons,
const validRows - state.mappedRows.filter(r => r.rowStatus === "Valid").length;
const errorRows = state.mappedRows.filter(r => r.rowStatus =-= "Error*).length;
zm
const aggregatedAddons = state.mappedRows.reduce((sum,r) →> sum +
assetSelect.innerHTML = Object.values(ASSET_CONFIGS).map(cfg=>
if(state.mappedRows.length && !state.processedSelectedRowld)
state.processedSelectedRowld = state.mappedRows[0].rowld;
of 241
customRules:Object.keys(currentFieldRules()).length};
const tradeldModeSelect = qs("tradeldModeSelect");
const addonWindowField = qs("addonWindowField");
const addonWindowDays = qs("addonWindowDays");
const rowsLoaded = state.sourceRows.length;
const runModeSelect = qs("runModeSelect");
133
const outputs = state.mappedRows.length;
const issueCount - state.exceptions.length;
(r.helpers.aggregatedAddonCount || O), O);
const assetSelect = qs("assetSelect");
function refreshSelectOptions(X
function buildSummary(X
if(assetSelect)
Search
renderAIl);

// OCR PHOTO 135: IMG_7259.HEIC
<option value="$(escapeHtml(cfg.key)}" ${cfg.key === state.assetKey ? "selected" :
* <option value="$(escapeHtml(m.value))" $(m.value === state.runMode ? "selected" :
if(!modes.find(m →> m.value === state.runMode)) state.runMode = "trade_by_trade";
if(addonWindowDays) addonWindowDays.value = String(state.addonWindowDays);
assetConfig().supportsAggregation && state.runMode === "aggregate_addons"? ''' :
zm
if(tradeldModeSelect) tradeldModeSelect.value = state.tradeldMode;
if(addonWindowField) addonWindowField.style.display=
of 241
runModeSelect.innerHTML = modes.map(m =>
134
const pillWorkbook = qs("pillWorkbook");
"">$(escapeHtml(cfg.label)/</option>
const modes - availableRunModes();
**/>$(escapeHtml(m.label)</option>
const pillSheet = qs("pillSheet");
const pillAsset = qs("pillAsset");
const pillMode = qs("pillMode");
- +
function updateTopPills(X
refreshSelectOptions();
if(runModeSelect)
Search
)-join(**):
D-join(**):
"none";

// OCR PHOTO 136: IMG_7260.HEIC
pillWorkbook.className = "pill " + (state.fileName ? "green" : "amber");
pillWorkbook.textContent = Workbook: $(state.fileName || "none"}* :
pillSheet.className = "pill" + (state.sheetName ? "green" : "amber");
pillSheet.textContent = * Sheet: $(state.sheetName || "none")* ;
pillAsset.textContent = *Asset: $(assetConfig() label}*;
0t241
pillRule.textContent = "Rutebook: by asset";
135
const pillExceptions = qs("pillExceptions");
const pillSelected = qs("pillSelected");:
const pillTradeld = qs("pillTradeld");
const pillProcess = qs("pillProcess");
const pillManual = qs("pillManual");
const pillGolden = qs(*pillGolden"):
pillAsset.className = "pill blue";
const pillRules = qs("pillRules");
pillRule.className = "pill blue";
const pillFiles = qs("pillFites"):
const pillRule = qs("pillRule");
- +
const pillAi = qs(*pillAi*):
it(pillWorkbook)
Search
if(pillSheet)(
if(pillAsset){
if(pillRule)(

// OCR PHOTO 137: IMG_7261.HEIC
pillMode.textContent = *Mode: $(state.runMode === "trade_by_trade" ? "trade by trade"
state tradeld Mode === "native_ref_preferred" ? "native ref preferred" : "legacy row order";
pillProcess.textContent = * Process: $(state.mappedRows.length ? "done" : "idte"}*;
zm
pillProcess.className = "pill" + (state.mappedRows.length ? "green" : ***);
const label = state tradeldMode === "business_key"? "business key" :
Jelse if(state.aiFormutaSuggestion && state.aiFormulaSuggestion.field)(
6+ 241
lif(pillRules) pillRules.textContent = * Custom Rules:
pillTradeld.textContent = *Trade ID: $(label}' ;
pillAi.textContent = "Al Formula: generating";
136
:aggregate $(state.addonWindowDays)d*}*;
S[Object.keys(currentFieldRules()).length}*;
pillAi.textContent = "Al Formula: review";
pillTradeld.className = "pill blue";
pillMode.className = "pill blue";
pillAi.className = "pill amber";
jelse if(state.aiFormulaError)
pillAi.className = "pill red";
- +
if(state.aiFormulaLoading)
if(pillMode)
lif(pillTradeld)
if(pillProcess)(
Search
if(pillAiX

// OCR PHOTO 138: IMG_7262.HEIC
if(pillGolden) pillGolden.textContent = * Golden Trades: $(state.goldenRecords.length}* ;
pillSelected.className = "pill" + (selected === 0 && total ? "red" : selected !== total &&
if(qs("pillDateFmt")) qs("pillDateFmt").textContent = "Date format: dd/mm/yyyy";
pillExceptions.className = "pill " + (state.exceptions.length ? "red" : "green");
if(pillFiles) pillFiles.textContent =* Inserted Files: $(state.fileHistory.length}* ;
pillAi.textContent = * Al Formula: $(state.aiFormulaSuggestion.field}*;
pillExceptions.textContent = Exceptions: $(state.exceptions.length};
pillManual.className = "pill" + (manualCount ? "amber" : "blue");
const manualCount = countCurrentSheetManualOverrides(;
pillSelected.textContent =* Export Rows: $(selected)/$(total}*:
pillManual.textContent = * Manual Edits: $(manualCount)*;
of 241
137
pillAi.textContent = "Al Formula: idle";
const selected = countSelectedRows);
const total = state.mappedRows.length;
pillAi.className = "pill green";
pillAi.className = "pill purple";
- +
total ? "amber" : "blue");
if(pillExceptions)(
if(pillManual)t
if(pillSelected)
Search
Jelsel

// OCR PHOTO 139: IMG_7263.HEIC
if(!rows || !rows.length) return <div class="empty">$(escapeHtml(emptyMsg || "Nothing
if(btnExportXlsx) btnExportXlsx.textContent = * Export XLSX ($(selectedCount))* :
if(btnExportCsv) btnExportCsv.textContent = * Export CSV ($(selectedCount))* :
zm
const thead = <thead><tr>$(cols.map(c => * <th class="$(c.className ||
const val = typeof c.value === "function" ? c.value(r) : r[c.value];
function tableHtml(rows, cols, emptyMsg, extraClass=**Х
'">$(escapeHtml(c.labet)j</th>*).join(**)}</tr>/thead>;
ot 241
setTextSafe("kpiAggAddons", s.aggregatedAddons);
const tbody = rows.map(r =>* <tr>$(cols.map(c => /
const selectedCount = countSelectedRows();
138
const btnExportXlsx = qs(*btnExportXlsx*);
const btn ExportCsv = qs("btnExportCsv");
setTextSafe("kpiRows", s.rowsLoaded);
setTextSafe("kpiErrored", s.errorRows);
setTextSafe("kpilssues", s.issueCount);
setTextSafe("kpiRules", s.customRules);
setTextSafe("kpiOutputs", s.outputs);
setTextSafe("kpiVatid", s.validRows);
- +
consts = buildSummary0:
function updateKpis(X
to show."))</div>;
Search

// OCR PHOTO 140: IMG_7264.HEIC
")))</span>${assetConfig().supportsAggregation ?* | Add-on window: <span class="inline-
code">$(escapeHtml(state.tradeldMode)J</span> | Date format: <span class="inline-
W
zm
class="$(extraClass)">$(thead)<tbody>${tbody)</tbody></table></div>;
state.mappedRows.filter(r => TARGET_HEADERS.some(h =>
const html = c.html ? c.html(val, г) : escapeHtml(val ?? ''');
return* <td class="$(c.className || "']">$(html}</td>*;
const filter = canonicalHeader(state.previewFilter || '');
<div class="section-title">Processing Summary</div>
of 241
<div class="note">Asset: <span class="inline-
code">$(escapeHtml(state.runMode.replace(/_/g,"
139
canonicalHeader(r.output[h]).includes(filter)));
return* <div class="table-wrap"><table
const overview = qs("overview");
<section class="card section">
code">dd/mm/yyyy</span></div>
7)-join (*')J</tr>").join(*'');
function renderOverview(X
const previewRows = Ifilter
? state.mappedRows
overview.innerHTML =*
<div class="toolbar">
if(loverview) return;
Q Search

// OCR PHOTO 141: IMG_7265.HEIC
4b>Current sheet:</b> <span class="inline-code">${escapeHtml(state.sheetName
<div><div id="summaryChart" class="chart-box"></div></div>
<b>Manual processed-sheet edits:</b> <span class="inline-
code'>$|countCurrentSheetManualOverrides()}</span><br />
<b>Persisted golden records:</b> <span class="inline-
<div class="section-title">Mapped Output Preview</div>
code">$(escapeHtml(assetConfig().label)j</span><br />
<b>Selected export rows:</b> <span class="inline-
<b>Persisted file history:</b> <span class="inline-
of 241
<b>Current asset:</b> <span class="inline-
cb>Source rows:</b> <span class="inline-
code*>$(state.sourceRows.length}</span><br />
<b>Output rows:</b> <span class="inline-
code*>$[state.mappedRows.lengthj</span><br/>
code">$[state.goldenRecords.lengthj</span><br/>
140
code*>$[countSelectedRows()}</span><br />
code">$/state.fileHistory.length}</span>
<section class="card section">
- +
<div class="grid grid-2">
<div class="good-note">
<div class="toolbar">
<div class="search">
l none*)j</span><br
Search
</section>
</div>
</div>
4/div>

// OCR PHOTO 142: IMG_7266.HEIC
{ label:"Add-ons", value:(r)=>r.helpers.aggregatedAddonCount, className:*num
{ label:"Primary CCY", value:(r)=>r.output["*Primary CCY"], className:"nowrap" },
( label:"Primary Amount", value:(r)=>r.output["Primary Amount"], className:"num
html:(v)=> v === "Selected" ? badge("Selected",'green") : badge("Excluded","amber"),
{ label:"Source Row", value:(r)=>r.record.baseRow.sourceRow, className:"num
{ label:"VA/GNBV", value:(r)=>r.output["*$ VA/GNBV"], className:"num nowrap",
{ label:"Trade ID", value:(r)=>r.output["*Trade ID"], className:"mono nowrap" ),
{ label:"Trade Date", value:(r)=>r.output["*Trade Date"], className:"nowrap" },
{ label: "Treats", value:(r)=>r.output["*Treats Acronym"], className:"nowrap" },
{ label:"Price", value:(r)=>r.output("Price"], className:"num nowrap", html:(v)=>
( label: "Export", value:(r)=>isRowSelected(r.rowld) ? "Selected" : "Excluded",
{ label:"Anchor Type", value:(r)=>r.helpers.anchorType, html:(v)=>badge(v,
nowrap", html:(v)=> typeof v === "number" ? prettyNumber(v,0) : escapeHtml(v) },
zm
J, "No mapped rows yet. Upload a workbook and process a sheet first.")}
{ tabel:"Asset", value:()=>assetConfig().label, className:"nowrap" },
{ label:"Status", value:(r)=>r.rowStatus, html:(V)=>statusBadge(v).
html:(v)=> typeof v === "number" ? prettyNumber(v,0) : escapeHtml(v) },
v.includes("add-ons") ? "purple" : "blue"), className:"nowrap" }.
typeofv === "number" ? prettyNumber(v,2) : escapeHtm((v) }
0f241
placeholder="Filter preview by any mapped value" />
141
$(tableHtml(previewRows.slice(0,120), [
- +
className:"nowrap" },
className:"nowrap" },
Search
</div>
</section>
</div>
nowrap* }.
nowrap" }.

// OCR PHOTO 143: IMG_7267.HEIC
title:( text:"Run outcome", style:( fontSize:"16px", fontWeight:"700", color:"#111" }},
window._HC_READY_MAP = window._HC_READY_MAP|I0;
window._HC_READY_MAP.summaryChart = true;
6t 241
142
const search - qs("previewSearch");
state.previewFilter - e.target.value;
Highcharts.chart("summaryChart", {
animation::SNAPSHOT_MODE,
- +
backgroundColor:"#ffffff",
consts - buildSummary();
search.oninput - e →>
renderOverview);
type:"column",
load:function(X
Search
if(searchX
chart: {
events:{
tryl

// OCR PHOTO 144: IMG_7268.HEIC
subtitle:( text: " S(assetConfig().label) | $(state.runMode —= "trade_by_trade" ? 'Trade
yAxis:{ title:( text:"Count" }, min:0, allowDecimals:false, gridLineColor:"#ededed" }.
plotOptions:{ series:( animation:|SNAPSHOT_MODE, borderRadius:6 } }.
categories:["Output Rows","Valid", 'Errored"," Aggregated Add-ons"],
labels:{ style:( color:"#333", fontSize:"12px" ]}
const mode - currentRule(field),mode || "defautt*;
{ y:s.aggregatedAddons, color:"#6d28d9" }
143
{ y:s.validRows, color:"#027a48" ],
{y:s.errorRows, color:"#b42318" },
{y:s.outputs, color:"#175cd3" },
function renderModeSelect(field)(
-+
legend:{ enabled:false}.
credits:( enabled:false },
lineColor:"#ddd",
tickColor:"#ddd",
color:"#666* }}.
Q Search
xAxis:K
series:[
data:[
Jcatch(e)0

// OCR PHOTO 145: IMG_7269.HEIC
return * «select class="rule-mode" data-field="$(escapeHtmt(field))">${options.map(m
$frule.sourceKey === "raw:" + h ? "selected" : **]>$(escapeHtmt(h)}</option>*).join(**)}
return * <input type="text" class="rute-constant" data-field="$(escapeHtmt(fietd)}"
$(state.sheetHeaders.map(h => * <option value="$(escapeHtml("raw:" + h)}"
value="$(escapeHtm!(rule.constant || **)}" placeholder="Enter constant value" />*;
field="$(escapeHtml(field)}" value="$(escapeHtml(rute.expr || **)}" placeholder=*e.g.
zm
const options = ['"defautt""source","constant",'formula","blank"];
<select class="rute-source" data-field="$(escapeHtm!(field)}">
return " <input type="text" class="rule-formuta mono" data-
coption value=**3-- choose source column -c/option>
return' <span class="inline-code'>forced blank</span>:
Ot 241
=> ' <option value="$(m)" $(m === mode? "selected" :
144
**>$(m)</option>*).join(**)}</select>*;
function renderConfigEditor(fietd)[
.const rule - currentRule(field):
if(rule.mode --= "source"X
if(rute.mode ==- *constant"X
if(rule.mode === "formula"X
if(rule.mode === *blank'X
src.raw['Currency'" />*;
Search
</select>
return

// OCR PHOTO 146: IMG_7270.HEIC
return«span class="inline-code">$[meta.sources.length ? meta.sources.join(" + *) =
const suggestion - state.aiFormulaSuggestion && state.aiFormulaSuggestion.field ===
selected asset, current headers, run mode, Trade ID mode, and live sample rows.</div>
const formulaText = trimFormulaText(state.aiFormulaEditable || suggestion.formula ||
const previewRows = validation.ok ? previewFormulaForField(state.aiFormutaField,
height:240px">$(escapeHtml(state.aiFormulaRawResponse || "Waiting for streamed
<div class="good-note"›cb>Generating formuta...</b> The copilot is using the
zm
<div id="aiStreamBox" class="log-box" style=*margin-top:12px;max-
DIID
const validation = validateFormulaExpression(formulaText);
ot 241
state.aiFormulaField ? state.aiFormulaSuggestion : null;
const hasContext = state.sourceRows.length > 0;
const assumptions = suggestion.assumptions || D:
145
const meta = defaultMappingMeta(field);
function renderAiSuggestionPanel(l
if(state.aiFormulaLoading)
meta.type]</span>*;
response...=)I</div>
if(suggestion)
formulatext) : U]:
Search
return*
***);

// OCR PHOTO 147: IMG_7271.HEIC
expression","green")} : &nbsp; | &nbsp: <b>Syntax:</b> $(badge("Needs edit","'red")}* }
zm
<b >Confidence:</b> $(confidenceBadge(suggestion.confidence || *n/a")}
<b>Date standard:</b> <span class="inline-code">dd/mm/yyyy</span>
$(validation.ok ? * &nbsp: | &nbsp: <b>Syntax:</b> $[badge(*Vatid JS
$(state.aiFormulaError ? <div class="warn-note" style=*margin-
<textarea id="aiSuggestedFormula" class="mono" style="min-
<div class="$(validation.ok ? "good-note" = "warn-note"]">
top:12px">${escapeHtmt(state.aiFormulaError))</div>* = **}
01 241
height:110px*>$(escapeHtml(formutaText)j</textarea>
<div class="grid grid-2" style="margin-top:12px*>
code*>$(escapeHtml(assetConfig().label)j</span>
<b>Suggested field:</b> «span class=*inline-
146
const warnings = suggestion.warnings i|D:
code">$(escapeHtml(suggestion.field)j</span>
<div class="field* style="margin-top:12px">
<label>Editable formula expression</label>
<b>Asset:</b> <span class=linline-
- +
&nbsp; | &nbsp;
&nbsp: | &nbsp:
&nbsp: | &nbsp;
<div class="note"*
Search
retun*
s/div>
</div>

// OCR PHOTO 148: IMG_7272.HEIC
zm
$/assumptions.tength?*<ut class="plain-list">$(assumptions.map(x=>
$(escapeHtml(suggestion explanation ]| *No explanation returned.")}
$(warnings.length? " <ut class-"plain-list">$(warnings.map(x =>
$(escapeHtml(currentRuleSummary(state.aiFormulaField))}
•<li>$(escapeHtml(x)/</li>*).join(***)}</ul>* : *<divstyle="margin-
• <li>$(escapeHtml(x)]</li>* )-join(**)}</ut>* : * <div style="margin-
<dív class="section-title'>Preview on first records«/div»
of 241
<div class="grid grid-2" style="margin-top:12px">
$/validation.ok ? tableHtml(previewRows, I
147
top:8px;color:#667085">None stated.</div>* }
top:8px;color:#667085*>None stated.</div> }
<b>Ourrent field baselines/bbcbr/
<b>Explanatione/b>cbr />
<div style="margin-top:12px*>
<b>Assumptions</b>
<div class=inote">
cdv class=*note»
cdiv class-"note">
<b>Warnings</b>
Search
</divo
</div>
</div>
</divp
</divp
</divo

// OCR PHOTO 149: IMG_7273.HEIC
{ label:"Current Value", value:"currentValue", className:"nowrap", html:(v)=> typeof
*<span class="badge red">Error</span> $(escapeHtml(r.error)}* : (typeofv === "number" ?
]. 'No preview rows availabte.") :* <div class-"bad-note">Preview is unavailable untit
$(state.aiFormulaRawResponse ? • <div class="log-box" style="margin-top:12px;max-
( label:"Preview Result", value:"result", className:"nowrap", html:(v,r)= r.error ?
the edited expression is valid JavaScript. $(escapeHtml(validation.error)J</div>* }
<summary style='cursor:pointer;font-weight:600*>Raw AIJSON</summary>
zm
( labet:"Source Row", value: "sourceRow", className:"num nowrap" },
{ label:"Anchor Type", vatue:"anchorType", className:"nowrap" },
height:240px">$(escapeHtm!(state.aiFormulaRawResponse || **)}</div>
height:240px">$(escapeHtmt(state.aiFormulaRawResponse)}</div>* ***}
QIID
<div class=*bad-note*»«b>Al Formula Copilot error: </b>
V=== "number" P prettyNumber(v,2) : escapeHtmt(v || **) }.
<div class="log-box" style=*margin-top:1 0px;max-
148
$(escapeHtml(state.aiFormulaError)j</div>
prettyNumber(v,2) : escapeHtml(v || *)) }
- +E
<details style='margin-top: 12px">
if(state.aiFormulaError)l
</details)
Search
</div>
return

// OCR PHOTO 150: IMG_7274.HEIC
?* Describe the field rute you want for <b>$(escapeHtml(assetConfig().label)}</b>,
if(TARGET_HEADERS.includes(state.aiFormutaField)) state.aiFormulaField - "Maturity
:Process a worksheet first so the copilot can see real headers and sample rows
zm
<div class="ms-chip (hs-chip" data-key="$(escapeHtml(node.key)}">
<span class="ms-chip-name">$(escapeHtml(node.label))</span>
<div class=*ms-chip-sub*>$(escapeHtml(node.sub)}</div>
of 247
const rhsHtml = TARGET_ HEADERS.map(fietd =>*
149
then generate a tailored formula suggestion."
const IhsNodes - getBlotterFieldNodes):
const IhsHtml = lhsNodes.map(node => "
<span class=*ms-dot source"›c/span>
function renderMappingStudio(K
<div class-ms-chip-row*}
before drefting a formuta.* }
<div class="empty">
$thasContext
Search
return"
*).join(**):
c/div>
</div>
</div>
Date",

// OCR PHOTO 151: IMG_7277.HEIC
name">${escapeHtml(field.replace(/^\*/,*')}$(field.startsWith("**)? '<span class="ms-
<div class="ms-chip-sub">$(escapeHtml(shorten(currentRuteSummary(field),
<td class="nowrap">$(escapeHtml(field)} ${MANDATORY_FIELDS.has(field) ?
<button class="btn ghost btn-ai-field" data-field="$(escapeHtm!(field))">Use
zm
<td>$(escapeHtml(shorten(currentRuleSummary(field), 130))}</td>
<div class="ms-chip rhs-chip* data-field="$(escapeHtml(field)}">
<td>$(escapeHtml(defaultMappingMeta(field).summary)}</td>
badge("Mandatory","red") : badge("Optional","blue"))</td>
<td class-"nowrap">$(renderModeSelect(field)j</td>
ot 241
const fieldRows = TARGET_HEADERS.map(field →> -
<td class="num nowrap">$(fillRate(field)}%</td>
<button class="btn ghost btn-reset-field" data-
150
field="$(escapeHtml(field)*>Reset</button>
<span class="ms-dot rule">c/span>
«td>$[renderConfigEditor(field)</td>
<div class=*ms-chip-row">
<span class="ms-chip-
star">*</span>*= **J</span>
$(currentRuleTag(field)}
<div class-action-row*>
<td class-"nowrap">
Search
</div>
110))]</div>
join');
</div>
Al</button>
‹tr

// OCR PHOTO 152: IMG_7278.HEIC
$(TARGET_HEADERS.map(field => * <option value="S(escapeHtmt(fietd)]" ${field
=== state.aiFormulaField ? "selected" - **>$(escapeHtm!(fietd))</option> ") join('*)}
zm
<a href="${escapeHtm(BASE_LOGIC_URL)Y" target="_blank"
<section id="aiFormulaCopilotCard* class-"card section">
aX 2Am
<div class="section-title">Al Formula Copilot</div>
const mappingStudio - qs("mappingStudio");
151
rel="noopener">current shared board</a>
mappingStudio.innerHTML = ~
<label>Target field</label>
<select id=*aiFieldSelect>
if(ImappingStudio) return;
<div class="field tight">
<div class="toolbar">
<div class="controls">
<div class="note">
Base board logic:
</select>
Search
</div>
').join(*');
</td>
c/div>
</div>
</div»
</tm

// OCR PHOTO 153: IMG_7279.HEIC
<textarea id="aiPrompt" placeholder='Describe the exact rute, fallback order,
1. Dates output as <span class=*intine-code*>dd/mm/yyyy</span><br />
zm
<span class=*inline-code'>formatOutputDateFromParts</span>
TDIC
PENDING.*>$(escapeHtml(state.aiFormulaPrompt)j</textarea>
<span class="intine-code">mapKnownlssuerText</span>,
<span class=*intine-code">normalizePricePoints</span>,
<span class="inline-code">src.raw['Header']</span>,
<span class="intine-code">deriveMaturitySafe</span>,
<b>Runtime available inside the formula</b><br />
< span class="intine-code">parseNumber</span>,
<span class="intine-code*>routePbByBook</span>,
of 241
<div class="grid grid-2" style="margin-top:12px*>
<span class="inline-code">cleanText</span>,
«label>Prompt to tailor the formula</label>
<span class=*inline-code">record</span>,
<span class="inline-code">helpers</span>,
152
<span class="inline-code">sro</span>,
<b>Board defaults enforced</b><br /»
exception handling, and when to return
<div class=*field wide"*
<div class-"blue-note"»
<div class="note"»
Search
</div>
</div>
</div>

// OCR PHOTO 154: IMG_7280.HEIC
2. Uncertain values should return <span class=*intine-code">PENDING</span›<br
$(trimFormulaText(state.aiFormulaEditable || **).length ? **: "disabled"]>Re-test edited
<button id="btnClearAiSuggestion" class="btn ghost">Clear suggestion</button>
W
<button id="btnAskAiFormuta* class="btn" $(state.sourceRows.length ? ** :
<b>Current baseline for $(escapeHtml(state.aiFormutaField)} in
$(escapeHtm!(currentRuleSummary(state.aiFormulaField))}
<button id="btnRetestAiFormula" class="btn secondary"
<button id="btnApplyAiFormula" class="btn secondary"
4. PB routing still depends on resolved input Book
юxt 241
<div class="action-row" style=*margin-top:12px'>
<div class="note" style="margin-top:12px">
"disabled")>Generate tailored formula</button»
153
"disabled"}>Apply to selected field</button>
$(escapeHtml(assetConfig().label)}:</b>
<div style="margin-top:12px*>
$(renderAiSuggestionPanelQF
formula</button>
Search
</div>
e/diva
</div>
</div>
</div>

// OCR PHOTO 155: IMG_7281.HEIC
<div class="ms-chip-list" id="msLhsList">$(lhsHitmt || * <div class="empty">Process
<div class=note">LHS = actual blotter columns. RHS = central output template
fields. Dashed grey = default mapping. Solid red = custom remap for the current
<div class=*ms-col-head*>cspan class=*ms-col-title">Centralised output
<div class="ms-col-head">sspan class="ms-col-title*>Original blotter
<div class="ms-chip-list* id="msRhsList">$(rhsHtml}</div>
a sheet to populate the original blotter fields.</div>* j/div>
34241
<div class=*section-title">Mapping Diagram</div»
154
<div class=*ms-canvas" id=*msCanvas>
<div class="ms-canvas-mid">c/divp
<div class-"ms-canvas-colrhs*
<section class="card section"y
<div class-"ms-canvas-col*>
<section class="card section'>
- +
<svg id="msSvg"></svg>
<div class="toolbar"»
fields</span></div>
fields</span></div>
</section>
Search
</section>
asset.</div>
</div>
c/div>
</div>
</div>

// OCR PHOTO 156: IMG_7282.HEIC
<div class="note">Rules are stored per asset. Use mapping rules for systematic logic
zm
changes. Use the Processed Sheet tab for one-off row or cell edits.</div>
if(state.activeTab === "mappingStudio") setTimeout(drawDiagram, 60);
<thead><tr></tr></thead><tbody><tr></tr></tbody>,
<div class="section-title">Field Configuration</div>
of 241
${tableHtml([(], 0, '', "wrap-table").replace(
155
<th class="nowrap">Target field</th>
<th class="nowrap">Mode</th>
<th class="nowrap">Fill rate</th>
<th class="nowrap">Action</th>
<tbody>$(fieldRows)</tbody>
<th>Current mapping</th>
<th> Configuration</th>
bindMappingStudioEvents();
<th>Default rule</th>
<div class="toolbar">
bindAiCopilotEvents();
<thead>
Search
</thead>
</section>
</div>
str>
</tr>

// OCR PHOTO 157: IMG_7283.HEIC
getManualOverrideValue(selectedRow.rowld, selectedFietd) !== undefined : false;
const someFilteredSelected = filteredRows.some(r => isRowSelected(r.rowld));
const allFiiteredSelected = filteredRows.length > 0 && filteredRows.every(r =>
if(ITARGET_HEADERS.includes(state.processedSelectedField)X
if(filteredRows.length && !filteredRows.find(r => r.rowld ——-
state.processedSelectedRowld = filteredRows[0].rowld;
181241.
const selectedField - state.processedSelectedField;
const filteredRows = getFilteredProcessedRows():
const processedSheet = qs(*processedSheet"):
state.processedSelectedField = "*Trade Date";
const manualOverrideExists = selectedRow ?
156
function renderProcessedSheetX
state.processedSelectedRowid)X
- +
it(JprocessedSheet) return;
]| filteredRows[0] || null;
isRowSelected(r.rowld)):
const tableHead -
Search
<thead>
<tro

// OCR PHOTO 158: IMG_7284.HEIC
<td class="select-col">cinput class-"processed-row-select" type="checkbox" data-
<td class="trade-col mono nowrap">$(escapeHtm!(r.output["*Trade ID"] ?? **)}</td>
sth class="select-col"»cinput id="processedSelectAllFiltered" type="checkbox"
W
$(allFilteredSelected? "checked" : **} $!filteredRows.length ? "disabled* : **}></th>
const cellSelected = r.rowid --- state.processedSelectedRowid && field ===
const cls = ['editable-cell', cel(Selected? "selected* : **, menual ? "manual" :
rowid="$(escapeHtml(r.rowld)}* $[isRowSelected(r.rowld) ? "checked" : ***></td>
str class="$fisRowSelected(r.rowld) ? ** = "processed-row-excluded"j">
const manual = getManualOverrideValue(r.rowld, field) |== undefined;
10IC
const vatueText = renderValueForDisplay(field, routput|field]):
nowrap'>$(escapeHtmt(r.record.baseRow.sourceRow)]</td>
<td class="nowrap">$(statusBadge(r.rowStatus)/</td>
D1 241
sth class='source-col nowrap">Source Rowc/th>
<th class="trade-col nowrap">Trade ID</th>
class="nowrap">$(escapeHtmt(h))</th>*).join(**»}
137
const tableBody = filteredRows.length ?
S(TARGET_HEADERS.map(h=> <th
$(TARGET_HEADERS.map(field => [
<th class="nowrap">Status</th>
citd class=source-col num
- +
SffilteredRows.map(r=>
state.processedSelectedField;
""J.filter(Boolean)-join(* *Y
Search
</thead>
<tbody>
</tr)

// OCR PHOTO 159: IMG_7285.HEIC
consttitle - manual ? "Manual override active. Click to edit in place.*: *Click to edit
field="$(escapeHtm!(field)]* title=*$(escapeHtmt(title)}*>$(escapeHtmt(vatueText)j</td>*:
<div class="note">Click any processed cell below to edit it directly in place. Manual
return * <td class="$[cls]" data-rowid="$(escapeHtml(r.rowld))" data-
edits update the mapped output and exports without changing the uploaded
code">$(escapeHtml(state.sheetName || *none")}</span><br /
<div class-*section-title'>Processed Sheet Editor</div>
o1 241
code">$(escapeHtml(assetConfig().tabel)j</span>cbr />
<b>Current worksheet:‹/b> espan class=*inline-
<b>Processed rows:</b> «span class=*inline-
<b>Ourrent asset: </b> cspan class-*inline-
code">$(state.mappedRows.lengthj</span>cbr />
processedSheet.innerHTML = *
ssection class-"card section">
- +
<div class="grid grid-2">
<div class="good-note">
<div class-"toolbar*>
))-join(*)}
join(***)
workbook.</divz
Search
- </tbody>
in place.";
</tr
</div>
=***

// OCR PHOTO 160: IMG_7286.HEIC
<b>Fittered rows:</b> cspan class='inline-code*>$(filteredRows.lengthJ</span><br
<b>Date output standard:</b> <span class-*inline-code">dd/mm/yyyy</span>
5. Use <b>Enter</b> to save and move down, <b>Tab</b> to save and move right,
$(manualOverrideExists ? **: *disabled'>Clear selected cell manual edit</button>
code">$(countSelectedRows))V$(state.mappedRows.length]</span><br />
3. Use <span class=*inline-code">Keep top N filtered</span> or <span
<b>Manual cell edits on current sheet:</b> <span class-*inline-
LOIC
<button id="btnClearSelectedCellEdit' class="btn secondary"
code*>S(countCurrentSheetManualOverridesJ</span><br />
4. Click any processed cell to edit directly in place<br />
01 241
<b>Selected for export:‹/b> «span class-*inline-
class="inline-code*>Keep bottom N filtered</span>cbr />
<div class="section-title">Direct In-Cell Editing</div>
2. Select exactly which rows should export<br (>
<b>Shift + Tab</b> to move left, <b>Esc</b> to cancel
1. Flter the processed sheet if needed«br/
150
< section class= card section *
cb>How to use</b>cbr />
<div class='action-row*>
<div class="note">
<div class="toolbar" =
Search
C/section)
</div»
4/div>
</div>

// OCR PHOTO 161: IMG_7287.HEIC
<button id="btnClearAllManualEdits" class="btn ghost" $(state.mapped Rows.length
<span id="processedSelectionStatus">$(manualOverrideExists ? • $(badge("Manual
<b>Enterc/b> saves the current cell and moves down in the same column.<br />
<span id="processedSelectionInfo">No cell selected yet.</span><br /><br />
override active","amber")} : "$(badge("No manual override","blue")}* }</span>
<b>Shift + Tab</b> saves the current celland moves left.<br /›
?''': "disabled">Clear all manual edits on current sheet</button>
<b>Tab</b> saves the current cell and moves right sbr />
<div class="section-title*>Row Selection for Export</div»
0: 241
1ec
<b>Esc</b> cancels the current edit.
<b>Keyboard behaviour</b>cbr />
<b>Override status</b>ebr />
<b>Selected cell</b><br />
<section class="card section">
- +
<div class-"grid grid-2">
cdiv class='blue-note"›
<div class-"note">
<div class=*toolbar*>
<div class-searcht»
Search
</section>
</divz
4/div>
</div>
c/divp
</div>

// OCR PHOTO 162: IMG_7288.HEIC
value="$(escapeHtm!(state.processedSearch)]* placeholder="Fitter processed sheet" />
cbutton id-"btnSelectAllFittered" class-"btn secondary" $(filteredRows.length ? ** :
<button id="btnKeepTopFiltered" class-"btn secondary" $(filteredRows.length? '™* :
«button id="btnKeepBottomFiltered" class="btn secondary" S{filteredRows.length ?
<button id="btnSelectAllRows" class="btn ghost" $(state.mappedRows.length ?** :
<button id="btnClearFiltered" class="btn secondary" $(filteredRows.length ? "** :
<button id="btninvertFiltered" class="btn secondary" $[filteredRows.length ? ** :
<input id="processedQuickSelectCount' type="number" min="1" step="1"
zm
TOID
value="$(escapeHtml(state.processedQuickSelectCount)}*/>
0l 241
sinput id="processedSheetSearch"type="text"
*: "disabled"}>Keep bottom N filtered</button>
161
<div class="note" style=*margin-top:12px*
"disabled")>Select all filtered</button>
*disabled">Keep top N filtered</button>
"disabled")>Reset row selection</button>
<b>Current filtered selection,</b>
<label>Quick select N</label>
"disabled"}>Clear filtered</button>
*disabled"}>Invert filtered</button>
- +
cdiv class="field narrow"»
<div class=*controls'>
Search
</div>
</div>
c/div>
</divz

// OCR PHOTO 163: IMG_7289.HEIC
: '<div class="empty"»$(state.mappedRows.length ? "No processed rows match the
someFilteredSelected ? badge("Partially selected",'amber*) : badge(*No filtered rows
zm
$(allFitteredSelected ? badge('All filtered rows selected", 'green*) :
current filter." : "No processed output yet. Process a sheet first."}</div>* }
TO1
<div class=section-title">Processed Output Sheet</div>
?" <div class="table-wrap"><table class="processed-
of 241
<b>Export buttons use selected rows only.«/b>
${badge("Excluded from export row",'amber" )}
$[badge("Selected for export row"""green")}
162
$[badge("Manual override cell',"amber")}
table">$(tableHead|$(tableBody)</table></div>
$[badge("Selected cell'",'red")}
<section class=card section">
- +
<div class-"toolbar"›
<div class-"note">
SffilteredRows.length
&nbsp; | anbsp:
<b>Legend:</b>
selected"""red"))
</section>
Search
</section>
</div
</div>
</div>

// OCR PHOTO 164: IMG_7290.HEIC
if(rule made == "source" && rule sourcekey && rute.sourceKey.startsWith("raw:"))X
zm
TOID
return M S(x1] Siyt) C Sfxt + dx) $(y1), $(x2 - dx) $(y2), $(x2) $(y2}*;
iffrute mode === "default' iltrule.mode || rute.mode === **X
return (lind "user", souroes:|rule.sourceKey.slice(4)] }:
h N41
const dx = Mtath.max 40, Matn.abs(x2 -x1) * 0.45);
veturn (kind "detaulr", sources meta sources ):
oonst mete - detauttMappingMeta(field):
comstr = chip.getbaundingObentReut):
const lr = listEl. getBoundineCtientRect):
function chipvisibieinList.chip, istEl
retum cy >= lutap &8 py c= a boctam:
function getConnectiorSpec(field)
bindProoessedSheetEvemsl
const rule = currentRule (field)
return (kind "none", soures:0 -
function bezierPath(x1.y1, *2. 32X
const cy = utop + theight /21
Search

// OCR PHOTO 165: IMG_7291.HEIC
if(!canvas || 1svg || !IhsList || IrhsList || state.activeTab !=- "mappingStudio") return;
qsa("#msLhsList_ths-chip").forEach(ch →> thsMap[ch.dataset.key] = ch);
if(1hsChip || IchipVisibleinList(hsChip, IhsList)) return:
const canvasRect = canvas.getBoundingClientRect();
0t 241
qsa("#msRhsList.rhs-chip").forEach(rhsChip => {
if(lchipVisiblelnList(rhsChip, rhsList)) return;
const IhsChip = IhsMap['raw:* + srcHeader]:
const lr = |hsChip.getBoundingClientRect):
const rr = rhsChip-getBoundingClientRect):
160
const spec = getConnectionSpec(field);
spec.sources.forEach(srcHeader =p (
constfield = rhsChip.dataset.field;
const x1 = lr.right - canvasRect.left;
const canvas = qs(*msCanvas"):
const IhsList = qs(*msLhsList):
const rhsList = gs(*msRhsList"):
if(lspec.sources.length) return;
- +
function drawDiagram(X
const svg = qs("msSvg");
const lhsMap = 0:
const paths = 0:
Search

// OCR PHOTO 166: IMG_7292.HEIC
key="$(escapeHtml("raw:" + srcHeader)}" class="$(spec.kind ==- "user" ? "user-line" :
qsa(".lhs-chip").forEach(ch => ch.classList.toggle("hover-linked", !!match.key &&
qsa(".rhs-chip") .forEach(ch => ch.classList.toggle("hover-linked", !!match.field &&
paths.push(" <path d="$(d}" data-field="$(escapeHtm!(field)}" data-
1O1D
const sameField = match.field && p.dataset field === match.field;
0t 241
const y1 = Ir.top + lr.height / 2 - canvasRect.top;
const y2 = rrtop + rrheight / 2 - canvasRect.top:
if(sameField || samekey) p.classList.add(*hot*)=
164
const x2 - rr.left - canvesRect.left;
const d = bezierPath(x1, y1, *2, y2):
function highlightDiagram(match)l
qsa(*#msSvg path").forEach(p => (
svg.innerHTML= paths.join(**):
p.classList.remove("hot","dim*);
ch.dataset.field === match.field):
-+
else p.classList.add("dim"):
ch.dataset.key === match.key)):
"defautt-line"y"></path>*):
Search

// OCR PHOTO 167: IMG_7293.HEIC
qsa(*.ths-chip,.rhs-chip*).forEach(ch => ch.classList.remove("hover-linked"));
zm
const shouldRefreshPrompt - ItrimFormulaText(state.aiFormulaPrompt) |I
state.aiFormulaPrompt = buildAiDefaultPrompt(state.aiFormulaField);
if(state.aiFormulaSuggestion && state.aiFormulaSuggestion.field |==
qsa(*#msSvg path").forEach(p => p.olassList.remove(*hot","dim"));
state.aiFormulaLastAutoPrompt = state.aiFormulaPrompt;
const previousAuto = state.aiFormulaLastAutoPrompt;
о1 2411
const editableFormula - qs("aiSuggestedFormula*):
166
const retestBtn = qs("btnRetestAiFormula*):
const clearBtn - qs("btnClearAiSuggestion"):
const applyBtn = qs(*btnApplyAiFormuta");
const askBtn = qs("btnAskAiFormula");
state.aiFormulaPrompt ==- previousAuto;
state.aiFormulaField - fieldSel.value;
constfieldSet = qs(*aiFieldSelect");
state.aiFormulaSuggestion = null;
function clearDiagramHightightOl
const promptEl = qs(*aiPrompt"):
function bindAiCopilotEvents()l
fieldSel.onchange = () => [
ff(shouldRefreshPrompt)(
state.aiFormulaFieldy
Search
if(fieldSelX

// OCR PHOTO 168: IMG_7294.HEIC
promptEl.onchange = 0 => [ state.aiFormulaPrompt = promptEl.value; saveLocal(): }:
promptEl.oninput = () →> { state.aiFormulaPrompt - promptEt.value: }:
if(askBtn) askBtn.onclick = () => runAiFormutaCopilot()=
101 241
if(edited) state.aiFormulaEditable - edited.value:
if(edited) state.aiFormutaEditable = edited value:
167
const edited - qs(*aiSuggestedFormula*):
const edited = qs("aiSuggestedFormula");
state.aiFormulaRawResponse = **:
state.aiFormulaEditable = *;
- +
state.aiFormulaError = **
renderMappingStudio():
retestBtn.onclick = 0 => (
renderMappingStudio():
applyBtn.onclick = 0=> (
updateTopPills():
saveLocal):
if(promptEl,
if(retestBtn)[
if(applyBtnX
Search

// OCR PHOTO 169: IMG_7295.HEIC
editableFormula.oninput = () => { state.aiFormutaEditable - editableFormula.value; };
log(* Rule mode changed: asset=$(assetConfig().label), $(field) -> $(sel.value)*,
) / [D
setStatus("Cleared the Al formula suggestion panet.", "blue");
rules[field] = [ currentRule(fietd), mode: sel.value };
of 241
const rules = ensureCurrentAssetRuleStore):
168
function bindMappingStudioEvents(K
qsa(".rule-mode").forEach(sel => (
applyAiFormulaSuggestion):
const field = sel.dataset.field;
- +
clearBtn.onclick = 0 => [
renderMappingStudio):
processCurrentSheet(:
clearAiSuggestion):
sel.onchange = 0 => [
if(editableFormula)
saveLocal():
saveLocal();
if(clearBtnX
Search
"info");

// OCR PHOTO 170: IMG_7296.HEIC
log(' Rule constant changed: asset-$(assetConfig() label), $(field) -> $(inpvalue)*,
log(' Rule source changed: asset-S(assetConfig) label), $(field) -* $(selvalue)*,
zm
of 241 ID
rules(field) = (.ourentRule(fieid), sourcekey: sel valun k
rules[field] = ( currentRule(feid), constant inp.value }
const rules = ensureCurrentAstetRuleStore(;
const rules = ensureCurrentAssetRuleStore0:
169
qsa(".rule-constant").forEach(inp »> (
qsa("rule-source") .forEach(sel => (
constfield - seldataset.field:
const field = inp.dateset.field;
processCurrentSheet(;
processCurrentSheet);
sel.onchange = 0 => (
inp.onchange = () => (
saveLooal(:
saveLocal):
Search
V):
"into"):
"info");
)):

// OCR PHOTO 171: IMG_7297.HEIC
log(" Rule formula changed: asset=$(assetConfig() tabel), $(field}*, "info");
log(" Rule reset: asset=$(assetConfig().label}, ${btn.dataset.field}*, "info");
02410
rules[field] = [ currentRule(field), expr: inp.value );
btn.onclick= () => jumpToAiField(btn.dataset.field);
const rules - ensure@urrentAssetRuleStore():
const rules - ensureCurrentAssetRuleStore(:
170
qsa(*.rule-formula").forEach(inp => (
qsa(*btn-reset-field").forEach(btn => (
qsa(".btn-ai-field") .forEach(btn => {
const field = inp.dataset.field;
delete rules[btn.dataset.fietd]:
const thsList = qs(*msthsList"):
processCurrentSheet();
processCurrentSheet();
linp.onchange = () => [
btn.onclick = () ->(
saveLocal():
saveLocal();
Search
3):

// OCR PHOTO 172: IMG_7298.HEIC
it((hsList) IhsList.addEventListener("scroll', () => requestAnimationFrame(drawDiagram),
zm
chip.onmouseenter = () -> highlightDiagram({field:chip.dataset.field });
- chip.onmouseenter = ) => highlightDiagram({ key:chip.dataset.key }):
of241 D|ID
const quickSelectinput = qs("processedQuickSelectCount"):
const clearSelectedBtn = qs(*btnClearSelectedCellEdit"):
const keepBottomFiltered = qs(*btnKeepBottomFltered"*);
requestAnimationFrame(drawDiagram), { passive:true });
chip.onmouseteave = () => clearDiagramHighlight():
chip.onmouseleave = () => clearDiagramHighlight();
if(rhsList) rhsList.addEventListener("scroll", () →
const clearAllBtn = qs(*btnClearAllManualEdits");
const selectAllFiltered = qs(*btnSetectAllFiltered");
const keepTopFiltered = qs(*btnKeepTopFitered*):
171
const search = qs("processedSheetSearch"):
const selectAllRows - qs("btnSelectAllRows"):
const clearFiltered = qs(*btnClearFiltered"):
const invertFiltered = gs("btninvertFiltered"):
function bindProcessedSheetEventsl
const rhsList = qs("msRhsList");
gsa(*lhs-chip*) forEach(chip => (
qsa(*.rhs-chip").forEach(chip →> [
{ passive:true H):
Search
7):

// OCR PHOTO 173: IMG_7299.HEIC
selectAllFilteredCheckbox.indeterminate = |selectALlFilteredCheckbox.checked &&
setStatus( $(selectAllFilteredCheckbox.checked ? "Selected" - "Cleared"] all filtered
state.processedQuickSelectCount = Math.max(1, Number(e.target.value ||5));
zm
const selectAllFilteredCheckbox = qs(*processedSelectAllFiltered*);
02411QID
getFilteredProcessedRows().some(r => isRowSelected(r.rowld)):
selectAllFilteredCheckbox.onclick= e => e.stopPropagation);
setRowsSelected(rows, selectAllFiiteredCheckbox.checked):
selectAllFilteredCheckbox.onchange = () =3 (
172
state.processedSearch = e.target.value;
const rows = getFilteredProcessedRows):
- + 1
quickSelectinput.onchange = e →> (
if(selectAllFitteredCheckboxX
renderProcessedSheet(;
renderProcessedSheet):
search.oninput = e =7 (
if(quickSelectinput)
rows for export. *, "blue"):
updateTopPills():
saveLocal();
Search
if(searchy

// OCR PHOTO 174: IMG_7300.HEIC
setStatus(* Cleared all <b>$(rows.length)</b> filtered rows from export selection.*,
setStatus(* Selected all <b>$(rows.length)</b> filtered rows for export. . "good"):
zm
const n = Math.max(1, Number(state.processedQuickSelectCount || 5));
of241 O 1 ID
173
const rows - getFilteredProcessedRows();
const rows = getFilteredProcessedRows();
const rows - getFilteredProcessedRows):
keepOnlyRows(rows, rows.slice(0, n)):
selectAllFiltered.onctick = 0 =>(
setRowsSelected(rows, true);
setRowsSelected(rows, false);
keepTopFiltered.onclick = 0 => (
clearFiltered.onclick = 0 => [
renderProcessedSheet);
renderProcessedSheet():
renderProcessedSheet);
if(selectAllFilteredX
updateTop Pills0:
updateTopPills):
if(keepTopFiltered)
lif(clearFilteredX
updateTopPills():
Search
"warn"):

// OCR PHOTO 175: IMG_7301.HEIC
setStatus(* Kept only the <b>bottom ${Math.min(n, rows.length)}</b> filtered row(s)
setStatus(* Kept only the <b>top $(Math.min(n, rows.length)}</b> filtered row(s)
setStatus(* Inverted export selection for the current filtered rows.* , "blue");
zm
const n = Math.max(1, Number(state.processedQuickSelectCount || 5)):
of 241 D | [D
keepOnlyRows(rows, rows.slice(Math.max(0, rows.length -n))):
174
const rows - getFilteredProcessedRows();
const rows - getFilteredProcessedRows();
keepBottomFiltered.onclick = 0) => {
invertFiltered.onclick= 0 → {
invertRowsSelection(rows);
selectAllRows.onclick = () => (
clearAllRowSelectionState();
selected for export.*, "good");
renderProcessedSheet);
selected for export.* . "good"):
renderProcessedSheet);
if(keepBottomFiltered)
updateTopPills();
if(invertFilteredX
updateTopPills():
if(selectAllRows)
Search

// OCR PHOTO 176: IMG_7302.HEIC
setStatus( Reset export selection so all current sheet rows are selected.", "good"):
"warn");
setStatus(* Cleared manual edit for <b>$(escapeHtm!(field)}</b> on source row
zm
setStatus('The selected cell does not currently have a manual override.",
log(" Manual processed-sheet edit cleared: asset=$(assetConfig().label},
<b>$fescapeHtmt(selectedRow.record.baseRow.sourceRow)}</b>.* , "blue"):
if(getManualOverrideValue(selectedRow.rowld, field) === undefinedX
row=$(selectedRow.record.baseRow.sourceRow), field=$(fietd) . "info*);
of241 | O| UD
const selectedRow = state.mappedRows.find(r => r.rowld —- -
clearManualOverrideValue(selectedRow.rowld, field);
"warn");
const field - state.processedSelectedField;
175
setStatus("Select a processed cell first.*,
clearSelectedBtn.onclick - 0 => [
setActiveTab("processedSheet"):
state.processedSelectedRowld);
renderProcessedSheet):
Ff(IselectedRow || Ifield)
processCurrentSheet):
if(clearSelectedBtnX
updateTopPills):
Search
return,
return?

// OCR PHOTO 177: IMG_7303.HEIC
log(* Cleared all manual processed-sheet edits for $(assetConfig().label}*, "warn");
setStatus("Cleared all manual processed-sheet edits for the current worksheet.",
", td.dataset.field ||
of241 @|CD
qsa("#processedSheet processed-row-select").forEach(cb => {
td.onclick = () →> openProcessedCellEditor(td.dataset.rowid || **
if(cb.checked) delete state.exportExcludedRows[rowld];
qsa("#processedSheet .editable-cell").forEach(td →> (
else state.exportExcludedRows[rowld] = true;
176
clearCurrentSheetManualOverrides);
cb.onclick = e => e.stopPropagation();
const rowld = cb.dataset.rowid || "™;
setActiveTab("processedSheet");
clearAllBtn.onclick = 0 → (
processCurrentSheet();
renderProcessedSheet);
state.processedSelectedField);
cb.onchange = () →> (
updateTopPills():
if(clearAllBtn)(
saveLocal():
Search
"blue");

// OCR PHOTO 178: IMG_7306.HEIC
code'>PENDING</span> is listed here. Mandatory unresolved fields also keep the row in
], state.mappedRows.length ? "No issues for the current run." : *No processed rows
{ labet:"Source Row", value:"sourceRow", className:"num nowrap" ],
<div class="note">Any field currently output as <span class="inline-
{ label:"Asset", value:()=>assetConfig().label, className:"nowrap" },
{ label:"Trade ID", value:"tradeld", className:"mono nowrap" }.
<div class-"section-title">Issues & Exceptions</div>
{ label:"Field", value:"field", className:"nowrap" }.
оr24t
{ label:"Source Mapping", value:"sourceValue" }
177
updateProcessedSelectionPanel):
const exceptions = qs("exceptions"):
{ label:"Reason", value:"reason" },
<section class=*card sectionty
$itable-itml(state.exceptions, I
- +
function renderExceptions(X
exceptions.innerHTML= *
<div class-"toolbar"y
it(lexceptions) return;
error status.</divp
Search
</section>
</divp
yet.")}

// OCR PHOTO 179: IMG_7307.HEIC
const selected - records.find(r => r.tradeKey ==- state.selectedGoldenKey) || records[0]
class=*value">$(uniqueWorkbookCount)</div><div class="sub">Distinct workbook names
<div class="kpi*»<div class="label*>Latest insert</div><div class="value" style="font-
const errorGoldenCount = records.filter(r => canonicalHeader(r.latestRowStatus) ---
class="value">$(fiteHistoryRows.length)</div><div class="sub">Persisted file insertion
class="value">$(errorGoldenCount)</div><div class="sub">Golden records with latest
class=*value">$(records.length)</div><div class="sub">Unique output trade records
const uniqueWorkbookCount = Array.from(new Set(fiteHistoryRows.map(x =>
formatDateTimeDisplay(fileHistoryRows[O].insertedAt) : *No file yet")</div></div>
zm
size:16px">$(escapeHtmt(fileHistoryRows[0] ? fileHistoryRows[0].fileName :
<div class="kpi"»<div class="tabel*>Errored golden trades</div><div
o241OD
<div class="kpi"> <div class="label">Unigue workbooks</div><div
"None")j</div><div class="sub">$(escapeHtm!(fileHistoryRows[0]?
<div class="kpi"> <div class='label">Inserted files</div><div
<div class="kpi"><div class="label">Golden trades</div><div
String(b.lastSeenAt).localeCompare(String(a.lastSeenAt))):
const fileHistoryRows - [.state.fileHistory].sort((a, b) →
const records - [...state.goldenRecords].sort((a, b) =>
String(b.insertedAt).localeCompare(String(a.insertedAt))):
<div class="grid grid-6* style="margin-bottom:12px"»
178
function renderGoldenRecord(X
x.file Name).filter(Boolean))).length;
const summaryCards =*
error status.</div></div>
persisted.</div></div>
history.</div></div>
seen.</div></div>
"error").length:
Search
II null;

// OCR PHOTO 180: IMG_7308.HEIC
{ label:"Latest Status", value:(r)=>r.latestRowStatus, html: (v)=>statusBadge(v || "Valid"),
style=*font-size:16px">$(escapeHtml(selected ? selected.tradeKey : *None")</div><div
{ label:"Last Processed At", value:(r)=>formatDateTimeDisplay(r.lastProcessedAt),
<div class-"kpi"»<div class="label">Selected trade</div><div class='value"
{ label:"Size", value:(r)=>bytesLabe((r.sizeBytes), className:"nowrap num" },
zm
{ label:"Process Count", value:"processCount", className:"num nowrap" },
( label:"Sheet Count", value:"sheetCount", className:"num nowrap" },
{ label:"Preferred Sheet", value:"preferredSheet", className:"nowrap" },
( label:"Rows", value:"lastProcessedRows", className:"num nowrap" }
{ label:"Trade Date", value:(r)=>r.summary && r.summary.tradeDate || **
selected.summary-salesClient) ||** = *No record selected")}</div></div>
( label:"Inserted At", value:(r)=>formatDateTimeDisplay(r.insertedAt).
{ label:"Asset", value:"lastProcessedAsset", className:"nowrap" },
QID
const fileHistoryTable = tableHtml(fiteHistoryRows.slice(0, 200), [
{ label:"Trade Key", value:"tradeKey", className:"mono nowrap" },
( label:"Asset", value:(r)=>r.latestAsset || '**, className:"nowrap" },
class="sub">$fescapeHtml(selected ? (selected.summary &&
of 241
const goldenTable = tableHtml(records.slice(0, 500), [
179
[ label:"Workbook", value:"fileName" },
]. "No inserted files recorded yet.");
className:"nowrap" },
className:"nowrap* }.
className:"nowrap" },
className:"nowrap" }.
Search
</div>

// OCR PHOTO 181: IMG_7309.HEIC
( label:"ISIN", value:(r)=>r.summary && r.summary.isin || **, className:"mono nowrap"
*, className:"nowrap" }
- { label:"Action", value:(r)=>r.tradeKey, html:(v)=> <button class="btn secondary btn-
<div class-"note">Golden records preserve full logical snapshots and remain cross-
<b>Asset:</b> <span class=*intine-code*>$(escapeHtm!(selected latestAsset ||
{ label:"Primary CCY", value:(r)=>r.summary && r.summary-primaryCcy || **.
{ label: "Sales Client", value:(r)=>r.summary && r.summery salesClient || *** },
zm
( label: "Occurrences", value:"occurrences", className:"num nowrap" },
view-golden" data-key=*$(escapeHtml(v)}*>View record</button>*
of241 @
<div class-"section-title">Selected Golden Record</div>
{ label:"Latest Workbook", value:"latestWorkbook* },
<div class="grid grid-2" style=*margin-bottom:12px">
code"»${escapeHtm!(selected.occurrences)/</span><br />
code">$(escapeHtm!(selected tradeKey)]</span><br />
<b>Occurrences:</b> <span class=lintine-
180
<b>Trade key: </b> <span class="intine-
], *No golden trades recorded yet."):
const detailHitml = selected ?-
csection class="card section"*
<div class="good-note">
<div class-"toolbar">
class Name:"nowrep" }.
'**)}</span><br />
Search
asset.</div>
</div>

// OCR PHOTO 182: IMG_7310.HEIC
<b>Primary CCY:</b> <span class="inline-code">${escapeHtml(selected.summary
<b>Client:</b> <span class="inline-code">$(escapeHtml(selected.summary &&
<b>Trade date:</b> <span class="inline-code">$(escapeHtml(selected.summary
<b>ISIN:</b> <span class="intine-code">$(escapeHtml(selected.summary &&
code*>$/escapeHtmt(formatDateTimeDisplay(selected.firstSeenAt))}</span><br />
code'>$(escapeHtm!(formatDateTimeDisplay(selected.lastSeenAt))|</span><br />
<div class="table-wrap"><table class="wrap-table"><tbody><tr»<td><pre
zm
code*>$(escapeHtml(selected.latestWorkbook || ***}</span><br />
code">$(escapeHtml(selected.latestSheet || **)}</span>
or 241
<b>Latest workbook:</b> «span class=*inline-
&& selected.summary.tradeDate || '*)}</span><br />
&& selected.summary.primaryCcy || ***)}</span><br />
<div class="section-title">Golden Input</div>
<b> Latest sheet:</b> <span class="inline-
selected.summary.salesClient ||**)}</span><br />
<b>First seen: </b> cspan class=*inline-
<b>Last seen:</b> <span class=*inline-
2))|</pre></td></tr>c/tbody>c/table>c/div>
- +E
selected.summary.isin || **)}</span>
style=*margin:O;white-space.pre-
<div class="grid grid-2">
<div class-note">
Search
</div>
s/divz
</div>
<div>
</div>

// OCR PHOTO 183: IMG_7311.HEIC
:"<section class-"card section">cdiv class=*empty">No golden record selected
<button id="btnClearFiteHistory" class="btn ghost">Clear file history</button>
<div class="table-wrap"><table class="wrap-table*><tbody><tr»<td><pre
2m
<button id="btnClearGoldenRecords" class=*btn ghost">Clear golden
TOID
wrap">$(escapeHtmLJSON.stringify(selected.goldenOutput |I0, null,
<div class="section-title">Golden Record Summary</div>
of 241
<div class="section-title">Golden Output</div>
const goldenRecord - qs("goldenRecord"):
2))]</pre></td›</tr></tbody></table></div>
style=*margin:0;white-space:pre-
<section class-"card section">
goldenRecord.innerHTML= *
if(IgoldenRecord) return;
<div class-"controls"»
cdiv class-"toolbar*>
yet.</div>c/section>*:
$(summaryCards)
</section>
record</button>
Search
<div>
</div>
</section>
</div>
</div>
</div>

// OCR PHOTO 184: IMG_7312.HEIC
<div class=*note">One row per unique output trade key, across assets and processed
<div class="note">This is persisted locally in the browser.</div>
<div class-"section-title*>Unique Trade Golden Record</div>
<div class="section-title">inserted File History</div>
of 241
state.selectedGoldenKey = btn.dataset.key || *M:
183
qsa(".btn-view-golden").forEach(btn => (
<section class="card section">
<section class-"card section">
<div class="toolbar>
<div class-"toolbar">
renderGoldenRecord):
$(fileHistoryTable}
btn.onclick = 0 => (
$(goldenTable}
$[detailHtml}
saveLocal():
</section>
</section>
Search
c/div>
files.</div>
</div>

// OCR PHOTO 185: IMG_7313.HEIC
const clearGoldenBtn = qs("btnClearGoldenRecords*):
of 241
const clearFileBtn = qs("btnClearFileHistory");
log("Persisted golden record cleared", "warn");
log("Persisted file history cleared", "warn*);
184
state.currentFileHistoryld = **;
clearGoldenBtn.onclick = () -> {
state.goldenInstancelndex = 0:
state.selectedGoldenKey = **;
- +
clearFileBtn.onclick = 0 => [
state.goldenRecords - 0;
state fileHistory - [J:
if(clearGoldenBtn)(
if(clearFileBtn)
saveLocal);
renderAIL):
saveLocal);
renderAll);
Search
0):

// OCR PHOTO 186: IMG_7314.HEIC
"Trade ID mode is selectable: deterministic business-key, native-ref preferred, or legacy
Unresolved values become PENDING and are raised in Exceptions.",
*Use one centralised finat output template across all assets.",
*PB routing is shared and comes from resolved input Book.*,
mandatory: MANDATORY_FIELDS.has(field) ? "Yes" : "No",
of 241
*Primary Amount replaces the old *$ Volume field.",
defaultRule: defaultMappingMeta(field).summary,
currentMode: currentRule(field).mode || "default',
const rows = TARGET_HEADERS.map(field => (1
currentMapping: currentRuleSummary(field)
185
"Output dates are always dd/mm/yyyy.",
function renderDefaultRules(X
const el qs("defaultRules"):
<section class="card section"»
"Output Book stays blank.",
const sharedRules = [
el.innerHTML= *
if(lel) return;
Search
row-order.*
field,
})):

// OCR PHOTO 187: IMG_7315.HEIC
<b>Date standard:«/b> <span class="intine-code">dd/mm/yyyy</span><br />
zm
$(sharedRules.map(x => <li>$(escapeHtm((x)}</i>*).join(**)}
code">S[escapeHtml(state.runMode.replace(/_/g," *)J</span><br />
code">$(escapeHtml(state.addonWindowDays)j</span><br />
cdiv class=*note">Documented field-by-field logic for
<div class="grid grid-2" style="margin-bottom: 12px">
code">$(escapeHtml(assetConfig().label)}</span><br/>
o+ 241
code*>$(escapeHtml(state.tradeldMode)]</span><br />
<b>Custom rules active:</b> <span class="intine-
code'>$(Object.keys(currentFieldRules().lengthj</span>
«div class-'section-title">Default Rules</div>
<b>$(escapeHtml(assetConfig(.label)}</b>.</div>
<b>Trade ID mode:</b> <span class="inline-
<b>Add-on window:</b> <span class=*inline-
180
<b>Asset:</b> <span class=*inline-
*b>Mode:</b> <span class="inline-
cb>Shared cross-asset rules</b>
<b>Current run settings</b>cbr />
<div class="good-note">
cul class-"plain-list">
cdiv class="blue-note">
<div class="toolbar»
Search
S/div
vul>
</div»
</div>
</div»

// OCR PHOTO 188: IMG_7316.HEIC
{ label:"Mandatory", value:"mandatory", className:"nowrap", html:(v)=> v === *Yes" ?
$(escapeHtml(L.Level)]">[$(escapeHtml(L.time)]] $(escapeHtml(Llevel.toUpperCase()}:
zm
{ label:"Current Mode", value:"currentMode", className:"nowrap" },
<button class="btn secondary" id="btnClearLog">Clear log</button>
k label: "Target Field", value:"field", className:"nowrap" }.
{ label:"Current Mapping", value:"currentMapping" }
of 241
badge("Mandatory","red") : badge("Optionat","blue") }.
? state.logs.map(l=> <div class="log-line log-
{ label:"Default Rute", value:"defaultRute" },
]. "No default rutes avaitable.", *wrap-table*)}
<div class="section-title"> Activity Log</div>
Project-yexuk
const activityLog = qs("activityLog"):
$(escapeHtmt(l.msg))</div>*)-join(**)
<section class-"card section">
function renderActivityLogil
activityLog.innerHTML= *|
S/tableHtml(rows, [
<div class-"toolbar">
if(lactivityLog) return;
<div class="log-box">
$(state.logs.length
</section>
Search
</div>

// OCR PHOTO 189: IMG_7317.HEIC
zm
el.innerHTML= <section class="card section"><div class="bad-
• <div class="log-line log-info">No log entries yet.</div>™
log(* Render failed for $(viewld): $(e.message} , "error");
ot 241
note"><b>${escapeHtm!(viewld))</b> failed to render:
188
$(escapeHtml(e.message)j</div></section>*;
const clearLogBtn = qs("btnClearLog");
function safeRender(viewld, renderFn)
clearLogBtn.onclick = () => {
renderActivityLog();
const el = qs(viewld);
if(clearLogBtn)
state.logs = 0:
</section>
renderFn();
Search
</div>
}catch(e)(
if(ely
tryf

// OCR PHOTO 190: IMG_7318.HEIC
qsa(".tab").forEach(t => t.classList.toggle("active", t.dataset.tab === tab));
of241 Q| [D
qsa(".view").forEach(v=> v.classList.toggle("active", v.id === tab));
names.some(n => canonicalHeader(n) === canonicalHeader(p))
if(tab === "mappingStudio") setTimeout(drawDiagram, 60);
safeRender("processedSheet", renderProcessedSheet);
const preferred = assetConfig().preferredSheets.find(p =>
safeRender("mappingStudio", renderMappingStudio);
safeRender("goldenRecord", renderGoldenRecord);
safeRender("defaultRules", renderDefaultRules);
const names = state.workbook.SheetNames || [];
safeRender("exceptions", renderExceptions);
189
safeRender("activityLog", renderActivityLog);
safeRender("overview", renderOverview);
function autoSelectPreferredSheet(X
setActiveTab(state.activeTab);
function setActiveTab(tab
if(Istate.workbook) return;
function renderALlK
state.activeTab = tab;
updateTopPills();
updateKpis();
Search

// OCR PHOTO 191: IMG_7319.HEIC
const exact = names.find(n →> canonicalHeader(n) --- canonicalHeader(preferred)):
setStatus("Please load a workbook and select a sheet first.", *warn"):
O241 @[D
if(sheetSelect) sheetSelect.value = state.sheetName;
const ws = state.workbook.Sheets[state.sheetName];
190
Jelse if(Inames.includes(state.sheetName)X
const matrix = XLSX.utils.sheet_to_json(ws,1
const sheetSelect = qs("sheetSelect"):
if(lstate.workbook || Istate.sheetName)
if(exact) state.sheetName - exact;
state.sheetName = names[0]|| ***;
function parseSelectedSheet(X
dateNF:"dd/mm/yyyy"*
blankrows:false,
if(preferred)
header:1,
raw:false,
Search
defval:**,
return:
tryt
I):

// OCR PHOTO 192: IMG_7320.HEIC
log(* Parsed sheet "$(state.sheetName]" for asset "$(assetConfig().tabel}" with
state.aiFormutaPrompt = buildAiDefaultPrompt(state.aiFormulaField);
state.sourceRows = extracted.rows.map(buildCanonicalSource);
setStatus(* Parsing failed. $(escapeHtml(e.message)}*, "bad");
of241 =
state.aiFormulaLastAutoPrompt = state.aiFormulaPrompt;
state.headerMap = buildHeaderMap(extracted.headers);
const extracted - extractSheetRows(matrix):
if(ltrimFormulaText(state.aiFormutaPrompt)X
191
rows=$(state.sourceRows.length}* , "success");
state.sheetHeaders = extracted.headers;
log(" Parse failed: $(e.message)*, "error");
const btnX = qs("btnExportX|sx");
async function handleWorkbook(file)
const btnC = qs("btnExportCsv");
if(btnX) btnX.disabled = true;
if(btnC) btnC.disabled = true;
processCurrentSheet();
Search
Jcatch(e)[
tryl

// OCR PHOTO 193: IMG_7321.HEIC
log( Workbook loaded: $(state.fileName); sheets=$(names.length} ,"success");
const workbook = XLSX.read(buffer, { type:"array", cellDates:false }):
of241| A | CD
value="$(escapeHtml(n))">${escapeHtml(n)j</option> *).join(*'*)
if(btnProcess) btnProcess.disabled = Istate.sheetName;
state.currentFileFingerprint = fileFingerprint(file);
:<option value="'>No sheets found</option>* ;
addFileHistoryEntry(file, names, state.sheetName);
192
const names = workbook.SheetNames || D):
const buffer = await file.arrayBuffer();
const sheetSelect = qs("sheetSelect");
sheetSelect.disabled = !names.length;
sheetSelect.innerHTML = names.length
sheetSelect.value = state.sheetName;
const btnProcess = qs("btnProcess");
state.sheetName = names(0] || '*;
state.fileName = file.name || "';
state.workbook - workbook;
autoSelectPreferredSheet();
?names.map(n => <option
state.rawFile = file;
if(sheetSelect)(
Search
renderAll);

// OCR PHOTO 194: IMG_7322.HEIC
WarG:
Agg Worksor Sooe sansoe Sl
ветононие Анак о кев
const selectedkone *pettx
Aab toselee om e 4 4
il(IselectedRows)vetum:
Жогианна зоша
WSaк N0т
сопеннам ретe
Tunction expertOsva
Sonotiongett apo
«Пнонь Двпуті)
МУYY
wonaieenb
Hetchtey
rbar ml
SaaTE
Кhnssi

// OCR PHOTO 195: IMG_7323.HEIC
const base = (state.fileName || "centralised_blotter_mapper").replace(/\.[^.]+$/,**);
a.download = • $(base}_$(state.assetKey}_$(state.runMode)_selected _output.csv;
log( CSV exported: asset=$(assetConfig().label}, rows=$(rows.length}* , "success");
setStatus(* Exported <b>${rows.length)</b> selected row(s) to CSV.*, "good");
const csv = XLSX.utils.sheet_to_csv(XLSX.utils.json_to_sheet(rows.(
"const blob = new Blob([csv), { type:"text/csv;charset=utf-8;* });
TARGET_HEADERS.forEach(h => out[h] = r.output[h]);
const selectedMapped = getExportRowsOrWarn();
194
const rows = selectedRows.map(r => {
const url = URL.createObjectURL(blob);
const a - document.createElement("a");
document.body.appendChild(a);
const wb = XLSX.utils.book_new(;
- +
header:TARGET_HEADERS }));
URL.revokeObjectURL(url);
if(!selectedMapped) return;
function exportXlsx)l
const out = 0:
return out;
a.href = url;
a.remove();
Search
a.click();

// OCR PHOTO 196: IMG_7324.HEIC
"Export Selected": isRowSelected(r.rowld) ? "Yes" : "No"
TARGET_HEADERS.forEach(h => out[h] = r.output[h]);
"Add-on Count": r.helpers.aggregatedAddonCount,
TARGET_HEADERS.forEach(h => out[h] = r.output[h]);
const mappedRows = selectedMapped.map(r => (
const ruleRows = TARGET_HEADERS.map(field →> {
const auditRows = selectedMapped.map(r => (
"Source Row": r.record.baseRow.sourceRow,
195
"Trade ID Mode": state.tradeldMode,
"Trade ID": r.output["*Trade ID"],
- +
"Asset": assetConfig().label,
const rule = currentRule(field):
"Row Status": r.rowStatus,
"Asset": assetConfig().label,
"Mode": state.runMode,
"Row ID": r.rowld,
const out = 0;
return out;
const out = {
Search
return out;
return {
}):
});

// OCR PHOTO 197: IMG_7325.HEIC
selectedMapped.some(r => r.record.baseRow.sourceRow === ex.SourCeRow &&
filter(field => getManualOverrideValue(r.rowld, field) !== undefined)
"Manual Override Value*: getManualOverrideValue(r.rowld, field),
"Mandatory": MANDATORY_FIELDS.has(field) ? "Yes" : "No",
of 241 0
"Default Rule": defaultMappingMeta(field).summary,
const manualEditRows - selectedMapped.flatMap(г »>
const selectedExceptions = state.exceptions.filter(ex=>
"Current Mapping": currentRuleSummary(field),
"Source Row": r.record.baseRow.sourceRow,
String(r.output[**Trade ID*]) === String(ex.tradeld))
196
"Current Mode": rule.mode || "default*,
"Final Output Value": r.output(field]
"Trade ID": r.output("*Trade ID"],
- +
"Asset"; assetConfig()label,
"Fill Rate %"; fillRate(field)
"Target Field": field,
TARGET HEADERS
map(field => (
"Field": field,
)):

// OCR PHOTO 198: IMG_7326.HEIC
XLSX.utils.book_append_sheet(wb, XLSX.utils.json_to_sheet(manualEditRows.length?
manualEditRows : [( "Asset":assetConfig).label, "Source Row":**, "Trade ID":"*, "Field": "No
manual edits in selected export set", "Manual Override Value":**, 'Final Output Value": ** ]]),
setStatus(* Exported <b>$(mappedRows.lengthJ</b> selected row(s) to XLSX.', "good");
"Asset":assetConfig().label, "Source Row":*'' "Trade ID":''' "Field":"No issues in selected
XLSX.utils.book_append_sheet(wb, XLSX.utils.json_to_sheet(ruleRows), "Rulebook");
const base = (state.fileName || "centralised_blotter_mapper").replace(/N.[^-]+$/,**);
log(* XLSX exported: asset=${assetConfig().label}, rows=$(mappedRows.length}*,
XLSX.utils.book_append_sheet(wb, XLSX.utils.json_to_sheet(mappedRows,(
XLSX.utils.book_append_sheet/wb,XLSX.utils.json_to_sheet(exceptionRows),
const exceptionRows - selected Exceptions.length ? selectedExceptions : K
XLSX.utils.book_append_sheet(wb, XLSX.utils.json_to_sheet(auditRows),
*$(base)_$(state.assetKey)_$(state.runMode}_selected_output.xlsx*);
of 241
tab.onclick = () => setActiveTab(tab.dataset.tab);
header:TARGET_HEADERS }), "Mapped_Output");
197
„"Reason":"* "Source Mapping":"* );
function bindStaticEvents(X
qsa(".tab").forEach(tab →> (
XLSX.writeFile(wb,
"Audit_Output");
"Manual_Edits");
"Exceptions"):
Search
export set",
"success");

// OCR PHOTO 199: IMG_7327.HEIC
Worksheet is now <b>${escapeHtml(state.sheetName || "none")}</b>. Click <b>Process
if(!assetConfig().supportsAggregation && state.runMode !== "trade_by_trade")
setStatus(*Asset switched to <b>${escapeHtml(assetConfig().label)J</b>.
if(state.sourceRows.length && state.workbook) parseSelectedSheet();
of241| 0 | CD
filelnput.addEventListener("change", async e => {
selected sheet</b> to refresh the mapping.* , "blue");
const file = e.target.files && e.target.files[O];
assetSelect.addEventListener("change", e => {
198
const assetSelect = qs("assetSelect");
if(file) await handleWorkbook(file);
const filelnput = qs("fileinput");
state.assetKey = e.target.value;
state.runMode = "trade_by_trade";
- +
autoSelectPreferredSheet();
if(state.workbookX
updateTopPills();
if(assetSelect)
else renderAll();
if(filelnput)
saveLocal();
Search

// OCR PHOTO 200: IMG_7328.HEIC
if(state.sourceRows.length && state.runMode === "aggregate_addons")
if(btnProcess) btnProcess.addEventListener("click", parseSelectedSheet);
of 241 • [D
const v = Math.max(0, Math.min(30, Number(e.target.value || 7)));
if(btnExportCsv) btnExportCsv.addEventListener("click", exportCsv);
if(btnExportXlsx) btnExportXlsx.addEventListener("click", exportXlsx);
if(state.sourceRows.length) processCurrentSheet();
const addonWindowDays = qs("addonWindowDays");
addonWindowDays.addEventListener("change", e => {
200
const btnResetRules = qs("btnResetRules");
const btnExportCsv = qs("btnExportCsv");
const btnExportXlsx = qs("btnExportX(sx");
const btnProcess = qs("btnProcess");
state.addonWindowDays = v;
- +
lif(addonWindowDays)(
updateTopPills();
processCurrentSheet();
else renderAll);
updateTopPilts();
saveLocal();
Search
}):

// OCR PHOTO 201: IMG_7329.HEIC
if(state.sourceRows.length && state.runMode === "aggregate_addons")
if(btnProcess) btnProcess.addEventListener("click", parseSelectedSheet);
const v = Math.max(0, Math.min(30, Number(e.target.value || 7))):
if(btnExportCsv) btnExportCsv.addEventListener("click", exportCsv);
if(btnExportXlsx) btnExportXlsx.addEventListener("click", exportXlsx);
of241 a
if(state.sourceRows.length) processCurrentSheet();
const addonWindowDays = qs("addonWindowDays");
addonWindowDays.addEventListener("change", e => (
200
const btnResetRules = qs("btnResetRules");
const btnExportCsv = qs("btnExportCsv");
const btnExportXlsx = qs("btnExportXlsx");
const btnProcess = qs("btnProcess");
state.addonWindowDays = v;
- +
if(addonWindowDays)[
updateTopPills();
processCurrentSheet();
else renderAll();
updateTopPills();
saveLocal();
Search

// OCR PHOTO 202: IMG_7330.HEIC
if(state.activeTab === "mappingStudio") setTimeout(drawDiagram, 50);
state.aiFormulaPrompt = buildAiDefaultPrompt(state.aiFormulaField);
of 241 / [D
if(tb) document.documentElement.style.setProperty("-topbar-h",
log(' All custom rules reset for $(assetConfig().label}*, "warn");
state.aiFormulaLastAutoPrompt = state.aiFormulaPrompt;
state.aiFormulaLastAutoPrompt = state.aiFormulaPrompt;
if(state.sourceRows.length) processCurrentSheet();
btnResetRules.addEventListener("click", ) => {
state.fieldRulesByAsset[state.assetKey)= 0;
const tb = document.querySelector("topbar");
201
if(!trimFormulaText(state.aiFormulaPrompt)X
window.add EventListener("resize", () => {
- +
function safelnitialPrompt(X
*$(tb.offsetHeight)px*);
if(btnResetRules)(
else renderAll();
saveLocal();
Search
Jelsel

// OCR PHOTO 203: IMG_7331.HEIC
selector, Al Formula Copilot, direct in-cell editing, selected-row export, dd/mm/yyyy output
if(ITARGET_HEADERS.includes(state.aiFormulaField)) state.aiFormulaField = "Maturity
log("Centralised Blotter Mapping Studio ready with cross-asset support, Trade ID
if(lassetConfig().supportsAggregation && state.runMode !== "trade_by_trade")
if(!ASSET_CONFIGS[state.assetKey]) state.assetKey = "structured_fi";
of241 • /CD
if(tb) document.documentElement.style.setProperty("-topbar-h",
if(:TARGET_HEADERS.includes(state.processedSelectedField))
const tb = document.querySelector(".topbar");
dates, and Golden Record protection", "success");
202
state.processedSelectedField = "*Trade Date";
state.runMode = "trade_by_trade";
- +
refreshSelectOptions();
safelnitialPrompt();
*$(tb.offsetHeight)px*);
bindStaticEvents();
setReady("ready");
function init(l
loadLocal();
Search
renderAll();
Date";
</script>
init();

// OCR PHOTO 204: IMG_7332.HEIC
if(typeof state.savedSettingsJsonEditor === "undefined") state.savedSettings/sonEditor =
if(typeof state.selectedSavedProfiteld === "undefined") state.selectedSavedProfileld =
if(typeof state.savedSettingsJsonError === "undefined") state.savedSettings/sonError =
const PROFILE_BACKUP_KEY = STORAGE_KEY + *__backup_before_profite_load":
if(typeof state.profileUserName === "undefined") state.profileUserName = ***;
if(window._CENTRALISED_MAPPING_SAVED_SETTINGS_PATCHED) return;
window._CENTRALISED_MAPPING_SAVED_SETTINGS_PATCHED = true;
CD
if(typeof state.profileUserld === "undefined") state.profiteUserld = **;
if(typeof state.savedProfiles === "undefined") state.savedProfites = 0;
const PROFILE_SCOPE_APP = "centralised_mapping_studio";
if(typeof state.savedSettingslmportFileName === "undefined")
if(typeof state.savedSettingsLastValidation === "undefined")
of 241
const PROFILE_TABLE = "user_mapping profile_store";
Set(["default","source","constant","'formula","blank"]);
203
const ALLOWED_RULE_MODES = new
state.savedSettingsImportFileName = ';
state.savedSettingsLastValidation = null;
function injectSavedSettingsStyle()
- +
function ensureStateFields(X
Search
(function()
< script>
null;

// OCR PHOTO 205: IMG_7333.HEIC
-saved-settings-note|font-size:12px;color:var(--muted);line-height: 1.45)
if(document.getElementByld("saved-settings-patch-style")) return;
.btn.blue:hover(background:#1849a9;border-color:#1849a9}
.btn.blue{background:var(--blue);border-color:var(--blue)}
.saved-settings-selected{background:#fff1f2 limportant}
of 241
const style = document.createElement("style");
204
font-family:Consolas, Monaco, monospace;
style.id = "saved-settings-patch-style";
- +
border:1px solid #1e293b;
background:#0f172a;
style.textContent=
saved-json-editorf
min-height:420px;
.saved-json-toolbar{
align-items:center;
.saved-validation-list{
line-height:1.45;
resize:vertical;
font-size:12px;
width: 100%;
color:#e2e8f0;
flex-wrap:wrap;
display:flex;
Search
gap:8px;

// OCR PHOTO 206: IMG_7334.HEIC
const pillProfiles = document.createElement(*span*);
const pillsWrap = document.querySelector(".pills*):
const pillUser = document.createElement(*span");
205
.saved-library-table tbody tr/cursor:pointer)
pillProfiles.textContent = "Saved Profiles: 0";
document.head.appendChild(style);
pillProfiles.className = "pill purple";
function ensureSavedSettingsDom(H
pillsWrap.appendChild(pillProfites);
if(pillsWrap && !qs("pillProfiles*)X
pillUser.className = "pill amber";
- +
pillProfiles.id = "pillProfiles";
pillUser.id = "pillProfiteUser";
margin:8px 0 0 18px;
saved-library-metal
coloriver(muted);
line-height:1.5;
font-size:11px;
line-height:1.35;
padding:0;
Search

// OCR PHOTO 207: IMG_7335.HEIC
o24•ED
const activityTab = tabbar.querySelector('[data-tab="activityLog"]');
if(tabbar && Itabbar.querySelector('[data-tab="savedSettings"]')X
const controls = document.querySelector(".controls*);
const tabbar = document.querySelector(".tabbar");
if(activityTab) tabbar.insertBefore(tab, activityTab):
const btn = document.createElement("button");
const tab = document.createElement("button");
tab.onclick = () →> setActiveTab(savedSettings");
pillUser.textContent = "Profile User: loading";
206
if(controls && !qs("btnOpenSavedSettings")X
pillsWrap.appendChild(pillUser);
btn.id = "btnOpenSavedSettings";
btn.className = "btn secondary";
btn.textContent = "Saved Settings";
tab.dataset.tab = "savedSettings";
tab.textContent = "Saved Settings";
else tabbar.appendChild(tab):
controls.appendChild(btn);
tab.className = "tab";
btn.type = "button";
Search

// OCR PHOTO 208: IMG_7336.HEIC
const res = await fetch(url, Object.assign ({ credentials:"include" }, options || 0));
throw new Error(data.error || data.message || "Request failed");
activityView.parentNode.insertBefore(div, activityView);
of 241
document.querySelector(".shell").appendChild(div);
if(!res.ok) throw new Error(* HTTP $(res.status}* );
207
const div = document.createElement("div");
if(activityView && activityView.parentNode)
return ''' + String(v ?? ""). replace(/'/g, '*)+ ™*
const activityView = qs("activityLog");
async function apiJson(url, options)(
if(data && data.success === false)
const data = await res.json();
if(!qs("savedSettings")X
div.id = "savedSettings";
div.className = "view";
function sqlString(v)(
Search
return data;
Jelsel

// OCR PHOTO 209: IMG_7337.HEIC
"scope_app TEXT NOT NULL DEFAULT 'centralised_ mapping_studio, "+
return await apiJson("/studio/api/database/workspace/sql". (
"CREATE TABLE IF NOT EXISTS" + PROFILE_TABLE + *(° +
"created_at TIMESTAMPTZ NOT NULL DEFAULT NOW), "+
"updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW), "*+
of 241
return sqlString(JSON.stringify(obj?? ()) + ":jsonb";
"is_default BOOLEAN NOT NULL DEFAULT FALSE,"+
headers:( "Content-Type":"application/json* ),
208
async function ensureSavedSettingsTable(X
"profile_name TEXT NOT NULL, *+
"id BIGSERIAL PRIMARY KEY, "+
"payload JSONB NOT NULL, *+
body: JSON.stringify({ sql})
"user_id TEXT NOT NULL, " +
async function apiSql(sqlX
function jsonSql(obj)
"asset_key TEXT, *+
method:"POST",
const createSql =
"note TEXT, "+
Search
7):

// OCR PHOTO 210: IMG_7338.HEIC
"CREATE INDEX IF NOT EXISTS idx_user_mapping profile_store_user_scope_asset*+
return ASSET_CONFIGS[key] ? ASSET_CONFIGS[key].label : (key || "Unknown");
const data = await apiJson("/studio/api/user_name", { method:"GET" });
manualOutputOverrides: clonePlain(state.manualOutputOverrides),
"ON" + PROFILE_TABLE + " (user_id, scope_app, asset_key)";
fieldRulesByAsset: clonePlain(state.fieldRulesByAsset),
of 241
"UNIQUE (user_id, scope_app, profite_name)* +
state.profileUserName = data.user_name|| "*;
209
state.profileUserld = data.user_id|| ";
function buildSavedSettingsPayloadl
async function loadProfileUser()
function profileAssetLabel(keyX
updateSavedSettingsPills();
await apiSql(createSql);
assetKey: state.assetKey,
runMode: state.runMode,
await apiSql(indexSql);
const indexSql =
Search
*)*:
return {

// OCR PHOTO 211: IMG_7339.HEIC
profileName: cleanText(profiteName || *$(assetConfig().label} - current draft"),
const effectivePayload = clonePlain(payload || buildSavedSettingsPayload():
processedQuickSelectCount: state.processedQuickSelectCount,
function buildSavedSettingsEnvelope(profileName, note, payload
exportExcludedRows: clonePlain(state.exportExcludedRows)
assetKey: effectivePayload.assetKey || state.assetKey,
of 241
throw new Error(*Imported JSON must be an object");
if(!obj || typeof obj !== "object* || Array.isArray(obj)X
addonWindowDays: state.addonWindowDays,
210
aiFormulaPrompt: state.aiFormulaPrompt,
aiFormulaField: state.aiFormulaField,
exportedAt: new Date().tolSOString().
function unwrapSavedSettings/son(obj)
tradeldMode: state.tradeldMode,
scopeApp: PROFILE_SCOPE_APP,
note: cleanText(note || '*),
payload: effectivePayload
profileVersion: 1,
Search
return (

// OCR PHOTO 212: IMG_7340.HEIC
if(obj.payload && typeof obj.payload === "object" && !Array.isArray(obj.payload)X
assetKey: obj.assetKey || obj.asset_key || obj.payload.assetKey || *™
of241| • | [D
profileName: cleanText(obj.profileName || obj.profile_name || ''),
if(!payload || typeof payload !== "object" || Array.isArray(payload)X
function validateSavedSettingsPayload(payload)(
errors.push("Payload must be a JSON object.");
211
return { ok:false, errors, warnings );
note: cleanText(obj.note || ''*),
assetKey: obj.assetKey || "
payload: obj.payload,
const warnings = [);
envelope: obj,
envelope: null,
profileName: "
const errors = 0;
payload: obj,
Search
return {
return{
note: "*,

// OCR PHOTO 213: IMG_7341.HEIC
I["business_key","native_ref_preferred","legacy_row_order*].includes(payload.tradeldMode)
if(payload.addonWindowDays !=- undefined && payload.addonWindowDays !== nully
errors.push("addonWindowDays must be a number between 0 and 30.");
errors.push(* Unsupported tradeldMode "$(payload.tradeldMode]".*);
I("trade_by_trade" 'aggregate _addons"].includes(payload.runMode)K
if(payload.assetKey &&!ASSET_CONFIGS[payload.assetKey]X
errors.push(* Unsupported assetKey "$(payload.assetKey)".*):
errors.push( Unsupported runMode "$(payload.runMode]*.*):
if(payload.processedQuickSelectCount |== undefined &&
const n = Number(payload.processedQuickSelectCount);
of 241
const n = Number(payload.addonWindowDays);
212
payload.processedQuickSelectCount |== nullX
if(Number.isFinite(n) || n < 0 || n > 30X
- +
if(!Number.isFinite(n) |[n < 1X
if(payload tradeldMode &&
if(payload.runMode &&
Search

// OCR PHOTO 214: IMG_7342.HEIC
if(payload.aiFormulaField && ITARGET_HEADERS.includes(payload.aiFormulaField)X
warnings.push(* fieldRulesByAsset contains unknown asset key "$(assetKey)'.*):
if(!payload.fieldRulesByAsset || typeof payload.fieldRulesByAsset !=- "object* ||
Object.entries(payload.fieldRutesByAsset).forEach(([assetKey, ruleMap]) => {
warnings.push( Rute field "$(field)' under asset "$(assetKey)" is not in
warnings.push(* aiFormulaField "$(payload.aiFormulaField)" is not in
errors.push(*fieldRulesByAsset must be an object keyed by asset.");
if(!ruleMap || typeof ruteMap !== "object" || Array.isArray(ruteMap)X
errors.push( Rules for asset "$(assetKey)" must be an object.*);
errors.push(*processedQuickSelectCount must be at least 1.*):
ot 241
Object.entries(ruteMap).forEach(([field, rule]) → [
213
if(payload.fieldRulesByAsset !== undefinedy
Array.isArray(payload.fieldRulesByAsset)X
if(:TARGET_HEADERS.includes(field)X
Ff(IASSET_CONFIGS[assetKey]X
- +
TARGET_HEADERS.*):
TARGET_HEADERS.*):
Search
return;
jelsel

// OCR PHOTO 215: IMG_7343.HEIC
errors.push(* Source rute for "$(field}" under "$(assetKey)" requires sourceKey. *);
errors.push(^ Formula rule for "$(field}" under "$(assetKey]" requires expr.*);
errors.push(* Rute mode "$(rute.mode)" for "$(field}' under "$(assetKey)" is
errors.push(* Rule for "$(field]" under "$(assetKey)" must be an object.*):
if(typeof rule.sourceKey !== "string* || !trimFormulaText(rule.sourceKey)X
errors.push(" Formula syntax error for "$(fieldy" under "$(assetKey)":
const mode = cleanText(rule.mode ]| "default").toLowerCase():
Ff(rule || typeof rule !== "object" || Array.isArray(rule)X
of 241
const syntax = validateFormulaExpression(expr):
const expr - trimFormulaText(rule.expr || ***):
214
if(IALLOWED_RULE_MODES.has(mode)X
if(mode -=- "source"X
if(mode -=- "formuta"X
if(lsyntax.ok)
#(syntax.error}*):
return;
if(lexpr)
Search
invalid.*):
Jelsel

// OCR PHOTO 216: IMG_7344.HEIC
if(!payload.exportExcludedRows || typeof payload.exportExcludedRows !== "object" ||
if(!payload.manualOutputOverrides || typeof payload.manualOutputOverrides !==
DID
errors.push("manualOutputOverrides must be an object.");
state.savedSettingsJsonEditor = JSON.stringify(obj, null, 2);
*object" || Array.isArray(payload.manualOutputOverrides)X
errors.push("exportExcludedRows must be an object.");
of 241
if(payload.manualOutputOverrides !== undefined)
if(payload.exportExcludedRows !== undefined)
return { ok: errors.length === 0, errors, warnings };
215
Array.isArray(payload.exportExcludedRows)X
function setSavedSettingsEditorObject(objX
Search
-

// OCR PHOTO 217: IMG_7345.HEIC
state.savedSettings/sonError =vatidation.ok ? **= (validation.errors[0] ll "Validation
state.savedSettingsLastValidation = {okifalse, errors:[e.message], warnings:0 l:
const validation - validateSavedSettingsPayload(unwrapped.payload);
o241O I
const parsed - JSON.parse(state.savedSettings/sonEditor || *)");
state.savedSettings/sonEditor = getSavedSettingsEditorText();
return ta ? ta.value : (state.savedSettings/sonEditor || **):
const unwrapped = unwrapSavedSettings/son(parsed);
state.savedSettingsLastValidation - vatidation;
216
state.savedSettingsjsonError= e messaper
state.savedSettingsLastValidation = null;
const ta = qs(*savedSettings/sonEditor*):
function syncSavedSettingsEditorState(X
return { parsed, unwrapped, validation }:
function getSavedSettingsEditorText(X
function validateSavedSettingsEditor(X
state.savedSettings/sonError - 7*:
syncSavedSettingsEditorState);
Searoh
Jcatch(eX
failed"):
tryl

// OCR PHOTO 218: IMG_7346.HEIC
return <div class="note">Validate the JSON editor before applying or saving. The editor
$(state.savedSettingsImportFileName ?* <br/>Imported file: <span class="intine-
$(state.savedSettingslmportFiteName? * <br/>Imported file: <span class="inline-
$(v.warnings.length ? * <ul class="saved-validation-list">$(v.warnings.map(x →>
code">$(escapeHtm!(state.savedSettingsImportFileName)}</span>* : ***}
code">$[escapeHtmt(state.savedSettingsimportFileName)/</span>* : **}
accepts either a full profile envelope or a raw payload object.</div>
of 241
217
const v = state.savedSettingsLastValidation;
• <li>$(escapeHtml(x)J</Li>^).join(**)}</u\>*:**}
function validation PanelHtmt(X
<b>JSON validation failed.</b>
<div class="good-note">
<b>JSON is valid.</b>
<div class=*bad-note"→
return null;
Search
if(v.ok)(
if(.v)
return
</div>
return *

// OCR PHOTO 219: IMG_7347.HEIC
top:8px*><b>Warnings</b>cul class=*saved-validation-list" > $/v.warnings.map(x =>
return t.replace(/[*\w.-]+/g, *_*).replace(/^_+|_+$/g. **) || "saved_settings";
const blob - new Blob([text], {type: "application/json;charset=utf-8* });
<ut class="saved-validation-list">$((v.errors l [])-map(×=>
$((v.warnings && v.warnings.length) ? <div style=*margin-
of 241
* <li>$(escapeHtml(x)|</li>= )-join(**}</ul></div>* • **}
const t = cleanText(text || fallback || "saved_settings");
218
function applySavedSettingsPayload(payloadX
• <li>$(escapeHtml(x)|</li>* )-join(**))</ut>
function downloadTextFile(filename, text)(
const url - URL.createObjectURL(blob);
const a - document.createElement("a*);
function safeFileStem(text, fallback)
document.body.appendChild(a);
- +
URL.revokeObjectURL(url):
a.download - filename;
Search
a.href - url;
a.remove):
</div>
a.click:

// OCR PHOTO 220: IMG_7348.HEIC
Number(payload.processedQuickSelectCount 2? state.processedQuickSelectCount ?? 5)
if(lpayload || typeof payload!== "object") throw new Error(*Saved profile payload is
if(IASSET_CONFIGS[state.assetKey].supportsAggregation && state.runMode!==
state.tradeldMode = payload.tradeldMode || state.tradeldMode || "business_key":
state.aiFormulaPrompt = payload.aiFormulaPrompt || state.aiFormutaPrompt ||**;
if(payload.assetKey && ASSET_CONFIGS[payload.assetKey]) state.assetKey =
TARGET HEADERS.includes(payload.aiFormutaField) ? payload.aiFormulaField :
state.manualOutputOverrides = payload.manualOutputOverrides |I0;
state.runMode = payload.runMode || state.runMode || 'trade_by_trade";
state.addonWindowDays = Number(payload.addonWindowDays ??
state.exportExcludedRows = payload.exportExcludedRows |I0:
state.fieldRulesByAsset = payload.fieldRulesByAsset ||0:
of 241
state.aiFormulaField = payload.aiFormulaField &&
state.processedQuickSelectCount = Math.max(1,
219
state.runMode = "trade_by_trade";
state.addonWindowDays ?? 7) || 7;
- +
refreshSelectOptions):
state.aiFormulaField;
payload.assetKey:
"trade_by_trade"X
Search
saveLocal();
invalid"):
|| 5);

// OCR PHOTO 221: IMG_7349.HEIC
2m
log("Could not create saved settings backup: " + e.message, "warn");
if(raw) localStorage.setltem(PROFILE_BACKUP_KEY, raw);
const raw - tocalStorage.get/tem(PROFILE_BACKUP_KEY):
if(!raw) throw new Error('No saved settings backup found"):
if(sheetSelect) sheetSelect.value = state.sheetName;
ot 241
if(state.sourceRows.length) processCurrentSheet();
const raw = localStorage.getltem(STORAGE_KEY);
220
localStorage.setltem(STORAGE_KEY,raw):
const sheetSelect = qs("sheetSelect");
function restoreSavedSettingsBackupl
function backupSavedSettingsDraft(X
- +
autoSelectPreferredSheet();
parseSelectedSheet):
if(state.workbooky
else renderAll);
Search
return;
Jcatch(e)
tryl

// OCR PHOTO 222: IMG_7350.HEIC
"SELECT id, profile_name, asset_key, note, is_default, created_at, updated_at, payload *
"ORDER BY COALESCE(asset_key,"), is_default DESC, updated_at DESC, profile_name
if(state.selectedSavedProfileld && !state.savedProfiles.some(p => String(p.id) ===
setStatus("Restored the previous saved-settings backup.", "good");
"AND scope_app = " + sq(String(PROFILE_SCOPE_APP) + "*+
"WHERE user_id = " + sqlString(state.profileUserld) + ** +
state.savedProfiles = Array.isArray(res.rows) ? res.rows: [];
of 241
221
applySavedSettingsPayload(payload);
const payload = JSON.parse(raw);
async function fetchSavedProfiles(X
String(state.selectedSavedProfileld))X
state.selectedSavedProfileld = null;
"FROM" + PROFILE_TABLE +""+
if(!state.profileUserld) return;
function selectedSavedProfile(X
const res = await apiSql(sql);
updateSavedSettingsPills();
renderSavedSettings();
Search
const sql =
ASC";

// OCR PHOTO 223: IMG_7351.HEIC
return state.savedProfiles.find(p => String(p.id) === String(state.selectedSavedProfileld))
*"INSERT INTO" + PROFILE_TABLE + " (user_id, scope_app, profile_name, asset_key,
async function updateProfileRecord(profiteld, profileName, note, assetKey, payload)[
zm
async function insertProfileRecord(profileName, note, assetKey, payload)[
if(lstate.profileUserld) throw new Error("User context not ready");
of 241
"profile_name = "+ sq(String(profiteName) + ", "+
"note = " + (note ? sqlString(note) : "NULL") + ", "+
222
(note ? sqlString(note) : "NULL") + ", FALSE)";
"asset_key ="+ sqlString(assetKey ||***) + ", "+
sqlString(PROFILE_SCOPE_APP) + ", "+
"UPDATE" + PROFILE_TABLE + "SET" +
"WHERE id = " + Number(profiteld) + "*+
payload, note, is_default) VALUES ("+
sqlString(state.profiteUserld) + ", "+
"payload = "+ jsonSql(payload) + ","+
sqlString(profileName) + ", "+
sqlString(assetKey || "*) + ", *+
jsonSql(payload) + ", "+.
"updated_at = NOW()*+
await apiSql(sql);
Search
const sql =
const sql =
Il null;

// OCR PHOTO 224: IMG_7352.HEIC
", profile.note || "*
const payload = typeof profile.payload === "string" ? JSON.parse(profite.payload) :
zm
const envelope = buildSavedSettingsEnvelope(profile.profile_name || "*,
if(!profile) throw new Error("Please select a saved profile first");
"AND scope_app =" + sqlString(PROFILE_SCOPE_APP);
"AND user_id = " + sqlString(state.profileUserld) + ""+
of 241
const envelope = buildSavedSettingsEnvelope(
223
* $(assetConfig() .label} - current draft*,
setSavedSettingsEditorObject(envelope);
state.savedSettingslmportFileName= "'*;
setSavedSettingsEditorObject(envelope);
state.savedSettingsImportFiteName = "*;
function loadCurrentBoardToEditor(X
function loadSelectedProfiteToEditor(X
const profile = selectedSavedProfile();
buildSavedSettingsPayload()
validateSavedSettingsEditor();
validateSavedSettingsEditor();
renderSavedSettings();
await apiSql(sql);
profile.payload;
Search
payload);
**,

// OCR PHOTO 225: IMG_7353.HEIC
const envelope = buildSavedSettingsEnvelope(profite.profile_name || **, profile.note || **.
setStatus(* Exported saved profile <b>${escapeHtml(profite.profile_name ||**)}</b> as
const payload = typeof profile.payload === "string" ? JSON.parse(profile.payload) :
zm
const fileName = * $(safeFiteStem(envelope.profileName || *current_draft",
const fiteName = $(safeFileStem(profite.profite_name || "saved _profile",
setStatus("Exported the current board settings as JSON.', "good"):
if(lprofile) throw new Error("Please select a saved profite first");
downloadTextFile(fileName, JSON.stringify(envelope, null, 2));
downloadTextFite(fiteName, JSON.stringify(envelope, nutl, 2)):
0F241 0
const envelope = buildSavedSettingsEnvelope(
224
$(assetConfig().label)- current draft,
const profile - selectedSavedProfile();
function exportSelectedProfileJson(X
function exportCurrentDraftJson(X
buildSavedSettingsPayload()
renderSavedSettings":
saved_ profile")}json*:
"current_draft")}-json*:
profile.payload;
JSON.* "good"):
Search
payload);

// OCR PHOTO 226: IMG_7354.HEIC
setStatus("Imported settings JSON into the editor. Review, validate, then apply or save.",
W
if(!result) throw new Error(state.savedSettings)sonError || "Editor JSON is invalid");
state.savedSettingsLastValidation = { ok:false, errors:[e.message], warnings:[] };
zm
log(* Imported saved settings JSON file $(file.name)* , "success");
setStatus(* Imported file could not be parsed as valid JSON.
log(* Saved settings import failed: $(e.message), "error");
of 241
state.savedSettingsimportFileName = file.name |"*;
225
async function importSavedSettings File(fileX
state.savedSettings/sonError = e.message;
const result = validateSavedSettingsEditor):
async function applyEditor/sonToBoard)
state.savedSettings/sonEditor = text;
unwrapSavedSettings/son(parsed);
state.savedSettings/sonError = **;
const parsed - JSON.parse(text);
${escapeHtml(e.message)}* , "bad");
validateSavedSettingsEditor();
const text - await file.text):
renderSavedSettings):
Search
Jcatch(eX
"good");
tryl

// OCR PHOTO 227: IMG_7355.HEIC
if(!profileName) throw new Error(*Please include profiteName in the JSON before saving
if(!result) throw new Error(state.savedSettingsJsonError || "Editor JSON is invalid");
zm
throw new Error(result.validation.errors[0] || "Editor JSON failed validation*);
log("Applied saved settings JSON editor payload to current board", 'success");
throw new Error(result.validation.errors|0] || "Editor JSON faited validation");
setStatus("Applied JSON editor settings to the current board.", "good"):
const assetKey = payload.assetKey || env.assetKey || state.assetKey:
applySavedSettingsPayload(result.unwrapped.payload);
т241
const profileName = cleanText(env.profileName || ***);
226
async function saveEditorJsonAsNewProfile()
const resutt - validateSavedSettingsEditor();
const note = cleanText(env.note || **):
backupSavedSettingsDraft();
const env = result.unwrapped;
const payload = env.payload;
iftresult.validation.okX
renderSavedSettings():
if(.result.validation.ok){
as a new profite."):
Search

// OCR PHOTO 228: IMG_7356.HEIC
setStatus( Saved editor JSON as new profile <b>$(escapeHtml(profileName)}</b>.*,
W
if(!result) throw new Error(state.savedSettings)sonError || "Editor JSON is invalid");
if(!profileName) throw new Error("Please include profileName in the JSON before
zm
throw new Error(result.validation.errors[0] || "Editor JSON failed validation");
const profileName = cleanText(env.profileName || profile.profile_name |l"');
await updateProfileRecord(profile.id, profileName, note, assetKey, payload);
log(* Saved editor JSON as new profile $(profileName)' , "success");
const assetKey = payload.assetKey || env.assetKey || state.assetKey;
await insertProfileRecord(profileName, note, assetKey, payload);
it(Iprofile) throw new Error("Please select a saved profile first");
of 241
async function overwriteSelectedProfileFromEditor(){
227
const result = validateSavedSettingsEditor();
const profile = selectedSavedProfile();
const note = cleanText(env.note || "*);
const env = result.unwrapped;
const payload = env.payload;
await fetchSavedProfiles();
iflresult.validation.ok)
Search
overwriting.");
"good");

// OCR PHOTO 229: IMG_7357.HEIC
setStatus( Overwrote selected profile <b>${escapeHtml(profileName)}</b> using the
log(* Overwrote selected saved profite from JSON editor: $(profileName), "warn");
pillProfileUser.textContent = * Profile User: $(state profileUserName || "loading"}* ;
pillProfileUser.className = * pill $(state.profileUserName ? "green" : "amber"y*;
zm
pillProfiles.textContent = Saved Profiles: $(state.savedProfiles.length) ;
return * <div class="empty">No prior saved settings found yet.</div>* ;
o$ 241
state.selectedSavedProfileld = String(profile.id);
228
const pillProfileUser = qs("pillProfileUser");
pillProfiles.className = "pitt purple";
function renderSavedSettingsLibrary(X
function updateSavedSettingsPills(X
const pillProfiles = qs("pillProfiles");
if(!state.savedProfiles.length)(
await fetchSavedProfiles(;
renderSavedSettings):
JSON editor.*, "good");
if(pillProfileUserX
if(pillProfilesX
Search

// OCR PHOTO 230: IMG_7358.HEIC
str class="$(String(p.id) === String(state.selectedSavedProfileld) ? "saved-settings-
<td class="wrap-table">$(escapeHtml(cleanText(p.note || "'*'*) || "_"}</td>
zm
<td>$(p.is_default ? badge("Default","green") : badge("No",'')}</td>
<td>${escapeHtml(formatDateTimeDisplay(p.updated_at)))</td>
<div class="saved-library-meta">Click to view JSON</div>
<div><b>${escapeHtml(p.profile_name ||"*)}</b></div>
<td>${escapeHtml(profileAssetLabel(p.asset_key))}</td>
selected" : "''" data-saved-profile-id="${escapeHtml(p.id)}">
of 241
229
<th class="wrap-table">Note</th>
<table class="saved-library-table">
$(state.savedProfiles.map(p =>
<div class="table-wrap">
<th>Updated</th>
<th>Profile</th>
<th>Default</th>
<th>Asset</th>
<thead>
</thead>
").join(')}
Search
<tbody>
return
</tr>
<td>
</td>
</tr>

// OCR PHOTO 231: IMG_7359.HEIC
if(existingEditor) state.savedSettings/sonEditor = existingEditorvalue;
const editorText = state.savedSettings/sonEditor || JSON.stringity(
of 241
const existingEditor = os/*savedSettings/sonEditor*):
230
const selected = selectedSevedProfile0a
*$(assetConfig().label) - current draft,
const view = qs("savedSettings"):
- +
function renderSavedSettings(X
buildSavedSettingsEnvelopel
buitdSavedSettingsPayload)
< section class="card section">
view.innerHTML=*
if(!view) return;
</tbody>
Search
</table>
</div>
014e
null,

// OCR PHOTO 232: IMG_7360.HEIC
<div class="note">Use JSON to move settings between board states. Import first into
<button id="btnSavedLoadCurrentToEditor" class="btn ghost">Load Current Board To
<button id="btnSavedExportCurrent" class="btn secondary">Export Current Board
<button id="btnSavedLoadSelectedToEditor" class="btn ghost" $(selected?"' :
<input id="savedSettingsImportFile" type="file" accept=".json,application/json"
<button id="btnSavedExportSelected" class="btn secondary" $(selected ? "' :
zm
<button id="btnSavedImportJson" class="btn secondary">Import Settings
<div class="saved-settings-note" style="margin-bottom:12px">
Click any saved setting below to view its JSON in the editor.
<div class="section-title">Import / Export Settings</div>
<div class="section-title">Prior Saved Settings</div>
of 241
"disabled")>Export Selected Profile JSON</button>
"disabled"]>Load Selected Profile To Editor</button>
231
the editor, validate, then apply or save.</div>
$renderSavedSettingsLibrary(}
<section class="card section">
- +
<div class="action-row">
<div class="toolbar">
style="display:none" />
</section>
JSON</button>
JSON</button>
Editor</button>
Search
</div>
</div>
</div>

// OCR PHOTO 233: IMG_7361.HEIC
<label style="display:block;font-size:10px;font-weight:700;color:var(--muted);text-
zm
transform:uppercase;letter-spacing:.05em;margin-bottom:6px">Saved settings
const btnLoadSelectedToEditor = qs("btnSavedLoadSelectedToEditor");
<textarea id="savedSettings)sonEditor" class="saved-json-editor"
const btnLoadCurrentToEditor = qs("btnSavedLoadCurrentToEditor");
const btnSaveJsonAsNew = qs("btnSavedSaveJsonAsNew");
const btnExportSelected = qs("btnSavedExportSelected");
spellcheck="false">${escapeHtml(editorText)}</textarea>
const btnExportCurrent = qs("btnSavedExportCurrent");
const btnOverwriteJson = qs("btnSavedOverwritelson");
of 241
const btnValidateJson = qs("btnSavedValidateJson");
const btnResetEditor = qs("btnSavedResetEditor"):
const editorEl = qs("savedSettings)sonEditor");
state.savedSettings/sonEditor = e.target.value;
const btnApplyJson = qs("btnSavedApplyJson");
233
const btnlmport = qs("btnSavedImportJson");
editorEl.addEventListener("input", e => {
<div style="margin-top:12px">
JSON</label>
</section>
Search
if(editorEly
</div>
7):

// OCR PHOTO 234: IMG_7362.HEIC
<label style="display:block;font-size:10px;font-weight:700;color:var(--muted);text-
zm
transform:uppercase;letter-spacing:.05em;margin-bottom:6px">Saved settings
const btnLoadSelectedToEditor = qs(*btnSavedLoadSelectedToEditor");
<textarea id="savedSettings)sonEditor" class="saved-json-editor*
const btnLoadCurrentToEditor = qs("btnSavedLoadCurrentToEditor");
const btnSaveJsonAsNew= qs("btnSavedSaveJsonAsNew");
const btnExportSelected = qs("btnSavedExportSelected");
spellcheck="false">$(escapeHtml(editorText)J</textarea>
const btnExportCurrent = qs("btnSavedExportCurrent");
const btnOverwriteJson = qs("btnSavedOverwrite)son*);
of 241
const btnValidateJson = qs("btnSavedValidateJson");
const btnResetEditor = qs("btnSavedResetEditor");
state.savedSettings/sonEditor = e.target.value;
const editorEl = qs("savedSettings)sonEditor");
const btnApplyJson = qs("btnSavedApply)son");
233
const btnlmport = qs("btnSavedlmportlson*);
editorEl.addEventListener("input", e => {
<div style="margin-top:12px">
JSON</label>
</section>
Search
if(editorElK
</div>
3);

// OCR PHOTO 235: IMG_7363.HEIC
setStatus(* Could not import settings JSON: ${escapeHtml(err.message)}*, "bad");
setStatus(* Coutd not export selected profile JSON: $(escapeHtml(e.message)}*,
zm
btnlmport.addEventListener("click", () => importinput.click());
of 241
importinput.addEventListener("change", async e => {
const importinput = qs("savedSettingslmportFile");
btnExportSelected.addEventListener("click", () =>{
const file - e.target.files && e.target.files[0];
234
await importSavedSettingsFile(file);
- +
if(btnimport && importinputX
exportSelectedProfileJson();
if(btnExportSelected)[
e.target.value = **-
it(file) return:
Jcatch(errX
Search
Jcatch(e)[
tryl
3):
tryf
"bad");
7):

// OCR PHOTO 236: IMG_7364.HEIC
setStatus(* Could not export current board JSON: ${escapeHtml(e.message)}*, "bad"):
setStatus(* Could not load selected profile to editor: ${escapeHtml(e.message)}*,
zm
setStatus Loaded the current board settings into the JSON editor.", "good");
setStatus("Loaded the selected profile into the JSON editor.", "good");
btnLoadCurrentToEditor.addEventListener("click", () =>f
btnLoadSelectedToEditor.addEventListener(*click", () => 1
of 241
btnExportCurrent.addEventListener("click", () => {
235
loadSelectedProfileToEditor();
- +
load CurrentBoardToEditor();
exportCurrentDraftJson();
if(btnLoadCurrentToEditor)
if(btnLoadSelectedToEditor)
if(btnExportCurrent)
Search
Jcatch(e)(
Jcatch(e){
tryl
tryl
7):
3):
"bad");

// OCR PHOTO 237: IMG_7365.HEIC
state.savedSettingsLastValidation && state.savedSettingsLastValidation.ok? *good* :
setStatus(* Could not apply editor JSON: $(escapeHtml(e.message)}*, "bad");
state.savedSettingsLastValidation && state.savedSettingsLastValidation.ok
of 241
btnApplyJson.addEventListener("click", async ) => {
btnValidateJson.addEventListener("click", 0 →> {
236
? "JSON editor validation passed."
: "JSON editor validation failed.",
await applyEditorisonToBoard():
validateSavedSettingsEditor):
renderSavedSettings();
if(btnValidateJson)
if(btnApplyJsonX
setStatus(
Search
Jcatch(e)(
"warn"
tryl

// OCR PHOTO 238: IMG_7366.HEIC
setStatus(* Could not save editor JSON as a new profile: $(escapeHtml(e.message))*,
setStatus(* Could not overwrite selected profile from editor JSON:
btnSaveJsonAsNew.addEventListener("click", async ) => {
btnOverwriteJson.addEventListener("click", async () =>(
of 241
await overwriteSelectedProfileFromEditor();
btnResetEditor.addEventListener("click", 0 => (
237
await saveEditorJsonAsNewProfile);
$(escapeHtml(e.message)}*, "bad");
- +
renderSavedSettings();
if(btnSaveJsonAsNew)(
if(btnOverwriteJsonX
if(btnResetEditor)(
Search
Jcatch(eX
Jcatch(eX
tryf
"bad");
tryf
)):

// OCR PHOTO 239: IMG_7367.HEIC
setStatus(* Could not reset editor to selected profile: $(escapeHtml(e.message))* .
setStatus("Loaded the chosen saved settings JSON into the editor.", "good");
zm
setStatus("Reset the editor to the selected saved profile.", "good"):
setStatus ("Reset the editor to the current board settings.", "good");
setStatus(* Could not load chosen saved settings into editor:
state.selectedSavedProfileld = row.dataset.savedProfileld;
of 241
238
row.addEventListener("click", 0 =>{
loadSelectedProfileToEditor0:
loadCurrentBoardToEditor):
loadSelectedProfileToEditor();
$(escapeHtml(e.message)}*,"bad");
- +
if(selected)(
Jcatch(eX
Search
Jcatch(e)
try(
Jelse(
"bad");
)):
tryl

// OCR PHOTO 240: IMG_7370.HEIC
tryf renderSavedSettings(); Jcatch(eX( log("Saved settings render failed:" + e.message,
btnOpenSavedSettings.addEventListener("click", () => setActiveTab("savedSettings"));
zm
const savedTab = document.querySelector('.tab[data-tab="savedSettings"]*);
if(typeof originalUpdateTopPills === "function") originalUpdateTopPills();
of241 | | 0D
if(typeof originalRenderAll === "function") originalRenderAll();
const btnOpenSavedSettings = qs("btnOpenSavedSettings");
savedTab.onclick = ( => setActiveTab("savedSettings");
const originalUpdateTopPills = window.updateTopPills;
239
const originalRenderAll = window.renderAll;
function wireSavedSettingsStaticEvents(X
window.updateTopPills = function(X
- +
updateSavedSettingsPills();
window.renderAll = function(X
if(btnOpenSavedSettingsX
if(savedTab)
Search
"warn"); }

// OCR PHOTO 241: IMG_7371.HEIC
2m
log("Saved Settings enhencement ready", "success"):
async function initSavedSettingsEnhancement(X
240
* $(assetConfig().label} - current draft,
wireSavedSettingsStaticEvents(:
if(!state.savedSettings)sonEditor)
await ensureSavedSettingsTable):
setSavedSettingsEditorObject(
- +
buildSavedSettingsEnvelope(
buildSavedSettingsPayload(
injectSavedSettingsStyle():
ensureSavedSettingsDom():
updateSavedSettingsPills():
await fetchSavedProfiles):
updateSavedSettingsPills():
ensureStateFields();
renderSavedSettings):
await load ProfiteUser);
renderSavedSettings):
Search
tryl

// OCR PHOTO 242: IMG_7372.HEIC
setStatus(' Saved Settings are not fully available yet. $(escapeHtml(e.message)/,
window.addEventListener("DOMContentLoaded", initSavedSettingsEnhancement, (
of 241 |
log("Saved settings init faited: " + e.message, "warn");
241
if(document.readyState === "loading"X
initSavedSettingsEnhancement();
updateSavedSettingsPills();
renderSavedSettings();
fcatch(eX
once:true });
"warn"):
</script>
Jelsel
</body>
</html>
30:
