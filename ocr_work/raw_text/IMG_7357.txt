setStatus( Overwrote selected profile <b>${escapeHtml(profileName)}</b> using the
log(* Overwrote selected saved profite from JSON editor: $(profileName), "warn");
pillProfileUser.textContent = * Profile User: $(state profileUserName || "loading"}* ;
pillProfileUser.className = * pill $(state.profileUserName ? "green" : "amber"y*;
zm
pillProfiles.textContent = Saved Profiles: $(state.savedProfiles.length) ;
return * <div class="empty">No prior saved settings found yet.</div>* ;
o$ 241
state.selectedSavedProfileld = String(profile.id);
228
const pillProfileUser = qs("pillProfileUser");
pillProfiles.className = "pitt purple";
function renderSavedSettingsLibrary(X
function updateSavedSettingsPills(X
const pillProfiles = qs("pillProfiles");
if(!state.savedProfiles.length)(
await fetchSavedProfiles(;
renderSavedSettings):
JSON editor.*, "good");
if(pillProfileUserX
if(pillProfilesX
Search
